# Auto-generated by analyze-project-structure rule
# Generated at: 2025-11-28T03:06:00Z
# Project: albatrion

project:
  name: "albatrion"
  type: "monorepo"
  description: "Monorepo for React form libraries and JSON Schema utilities"

structure:
  root: "."
  packages_dir: "packages"
  source_dir: "src"
  tests_dir: "__tests__"
  docs_dir: "documents"
  tasks_dir: ".tasks"

package_manager:
  type: "yarn"
  workspace_command: "yarn workspace"

commands:
  dev:
    schema_form: "yarn workspace @canard/schema-form storybook"
    all: "yarn run:all dev"
  test:
    schema_form: "yarn workspace @canard/schema-form test"
    json_schema: "yarn workspace @winglet/json-schema test"
    all: "yarn run:all test"
  lint:
    schema_form: "yarn workspace @canard/schema-form lint"
    all: "yarn run:all lint"
  build:
    schema_form: "yarn workspace @canard/schema-form build"
    all: "yarn run:all build"
  typecheck:
    schema_form: "yarn workspace @canard/schema-form typecheck"
    all: "yarn run:all typecheck"
  custom:
    storybook: "yarn workspace @canard/schema-form storybook"
    import_test: "./scripts/test-package-import.sh"

tech_stack:
  frontend:
    framework: "react"
    language: "typescript"
    ui_library: "custom"

  testing:
    unit: "vitest"
    e2e: "playwright"
    mocking: "msw"

  build_tools:
    bundler: "rollup"
    compiler: "esbuild"

development:
  ports:
    storybook: 6006

path_conventions:
  component_path: "packages/{scope}/{package}/src/components/{ComponentName}/{ComponentName}.tsx"
  test_path: "packages/{scope}/{package}/src/**/__tests__/*.test.ts"
  story_path: "packages/{scope}/{package}/coverage/*.stories.tsx"

naming_conventions:
  components: "PascalCase"
  files: "PascalCase"
  directories: "kebab-case"
  variables: "camelCase"

# Main packages in monorepo
examples:
  packages:
    - name: "schema-form"
      scope: "@canard"
      type: "library"
      path: "packages/canard/schema-form"
      description: "React form generation from JSON Schema"
    - name: "json-schema"
      scope: "@winglet"
      type: "library"
      path: "packages/winglet/json-schema"
      description: "JSON Schema manipulation utilities"
    - name: "common-utils"
      scope: "@winglet"
      type: "library"
      path: "packages/winglet/common-utils"
      description: "Common utility functions"
    - name: "react-utils"
      scope: "@winglet"
      type: "library"
      path: "packages/winglet/react-utils"
      description: "React-specific utilities"
    - name: "promise-modal"
      scope: "@lerx"
      type: "library"
      path: "packages/lerx/promise-modal"
      description: "Promise-based modal management"
