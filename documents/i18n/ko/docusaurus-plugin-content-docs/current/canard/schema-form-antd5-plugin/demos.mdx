---
sidebar_label: '컴포넌트 카탈로그'
sidebar_position: 2
---

import BrowserOnly from '@docusaurus/BrowserOnly';

# Ant Design 5 컴포넌트 카탈로그

`@canard/schema-form-antd5-plugin`이 제공하는 18개 폼 입력 컴포넌트의 인터랙티브 카탈로그입니다.
각 컴포넌트는 JSON Schema의 `type`, `format`, `formType`, `enum` 속성에 따라 자동으로 선택됩니다.

<BrowserOnly fallback={<div>카탈로그 로딩 중...</div>}>
  {() => {
    const Demo = require('@site/src/components/demos/catalog/Antd5CatalogDemo').default;
    return <Demo />;
  }}
</BrowserOnly>
