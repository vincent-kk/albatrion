---
sidebar_label: 'Component Catalog'
sidebar_position: 2
---

import BrowserOnly from '@docusaurus/BrowserOnly';

# Ant Design Mobile Component Catalog

Interactive catalog of all 10 form input components provided by `@canard/schema-form-antd-mobile-plugin`.
Each component is automatically selected based on JSON Schema properties like `type`, `format`, `formType`, and `enum`.

<BrowserOnly fallback={<div>Loading catalog...</div>}>
  {() => {
    const Demo = require('@site/src/components/demos/catalog/AntdMobileCatalogDemo').default;
    return <Demo />;
  }}
</BrowserOnly>
