/** Placeholder for `push`'s `Promise<number>` return on the locked path. */
export const RESOLVED_LENGTH: Promise<number> = Promise.resolve(0);

/** Placeholder for `clear`'s `Promise<void>` return on the locked path. */
export const RESOLVED_VOID: Promise<void> = Promise.resolve();
