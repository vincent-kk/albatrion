# 10라운드 — 소유자의 답 (원문)

2026-09-23. 소유자가 `round-10-owner-review.md`를 deilen 뷰어에서 읽고 줄마다 남긴 답의 원문이다. 편집자는 고치지 않았다. 해석과 반영은 `../07-conclusions.md` 5절 머리의 "답 반영" 표에 있다.

| 항목 | 소유자의 답(원문) |
| --- | --- |
| A-1 채움은 노드가 생길 때 한 번 | 예상이 맞다. |
| A-2 노드 게이트 통합 | 기존과 달리 의미를 통일했으니 이렇게 해도 무방하다. 단, 이는 검증과 무관하므로, 잘못된 스키마에 대한 책임은 사용자에게 있고, form은 고지 의무만 진다. |
| A-3 폼은 분기를 고르지 않는다 | 분기를 고르지 않으나, if-then-else 문법은 예외적으로 분기를 결정하는 것에 쓰인다. 내 이해가 맞나? (예시 스키마) 이 구조에서 `"if": { "properties": { "kind": { "const": "card" } }, "required": ["kind"] }` 이 조건식에 따라, `"then": {...}`의 스키마가 자식노드에게 전달되고, 또한 이들 필드가 노출된다. 즉, &를 제외하고 schema 만으로 form 을 제어하는 방법이 if-then-else 인 것이다. |
| A-4 정착 안 에지 소비 | 루프의 가능성을 제한하지는 않는다. 이때문에 순환된 값이 진동하거나 발산할 수 있으며, 이를 막기 위한 상한값이 있고, 그 상한값을 초과하면 적절한 error 를 표시한다. 이는 react 의 hook 과 동일한 설계를 갖는다. |
| B-22 `&discriminator` | 동의합니다. 이 경우에 대한 예외적 허용을 하죠. 이를 통해 스키마를 통해서 form을 제어하는 방법은 if-then-else 와 oneOf / anyOf 의 const 와 enum 이 됩니다. 그럼 현행과 같은거같긴 하군요. 다만, (첫 예시)가 아니라 `{ "oneOf": [ { "&active": "../kind === 'card'", "properties": { "kind": { "const": "card" }, "cardNumber": {...} }, "required": ["kind", "cardNumber"] }, ... ] }` 이거죠? 우리는 "&active": 을 더하는거지 스키마를 수정하는건 아니니까 |
| B-12 루트 `readOnly` | 이건 "읽기전용 form 인 경우" 등을 구현하기 위한 구조라서 props 로 전달되는 글로벌 값이 개별 값을 덮도록 하는게 맞습니다. rootJSONSchema 도 props 와 동치이므로 그렇게 읽히지 않을까 싶군요 |
| B-1 예산 초과 시 제출 차단 | 권고를 따릅니다만, 기본적으론 form 을 터트려서(error 를 throw 해서) 알려주는게 좋지 않을까 싶네요... 이 루프가 만들어지는건 기본적으로 schema를 잘못 짰기 때문이니까, 런타임에 발생할 확률은 비교적 낮겠고요. 그리고 다시 말하지만, 구태여 막지 않을 뿐이지 루프를 만드는걸 권하는 설계는 절대 아닙니다. |
| C-10 노드 게이트 | A2에서 답변함 |
| C-11 런타임 채움 키 | 네. 따로 두지 않습니다. 다만, 개별 formTypeInput 에서는 undefined 를 onChange 로 전달하는게 값을 빼는 것으로 동작할겁니다. |
| C-15 `&children`은 상태 키만 | 값을 빼거나, 넣는 값도 일부 있긴 해야합니다. 예컨대 A 브랜치가 비활성화 될때, A의 값을 제거할지 남겨둘지, 혹은 변경할지, 그리고 input을 초기화할지 등을 제어해야 해서요. 좀 복잡해져도 거의 풀펑션을 지원하길 바랍니다. |
| C-20 `oneOf` 다중 활성 경고 | 예 이건 jsonSchema 스팩을 존중합니다 |
| C-2 `Merge` + `undefined` | 예 맞습니다 |
| D-6 로드 시 `&injectTo` | 동의 |
| D-7 조각의 주석이 본체를 덮음 | 네, 세부적인 규칙이 포괄적인 규칙을 덮는 기존 관례를 따릅니다 |
| D-14 `&children`의 형태 | readOnly, visible 같은거에 &를 붙이는건 좀 이상하긴 하니까 control(or controls 적절한거 선택) 블록으로 묶으면 어때요? target 과 control 로 분리하는거죠. |
| D-17 켜진 조각끼리 나중 승 | 예 |
| E-8 `&clearValue`가 이김 | 예 이깁니다 |
| E-9 `&injectTo` 대 `&derived` | 이게 충돌하는게 좋은 상황은 아닙니다만, derived 가 이깁니다 |
| E-13 중간 객체의 잠금 전파 | 이게 좀 애매합니다. 기본적으로 본인의 readOnly 조건을 글로벌 조건이 이기는 설계입니다. 그 이유는 앞서 말한대로, form 세부 정보와 무관하게 모두 잠궈버려야 하는 조건이 있어서 그렇습니다. (그게 아니면 제어가 어렵습니다. 예컨대 readOnly 상태로 값을 보기만 하는 뷰일때) 그래서 저는 글로벌과 로컬만 보고, 중간단계 상태 상속은 구현을 하지 않으려고 합니다 |
| E-16 무조건 조각 나중-승 통일 | 예. 이미 답함 |
| E-18 `options` 병합 | 깊은 병합을 했으면 합니다. 그 이유는 커스텀 props는 (FormTypeInputProps 같은) 내부에 객체타입을 가질 수도 있고 함수나 핸들러를 가질 수도 있어서... 이때는 common-utils 의 merge 를 쓰죠 |
| E-19 2항의 개발 모드 검사 | 추가 정보를 원합니다. 다음 라운드에서 보강해서 다시 질문하세요. 무슨소린지 모르겠음 |
| E-21 로드 시 `&clearValue` 에지 | 이건 어떠려나요? 이전 상태와 무관하게 현재 데이터를 기준으로 true 면 지우고, false 면 안지워야 할거같은데요. 이전 상태는 중요치 않을거같아요 |
| E-23 `required` 누락 경고 | 게이트를 가진 분기는 required 가 필수에요? if 문 자체를 통으로 jsonSchema 평가하는거 아닙니까? 그럼 표현에 한계가 없을텐데요? |
| E-4 `virtual` | 네, virtual 은 스키마로 선언되는건 아니니까 그냥 둡시다. 유효성검증도 영향 없고. |
| E-5 `&pristine` 이름 | 동의 |

## 되물음에 대한 답 (같은 날, 대화)

| 항목 | 소유자의 답(원문) |
| --- | --- |
| E-23 `required` 누락 경고 | 이해했습니다. 다만, 이건 우리가 참견할 문제는 아닙니다. 평가하지 않습니다. |
| E-21 로드 시 `&clearValue` | 최초 로드시에입니다. 런타임에는 false->true, true->false 경계에서만 동작하는걸 생각합니다. |
| E-19 2항의 개발 모드 검사 | 23에서 말한대로 굳이 안봐도 되지않나 싶습니다. 이렇게 말하는 이유는 그냥 if 문의 스키마로 지금 값을 평가하면 되는거라 굳이 그런 규칙은 없어도 될거같습니다. 말한대로 하는게 안전하긴 할텐데, if 문 내에 anyOf 나 oneOf, 아니면 다른 또 복잡한 스키마가 올 수도 있는거라 우리는 그걸 관여하기로 하면 끝이 없을거에요 |
