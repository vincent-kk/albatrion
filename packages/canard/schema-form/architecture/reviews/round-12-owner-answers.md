# 12라운드 — 소유자의 답 (원문)

2026-09-24. 소유자가 `round-12-owner-review.md`를 deilen 뷰어에서 읽고 줄마다 남긴 답의 원문이다("Continue in chat"). 편집자는 고치지 않았다. 해석과 반영은 `round-12-derivation.md`(작성 예정)와 원장에 있다.

| 위치 | 항목 | 소유자의 답(원문) |
| --- | --- | --- |
| §1 권고 | 1 Form 속성 `false` | 권고 수용. false 일때는 로컬 동작을 보장한다. 이는 props 와 global 로 전달되는 경우에 대한 특별 동작이다. |
| §1 함께 정할 것 | 1 글로벌 안의 충돌 | 그냥 jsonSchema 상의 글로벌 readOnly 와 disabled 스팩은 제거해버릴까? 생각보다 성가시네 / 레드팀 공격 요청 |
| §2 권고 | 2 `&discriminator` | 명시 필수 수용. |
| §3 권고 | 3 꺼질 때 값 정책 | 동의. 조각이 꺼질때 해당 값을 "제거"할 것인지는 별도 옵션을 쓴다. 단, 기본값은 true, false 로 끌 수 있도록 한다. 이는 omitEmpty 와 동일하게, 빼는게 기본 동작이라고 가정한 구성이다. 레드팀 공격 필요 |
| §3 원칙이 답하지 않는 부분 | 3 중복 선언 | 해당 조건은 중복 선언시(부모선언 + 자식노드 자체 선언) 이를 병합하는게 아니라 덮어쓰는걸 고려했다. 하지만 이를 or 조건이나 체인으로 엮을 수 있나? 말한대로 루프가 발생하면 form 을 터트리고 오류를 보여주면 된다. 이 기능은 설계를 요한다. |
| §4 권고 | 4 프로덕션 throw | 동의한다. 개발모드에선 throw, 이를 무시하고 리얼에 나간 경우, "어떻게든 동작하는 형태"를 고수한다. 이는 리엑트와 동일 설계이다. |
| §5 배경 | 5 `&clearValue` 두 문장 | 동의한다. 권고를 수용한다. resetInteraction 동작도 clearValue 와 동일하다. 다만 한가지, unsetValue 가 낫지 않나? clear 가 나은가. |
| §6 권고 | 6 `else: false` 경고 | 둔다. 이는 막을 수 있는 간단한 실수이므로. 그 이상의 스키마 개입은 하지 않는다. 그냥 스키마의 해석에 따른 에러만 있을 뿐 form 으로서 대응하진 않는다 |
| §7 권고 | 7 "input을 초기화" | 이 내용은 기반지식에 근거한다. 현재 formType 은 개별 노드에 대해 touched 와 dirty 를 따로 관리하며, 이 값이 error의 노출에 관여한다. 이때문에 해당 노드에 값을 쓰거나 unset 하거나 초기화할때, 해당 노드의 "상태"가 초기화되지 않는 문제가 있다. 이는 사용자에게 입력한 적 없는 오류를 보여주게 되어서 추가된 필드이다. 즉, 가. dirty·touched 초기화(resetInteraction) 가 맞다. |
| §8 권고 | 8 `virtual` | 제거해도 무방하나 node 로서는 동일한 대우를 받아야 한다. virtual node 설정은 기본적으로 커스텀 필드이니까 제거할 필요가 없지않나 싶지만, 지금처럼 다른 필드 전처리를 해야한다면 제거해도 무방하다. 우리는 투명한 jsonSchema 를 추구하므로, virtual 여부와 무관한 실제 필드 명시를 요구하는 바이다. |
| §9 같은 순위끼리 | | 동의 |
| §9 형상에 없는 노드 | | 동의 |
| §9 로드에서 `&derived` | | 동의 |
| §9 `undefined` 반환 | | 동의. 이는 자칫 ealry return 과 혼동이 발생해서 문제가 있었다 |
| §9 조각 식의 경로 기준 | | 모든 JsonPointer 는 선언된 node 를 기준으로 처리된다. 이때 .. 나 . 는 확장 커스텀 표식이므로 form 이 정당하고 일관되게 처리해야 한다. |
| §9 게이트 없는 분기 안의 `if/then` | | 게이트 없는 분기의 if-then은 뭘 의미하는가? 이때도 if 문에 따른 then / else 동작이 해석되어야 하는것 아닌가? |
| §9 로컬 층 안의 결합 | | 좀 더 설명 필요. 이것만으론 이해가 잘 안된다. 글로벌이 덮을 수 있는건 readOnly / disabled 뿐이고, 나머지는 글로벌 선언과 무관하게 각자의 스콥에서만 동작하지 않나? |
| §9 `&options` | | 1. options 도 & 키워드를 가지나? 컨트롤이 아닌데? 2. 대체로 맞음. 병합 불가한 함수는 나중이 승, 병합 가능한 객체는 깊은 병합 |
| §9 `&derived`·`&injectTo` 충돌 | | 기본적으로 둘다 동작하는데, 이벤트 전파 순서에 따라 나중에 선언된 것이 이김. 경고를 할 필요는 잘 모르겠음. 무한루프도 아니고. |
| §9 같은 이름·다른 종류 | | 이건 근데 jsonSchema 가 에러를 내지 않나? |
| §9 경고 목록 일곱 | | 더 많을 수도 있다. 기존 error 종류와 용법을 참고해서 분류하라. 물론 기존 분류체계가 애매하면 다시 구성해도 무방 |
| §9 게이트 가진 분기의 정의 | | 1항? 무슨 뜻이지? |
