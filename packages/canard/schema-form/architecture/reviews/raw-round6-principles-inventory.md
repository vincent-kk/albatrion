# 6라운드 시선 A — 동작·인터페이스 원리 정합성 인벤토리

대상: `packages/canard/schema-form/architecture/` 4차 본문 (커밋 01e52d0bd).
판정 범례 — **도출**: 명시된 원리에서 실제로 따라 나온다 / **편의**: 원리와 무관한 정책·UX 판단 / **어긋남**: 명시된 원리와 충돌 / **미근거**: 원리도 근거도 적히지 않음.
교차: 같은 사건을 다른 문서가 다르게 말하는 곳.

---

## A. P1 / P1′ — 판정은 검증기의 것, 폼은 형상 문법만 읽는다

| # | 동작·인터페이스 | 위치 | 근거 원리 | 판정 | 교차 |
|---|---|---|---|---|---|
| A1 | 폼의 판정 = `validator(작성된 스키마, 방출 값)` | adr/0001:18 | P1 | 도출 | — |
| A2 | 폼 전용 키를 **키워드 위치에서만** 제거 | adr/0001:21, adr/0003:18 | P1 | 도출 (근거는 판정이 아니라 컴파일 보호) | — |
| A3 | 추가 검사는 AND로만 | adr/0001:23 | P1 | 도출 | — |
| A4 | "같은 설정의 검증기"로 계약 한정 | adr/0001:29-31 | P1 | 도출 | — |
| A5 | 방출 값은 JSON 직렬화 동치여야 한다 | adr/0001:33 | P1 | 도출 | — |
| A6 | `ValidationMode.None`은 통과가 아니다 | adr/0001:34 | P1 | 도출 | — |
| A7 | 판정은 **값의 revision**에 묶인다 | adr/0001:32 | P1 | **어긋남(구식)** | **0007:34·0008:84는 커밋 번호(F28)로 대체. 0001은 개정되지 않아 revision을 그대로 말한다** |
| A8 | 가드는 검증기 플러그인의 `compileGuard`가 컴파일 | adr/0004:20-24, adr/0002:44 | P1 | 도출 | — |
| A9 | 가드는 **투영 후** 값을 본다 | adr/0002:60, adr/0007:50 | P1 | 도출 | — |
| A10 | 호스트 raw가 비객체이면 `G = {}` | adr/0002:61, adr/0007:50 | P1 | 편의(S8 대응, 원리에서 도출되지 않음 — "검증기가 볼 값"이라면 `null`을 그대로 넘겨야 한다) | — |
| A11 | 판별식 가드에 `required`를 더한다 | adr/0002:114 | P1(형상 최소 읽기) | 편의(R11-a 실측 대응). 검증기가 보는 `if`와 다른 술어를 폼이 만든다 | 0002:119 "`if` 가드에는 `required`를 더하지 않는다" — 같은 문제에 반대 처방 |
| A12 | `const`/`enum` **값**을 읽어 서로소를 검사 | adr/0005:79-80,90 | P1′ 확장 | 도출(명시적 확장, 소유자 인용) | P1′ 목록(0002:26)에 `const`/`enum` 없음 |
| A13 | `discriminator.propertyName`을 **힌트로** 읽는다 | adr/0005:79 | P1′ | 편의(비표준 어휘, 원리 밖) | — |
| A14 | 호스트의 `required`/`dependentRequired`를 읽어 **초기 분기 선택에 가중** | adr/0002:127 (E17) | — | **어긋남** — P1′(0002:26)는 `required`를 명시적으로 "검증기의 것"으로 분류 | — |
| A15 | 금지 조각을 읽지 않는다 (`false`·`not`·`additionalProperties`) | adr/0002:80, adr/0007:53 | P1′ | 도출 | — |
| A16 | 잔여 키의 해석은 플러그인의 `rejectedKey?: string` 계약 | adr/0002:90, adr/0006:81 | P1 | 도출 | — |
| A17 | 두 이름 이상의 `not:{required}`는 호스트 에러로 남는다 | adr/0002:97 (F8) | P1 | 도출 | — |
| A18 | `mapping` 전용 OpenAPI 3.0은 선택 가드로 보내고 경고 | adr/0005:85 | P1 | 도출 | — |
| A19 | 검증기 미등록이면 **조건부 장치가 동작하지 않는다** | adr/0004:26 | P1 | 도출 — 다만 폼의 **형상**이 플러그인 등록 여부에 좌우된다 | — |
| A20 | `node.jsonSchema`가 메모된 유효 스키마가 된다 | adr/0005:104, adr/0013:26 | P1 | 도출 | 변경 통지 이벤트 없음 — 0007:138 미결 |
| A21 | 에러 라우팅: `schemaPath`가 활성 조각 아래인 에러만 그 노드에 | adr/0001:41 | P1 | 도출 | **0005:92는 다른 규칙(instancePath + union 키워드), 0002:90은 세 번째 규칙(rejectedKey). Q12는 "규칙은 0004에 적는다"고 하나 0004에 해당 절이 없다** |
| A22 | 주인 없는 에러의 폼 수준 sink | adr/0001:42 | P1 | **미근거** — 이후 어느 문서도 모양·이름·소유자를 정하지 않음 | — |
| A23 | `if`의 공허한 참을 그대로 따른다 | adr/0001:49, adr/0002:119 | P1 | 도출 | 경고 여부 Q10 미결(경고하면 P1′와 닿음) |
| A24 | 폼은 방언 스위치 없이 두 철자를 모두 읽는다 | adr/0005:46, 00-goals:106 | P1′ | 도출 | — |
| A25 | `$ref` 해석 깊이 기본 1 | adr/0005:112 | — | **미근거** — 재귀 스키마에서 조각 열거의 종료가 미정 | — |
| A26 | 주석성 키워드 병합: `default`·`title` 등은 먼저 정의된 쪽이 이긴다 | adr/0005:109 | — | **미근거** — 본문 스스로 "표준에서 나온 결과가 아니라 폼의 쓰기 규칙" | — |

---

## B. P2 — 원본은 호출자와 작성자만 쓴다

| # | 동작·인터페이스 | 위치 | 근거 원리 | 판정 | 교차 |
|---|---|---|---|---|---|
| B1 | 원본에 쓰는 주체는 넷 | 03:10 | P2 | **어긋남(자기모순)** — 같은 행이 "core가 스스로 원본을 고치는 일은 없다"고 하는데 03:45 표의 `default` 주입 '누가' 칸은 `core` | 03:10 ↔ 03:45 |
| B2 | 사용자 입력 = 부분 쓰기, 그 리프만 | 03:40, adr/0013:35 | P2 | 도출 | — |
| B3 | `setValue(V, {mode:'Merge'})` = 부분 쓰기, 로드 아님 | 03:41 | P2 | 도출 | — |
| B4 | `setValue(V)` 기본 `Overwrite` = 전체 교체 | 03:42, adr/0007:64 | P2 | 도출 | — |
| B5 | `null`·`17`도 V다 — 모든 자식이 없음 | 03:42, adr/0013:42 | P2 | 도출 | — |
| B6 | `setValue(getValue())`도 전체 교체 (D-7) | 03:42, adr/0013:59 | P2·P3 | 도출(수락) | — |
| B7 | `defaultValue`·`reset()`도 전체 교체 | 03:42 | P2 | 도출 | `defaultValue` prop이 마운트 후 바뀌면? 미정의 |
| B8 | `injectTo` = 작성자가 선언한 전체 교체 | 03:43, adr/0013:41 | P2 | 도출 | — |
| B9 | `injectTo`는 **원천의 방출이 직전 커밋과 다를 때만** 발화 | adr/0007:79 (F11), adr/0013:41 | — | **편의** — 이력 의존. "사용자의 부분 쓰기를 되돌리지 않는다"는 UX 판단이며 어느 원리에서도 도출되지 않음 | P3의 "이력을 읽지 않는다"(03:11)와 긴장 |
| B10 | 배열 `push`/`remove`/`update` = 구조 연산 | 03:44, adr/0013:37 | P2 | 도출 | **T-7(04:15)은 `push`/`remove`/`pop`/`clear`, 0008:91 진입 목록은 `push`/`remove`. 세 문서가 배열 API 목록이 다르다** |
| B11 | `default` 주입 = core의 유일한 자동 쓰기 | 03:45, adr/0007:81, adr/0013:57 | P2 | 도출 | B1과 충돌 |
| B12 | 주입은 **없음**인 키에만 | adr/0013:38,57 | P2 | 도출 | — |
| B13 | 주입 시점 (i) 전체 교체 직후 (ii) 조각 꺼짐→켜짐 직후 | adr/0007:66, adr/0013:38 | P2·P3 | 도출 | **"비객체 호스트가 다시 객체가 될 때"라는 D-1이 기대는 세 번째 시점이 목록에 없다 — 아래 B15** |
| B14 | 자동 쓰기는 비객체 호스트의 원본을 건드리지 않는다 (F10, T-12) | adr/0007:78, adr/0013:41 | P2 | 도출 | — |
| B15 | `setValue(null)` 뒤 "다시 객체가 될 때 로드 계약이 빈 상태를 만든다" | adr/0006:58, adr/0013:42 | P2 | **어긋남/공백** — B13의 두 시점 어디에도 해당하지 않는다. `null` 시점에는 F10이 주입을 막고, 사용자 부분 쓰기로 호스트가 객체가 되는 경로에는 주입 트리거가 없다. #338이 목표로 한 바로 그 경로 | — |
| B16 | 쓰기의 종류는 호출자가 선언, core는 추론하지 않는다 (D-4) | 03:47, adr/0007:59 | P2 | 도출 | — |
| B17 | `Refresh`는 옵션이 아니라 core가 **출처**로 판단 (F7) | 03:47, adr/0007:77 | P2 | **편의** — "출처"는 이력이다(그 노드의 입력이 방금 보고한 값). P2·P3 어느 쪽에서도 도출되지 않음 | — |
| B18 | 공개 옵션은 객체 `{mode?, disableDefaultInjection?}` | adr/0013:46, adr/0007:71 | P2 | 도출 | — |
| B19 | 같은 옵션이 `reset(options)`·마운트에도 | adr/0013:51, adr/0007:74 | P2 | 도출 | **`reset`은 항상 전체 교체이므로 `mode`가 무의미한 no-op** |
| B20 | 호출 옵션 > Form 속성, 양방향 | adr/0013:52 | P2 | 도출 | Form 속성의 **이름이 없다** |
| B21 | 배치 안에 억제가 섞이면 억제가 이긴다 | adr/0013:53, 03:45 | — | **편의**(보수적 선택). 한 호출의 명시적 `false`가 형제 호출에 의해 뒤집힌다 | — |
| B22 | 억제의 범위는 "호출자의 전체 교체가 일으킨 정착"뿐 | adr/0013:54 (F25) | P2 | 도출 | **이름 `disableDefaultInjection`이 범위보다 넓다 — 사용자 입력이 조각을 켜서 일어나는 주입은 끄지 못한다** |
| B23 | `Merge` + 억제 = 무해한 no-op, 타입으로 막지 않고 문서로 | adr/0013:55 | — | 편의(tsc 실측 근거) | B19와 합쳐 한 옵션 객체에 no-op 조합이 둘 |
| B24 | 전체 교체는 **전이 기준을 비운다** (F4) | adr/0007:79 | P2·P3 | 도출 | — |
| B25 | 호스트 raw를 비우는 것은 사용자·호출자의 부분 쓰기뿐 (F1) | adr/0007:78 | P2 | 도출 | — |
| B26 | core는 호출자가 넘긴 객체를 바꾸지 않는다 (F24, T-19) | adr/0007:78, adr/0011:56 | P2 | 도출 | — |
| B27 | 타입별 파서 강제 변환·`options.trim`을 입력 컴포넌트로 | adr/0013:77 | P2 | 도출 | — |
| B28 | `minItems` 채우기·`maxItems` 차단을 입력 컴포넌트로 | adr/0013:78 | P2 | 도출 | — |
| B29 | 비객체 값을 `{}`로 바꾸던 것 폐기, 보존·방출 + type 에러 (F27) | adr/0013:79 | P2 | 도출 | — |
| B30 | `Normalize`의 미선언 키 제거 폐기, `extras`에 보존 (E16) | adr/0013:80 | P2·P1′ | 도출 | — |
| B31 | 분기 전환·`active` 전이의 reset 폐기 | adr/0013:81 | P2·P4 | 도출 | — |
| B32 | `computed.derived`·`injectTo`는 **남는다 — 작성자가 명시한 쓰기** | adr/0013:82 | P2 | **어긋남** — adr/0007:32와 adr/0013:41은 `&derived`를 **읽기 전용**이라 한다 | **0013:41 ↔ 0013:82 ↔ 0007:32 ↔ 0003:21 ↔ 03:91. 네 문서가 `&derived`가 원본을 쓰는지 정반대로 말한다. 현재 구현은 쓴다(`AbstractNode.ts:545-548`)** |
| B33 | P2의 네 주체 목록에 `&derived`가 없다 | 03:10 | P2 | **공백** — B32가 쓰기라면 다섯째 주체 | — |
| B34 | `selection` 칸에 누가 언제 쓰는가 | — | — | **공백** — P2는 `raw`만 규율한다. `selection`은 상태(03:34)인데 쓰기 표(03:38-45)에 행이 없다. 흩어진 규칙: `Overwrite`가 다시 정함(0002:128), `reset`이 초기 선택으로(0002:128), 사용자가 선택기로, `select`가 공개 쓰기 API(0008:91) | — |
| B35 | `extras` 칸에 누가 언제 쓰는가 | — | — | **공백** — 상태 셋 중 하나인데 쓰기 표에 행이 없다 | — |
| B36 | 잠복 값 열거 `latent(path)` (가칭, F26) | adr/0013:67, adr/0006:80 | P4 | 도출 | 이름·모양 미결. 파기 시점 Q1 |
| B37 | 실수로 누른 `null`의 되돌리기는 입력 컴포넌트의 몫 | adr/0013:93 | P2 | 도출 | — |
| B38 | 초기값 보존 (a) 기각, (b)의 "손대지 않은 값엔 omitEmpty 건너뛰기"도 기각(이력 의존) | adr/0013:71 | P3·P4 | 도출 | B9(injectTo F11)·B17(Refresh 출처)은 같은 이유로 기각되어야 하나 남아 있다 |

---

## C. P3 — 형상은 상태의 순수 함수다

| # | 동작·인터페이스 | 위치 | 근거 원리 | 판정 | 교차 |
|---|---|---|---|---|---|
| C1 | 상태는 `raw`·`selection`·`extras` 셋뿐 | 03:34, adr/0006:38 | P3 | 도출 | — |
| C2 | 출발점 `A := 무조건 조각 ∪ 판별 프로퍼티 ∪ 상속 overlay` | adr/0002:68, adr/0007:47 | P3 | 도출 | **03:56은 "무조건 조각만 켠 채 출발" — 세 항 중 둘이 빠졌다** |
| C3 | 직전 커밋의 `active`를 읽지 않는다 | adr/0002:68 | P3 | 도출 | 단, 전이(0007:33)와 평가 순서 힌트(F13, 0002:72)는 읽는다 |
| C4 | 한 바퀴에 모든 조각을 평가, 가우스-자이델 | adr/0002:69, adr/0007:49 | P3 | 도출 | — |
| C5 | 비단조 재평가 — 참이면 켜고 거짓이면 끈다 | adr/0002:69 | P3 | 도출 | — |
| C6 | 상한 = 조건부 조각 수 + 1 | adr/0002:70, adr/0007:51 | P3 | 도출 | **03:57은 "조각 수 + 1"(조건부 누락)** |
| C7 | 조각은 트리, 중첩은 감싸는 조각이 활성일 때만 | adr/0002:50, adr/0005:36 | P3 | 도출 | — |
| C8 | 전순서 = (감싸는 조각, 키워드 순위, 배열 인덱스) | adr/0002:51, adr/0005:66 | P3 | 도출 | — |
| C9 | 고정점이 없으면 지원 범위 밖, 결과는 결정적이되 임의적 | adr/0002:71, adr/0007:57 | P3 | 도출 | — |
| C10 | `budget-exceeded`는 프로덕션에서도 관측 가능, 조상으로 전파 | adr/0002:70, adr/0007:52 | P3 | 도출 | — |
| C11 | 개발 모드는 커밋 뒤 **동기로** throw | adr/0007:52, 04:12 | P3 | 도출 | 파생 라운드 상한 초과(0007:41)에는 throw 언급이 없다 |
| C12 | 상속 overlay는 부모 바퀴 안에서 자식 재계산, 메모 (E12, F5) | adr/0002:110, adr/0007:54 | P3 | 도출 | — |
| C13 | 전이(`default` 주입)는 **계산 밖** | 03:60,86, adr/0007:33 | P3·P2 | 도출 | 직전 커밋의 `active`를 읽는다 — P3 한 줄("이력을 읽지 않는다")의 예외이나 원리로 승격되지 않음 |
| C14 | 파생 라운드 상한 25, 넘으면 실행하지 않고 마지막 완료 라운드 커밋 (E7) | adr/0007:32,41 | P3 | 도출 | — |
| C15 | 전이 라운드 = 조각 수, 파생과 **별도** (F2) | adr/0007:33 | P3 | 도출 | 초과 시 처리 **미정의** |
| C16 | 커밋된 트리는 (스키마, raw·selection·extras)의 순수 함수 (F9) | adr/0007:40 | P3 | **어긋남** — B9(injectTo F11)가 직전 커밋을 읽으므로 같은 상태가 다른 결과를 낼 수 있다 | — |
| C17 | 변경 키 역색인은 쓰지 않는다 (E13) | adr/0006:68, adr/0005:54 | P3 | 도출 | — |
| C18 | 직전 `active`를 **평가 순서 힌트로만** (F13) | adr/0002:72 | P3 | 도출 | 출발 가설로 쓰는 최적화는 Q15 |
| C19 | 빈 값·분기 키 없는 값은 분기 없음 (D-8, C-1·C-7) | adr/0002:125, 03:78 | P2("폼은 값을 만들지 않는다") | 도출 | **프로토타입 `loop-v4b`는 `{zzz:1}`에 첫 분기를 고른다 — 설계와 실행이 반대** |
| C20 | 초기 선택 = 값의 키를 가장 많이 선언한 통과 분기, 동점이면 앞 | adr/0002:126 | P3 | 편의(R9 대응). "통과" 판정이 검증기 설정에 의존한다는 점은 적히지 않음 | — |
| C21 | 어느 분기도 안 켜진 선택 가드 호스트에 부분 쓰기가 오면? | adr/0002:163 | — | **공백(미정의)** | — |
| C22 | `emit`의 키 순서: 스키마 선언 순서, extra는 뒤에 **삽입 순서** | adr/0007:55 | — | **어긋남(약함)** — 삽입 순서는 이력. 같은 상태가 다른 직렬화를 낸다 | Q14 미결 |
| C23 | 같은 이름 + 다른 종류가 동시 활성이면 앞선 종류를 살리고 경고 | adr/0005:64 | P3 | 도출(전순서로 결정적) | — |
| C24 | 제약 교차가 공집합이어도 throw하지 않는다 | adr/0005:69 | P3 | 편의(R11-e, UX 판단) | — |
| C25 | 읽기는 계산하지 않는다 | adr/0006:52, 04:26 | P3 | 도출 | — |
| C26 | `emit` 참조는 네 입력 중 하나라도 바뀌면 새로 (F9) | adr/0006:50 | P3 | 도출 | — |

---

## D. P4 — 방출은 정책이다

| # | 동작·인터페이스 | 위치 | 근거 원리 | 판정 | 교차 |
|---|---|---|---|---|---|
| D1 | 비활성 조각의 값은 방출에서 빠지고 원본은 남는다 | adr/0006:44, adr/0013:25 | P4 | 도출 | — |
| D2 | `omitEmpty`·`omitTrailing`은 원본을 건드리지 않는 투영 | adr/0006:44, 03:89 | P4 | 도출 | — |
| D3 | 불변식은 `emit`에만 있다 (E9) | adr/0006:46 | P4 | 도출 | — |
| D4 | `node.value` = `local`, `getValue()` = `emit` | adr/0006:48 | P4 | 도출 | **open-questions:25는 "`value`(원본) / `normalizedValue`(방출)" — 호스트에서 `local ≠ raw`이므로 정면 충돌** |
| D5 | `&visible:false`로 숨긴 필드와 미선언 키는 방출된다 | adr/0013:64 | P4 | 도출 | — |
| D6 | 비활성 조각이 선언한 자식의 원본은 설계상 잠복 | adr/0013:67, adr/0006:80 | P4 | 도출 | — |
| D7 | 방출 정책을 작성자가 고른다 | 03:12 | P4 | 도출 | 정책 키가 `options.*`(비`&`)에 있다 — ADR 0003의 `&` 규칙과 어긋남 |
| D8 | 참조 그룹은 방출·가드·검증에 나타나지 않는다 | adr/0011:31,67 | P4·P5 | 도출 | `node.enhancedValue`(가상 필드 포함 읽기)의 새 정의가 **어느 문서에도 없다** |

---

## E. P5 — core는 렌더러를 모른다

| # | 동작·인터페이스 | 위치 | 근거 원리 | 판정 | 교차 |
|---|---|---|---|---|---|
| E1 | 이벤트 시스템은 통지 전용, 역할 셋 | adr/0008:24-28 | P5 | 도출 | — |
| E2 | 통지는 커밋 뒤 1회, 동기 | adr/0008:36, 03:90 | P5 | **편의** — 근거가 React의 `restoreStateOfTarget` 기제(0008:47). 렌더러 내부에서 core 규칙을 도출한다 | — |
| E3 | 루트 단일 디스패처, 문서 순서 위 → 아래 | adr/0008:59 | P5 | 도출 | — |
| E4 | 배달 집합 = 값 변화 ∪ 시그널 대기 ∪ **활성 여부가 바뀐 노드** | adr/0008:60 (F20) | P5 | 도출 | "노드의 활성"은 조각 집합에서 유도되나 그 사상이 어디에도 없다 |
| E5 | `revision`은 커밋 시 배달 집합 전체를 한 번에 (F16) | adr/0008:61 | P5 | 도출 | — |
| E6 | 파동 상한 틱당 25, 상한에서 리스너 쓰기 거부 | adr/0008:62 | P5 | 도출 | 초과 시 `settle` 기록 — **상태 이름이 없다** |
| E7 | 같은 파동의 보장은 **payload에 한정** | adr/0008:62 | P5 | 도출 | — |
| E8 | 리스너 목록은 파동 시작 시점에 고정 (F17) | adr/0008:66 | P5 | 도출 | — |
| E9 | 리스너 격리, `onListenerError`(가칭) | adr/0008:64 (F21) | P5 | 도출 | 채널·모양 미결 |
| E10 | `batch(fn)` — 표시만 하고 끝에 정착 1·파동 1 | adr/0008:72 | P5·G7 | 도출 | — |
| E11 | 중첩 `batch`는 가장 바깥이 이긴다 | adr/0008:72 | P5 | 도출 | — |
| E12 | **리스너 안의 `batch`는 자기 배치다(진입 깊이는 통지 중 0)** | adr/0008:74 (F18) | — | **어긋남(실행 반증)** — 인용된 프로토타입은 "a listener write during a wave runs at depth >= 2 … fires NO onChange"라고 적는다(`spikes/work-loop/REPORT-v4c.txt` §1). 깊이 0인 것은 `onChange` 안의 쓰기뿐 | **0008:74 ↔ 0008:62 ↔ 0008:91 ↔ 0008:101(C-9 onChange=1)** |
| E13 | 예산은 넷 (F19) | adr/0008:77 | P5 | 도출 | 실제로는 다섯(전이 포함), 관측 방식이 제각각 |
| E14 | payload = 커밋 시점의 `{previous, current}`, 호스트는 local·emit 둘 다 | adr/0008:81 | P5 | 도출 | — |
| E15 | `previous`는 **그 노드에 마지막으로 통지한 값** | adr/0008:83 | — | **편의** — 이력. 되돌림 가능성 절이 관측 가능한 변화로 인정(0008:156) | — |
| E16 | 커밋 번호는 `revision`과 별개, 검증 스탬프는 커밋 번호 (F28) | adr/0008:84, adr/0007:34 | P1 | 도출 | **A7(0001:32)은 여전히 revision** |
| E17 | payload는 불변, 개발 모드 `Object.freeze` | adr/0008:85 | P5 | 도출 | — |
| E18 | 루트 `onChange`는 최외곽 동기 진입당 1회, 디바운스 없음 (D-10) | adr/0008:88, 03:91 | P5 | 도출(수락) | — |
| E19 | 진입의 정의 — 공개 쓰기 API = `setValue`·`write`·`select`·`removeKey`·`push`·`remove`·`batch` | adr/0008:91 | P5 | 도출 | **`write`·`removeKey`는 다른 어느 문서에도 없다. `select`는 아래 E24와 충돌** |
| E20 | 깊이 1→0에서 검증 먼저, 그다음 `onChange` | adr/0008:91 | P1·P5 | 도출 | — |
| E21 | `onChange` 안의 쓰기는 새 진입, 중첩 상한 25, `onchange-cap-exceeded` | adr/0008:93 | P5 | 도출 | **`settle`의 타입(0006:34)은 `'stable' \| 'budget-exceeded'` 둘뿐 — 03:28도 둘뿐** |
| E22 | React 이펙트의 쓰기는 새 진입 — 문서화 대상 (C-10) | adr/0008:109, 03:91 | P5 | 도출 | 처방("파생 값은 `derived`로")이 B32(derived가 쓰는가)에 의존 |
| E23 | 명령 시그널이 core에 남는다 | adr/0008:28, 00-goals:115 | P5 | 도출(소유자 인용) | — |
| E24 | 명령 publish를 공개 API로 — `FormHandle.refresh(path)`·`remount(path)`를 `focus`·`select`와 대칭으로 (C-11) | adr/0008:121 | P5 | 도출 | **`select`가 두 뜻을 갖는다: `FormHandle.select(path)`는 입력 텍스트 선택(`Form/type.ts:114` 실측), E19의 `select`는 union 분기 선택 쓰기 API** |
| E25 | `Refresh`가 버리는 것 = 입력의 캐럿·선택·IME | adr/0008:125 | P5 | 도출 | **정의가 "입력의 `key`를 바꿔"(React 기제)로 적혀 있다 — 렌더러 중립 어휘가 아니다** |
| E26 | `Remount`가 버리는 것 = 서브트리 React 로컬 상태 | adr/0008:126 | P5 | 도출 | 같은 문제 — "래퍼의 `key=version`" |
| E27 | core가 스스로 `Refresh`를 보내는 규칙은 출처 판단 (F7, T-2) | adr/0008:128 | P5 | 편의(B17과 같음) | — |
| E28 | 터미널 전략: core는 `isReactComponent`를 부르지 않고 "있고 null이 아니다"만 본다 | adr/0011:53 | P5 | **어긋남(부분)** — 감지는 없어지나 **트리의 형태가 렌더링 선택에 좌우된다**는 성질은 남는다. 00-goals:59는 이를 고쳐야 할 결함으로 명시 | **00-goals:59 ↔ adr/0011:49-53** |
| E29 | `terminal: true`/`false` 양방향 재정의 | adr/0011:54 | P5 | 도출 | 키 이름·`&` 접두 미결 |
| E30 | 터미널 노드 아래 경로의 별칭 (S11) | adr/0011:88 | — | **공백** — 처방 없음. `find`의 계약 미정 | — |
| E31 | 참조 그룹은 표현 계층의 것 (D-6) | adr/0011:60, 03:93 | P5·P4 | 도출 | (a)/(b) 비용 비교 미결 |
| E32 | `virtual`의 `required` 재작성 전처리가 사라진다 | adr/0011:71 | P1′ | 도출 | — |
| E33 | 가상화 명령 재발행은 렌더 계층에 남는다 (T-3) | 04:11, adr/0008:138 | P5 | 도출 | — |
| E34 | 배열 연산은 동기, Promise 없음 (T-7) | adr/0008:36, 04:15 | P5·G5 | 도출 | — |
| E35 | `globalState`는 OR 누적이라 내릴 수 없다 — 함께 고칠지 미결 | adr/0008:151 | — | **공백** | — |
| E36 | `UpdatePath`(배열 재인덱싱 경로 통지) 미결 | adr/0008:150 | — | **공백** | — |

---

## F. 인터페이스 표면 — 이름과 자리

| # | 항목 | 위치 | 판정 |
|---|---|---|---|
| F1 | 상태 칸 이름 `raw`/`local`/`emit` ↔ 공개 읽기 `value`/`getValue()`/`normalizedValue`/`enhancedValue` | adr/0006:48, open-questions:25, 패키지 CLAUDE.md | **비대칭** — 두 어휘의 사상이 한 줄에만 있고 그 한 줄이 Q3과 충돌. `enhancedValue`는 4차 본문 전체에 등장하지 않는다 |
| F2 | `settle` 읽기 API | adr/0006:34, adr/0008:81 | **없음** — 이벤트(`UpdateSettle` 가칭)로만 관측 |
| F3 | `latent(path)` | adr/0013:67 | 가칭 |
| F4 | `onListenerError` | adr/0008:64 | 가칭, 채널 미정 |
| F5 | Form 속성(마운트 억제 스위치) | adr/0013:51 | **이름 없음** |
| F6 | `disableDefaultInjection` | adr/0013:46 | 후보. 범위(로드 전용)보다 이름이 넓다 |
| F7 | `mode: 'Overwrite' \| 'Merge'` | adr/0013:46 | 배열 `Merge`의 뜻 미정, `reset`에서 no-op |
| F8 | `batch(fn)` | adr/0008:72 | 진입 정의와 맞물림 — 단 F18의 깊이 서술이 반증됨(E12) |
| F9 | `focus`/`select`/`refresh`/`remount` | adr/0008:121, `Form/type.ts:113-114` | `focus`/`select`는 오늘 `Fn<[path]>`로 존재. **`select` 이름 충돌(E24)** |
| F10 | 공개 쓰기 API 목록 | adr/0008:91 | `write`·`removeKey`가 이 한 곳에만 등장 |
| F11 | `&` / `options.*` / `computed.*` 세 어휘 공존 | adr/0003:13,30, adr/0013:77,82, adr/0011:54 | **규칙이 하나가 아니다** — 0003이 "모두 `&`"라 선언했으나 4차 본문이 `options.trim`·`computed.derived`·`injectTo`·`terminal`을 비접두로 계속 쓴다. 0003:30이 미결로 남김 |
| F12 | `settle.status` 값 집합 | adr/0006:34 vs adr/0008:93 vs 03:28 | **세 문서가 다르다**: `'stable'\|'budget-exceeded'` / `'onchange-cap-exceeded'` 추가 / "정상 / budget-exceeded" |
| F13 | 배열 API 어휘 | adr/0013:37 vs 04:15 vs adr/0008:91 | `push/remove/update` vs `push/remove/pop/clear` vs `push/remove` |

---

## G. 집계

- 총 항목 **113**.
- **도출 72** / **편의 14** (A10, A11, A13, B9, B17, B21, B23, C20, C24, E2, E15, E27, 그리고 A2·A26의 보호 근거 2건) / **어긋남 9** (A7, A14, B1, B15, B32, C16, C22, E12, E28) / **미근거·공백 18** (A22, A25, A26, B33, B34, B35, C15, C21, E6 상태명, E30, E35, E36, F2, F5, 그 밖 4) — 나머지는 교차 표기만.
- 교차(문서 간 불일치) **13건**: A7·A21, B1, B10, B32, C2, C6, C19, D4, E12, E19/E24, E21/F12, E28, F11, F13.
