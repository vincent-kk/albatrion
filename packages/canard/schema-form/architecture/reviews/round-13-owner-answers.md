# 13라운드 — 소유자의 답 (원문)

2026-09-24. 소유자가 `round-13-owner-review.md`를 deilen 뷰어에서 읽고 남긴 답의 원문이다("Continue in chat"). 3번은 같은 날 대화에서 먼저 답했다. 편집자는 고치지 않았다.

| 항목 | 소유자의 답(원문) |
| --- | --- |
| 1 잠금 규칙 | 이걸로 가자.(다) 글로벌 readOnly 나 disabled 같은 개념은 없애고 모든 control 필드는 자체 노드만 지원. children 그룹은 예외. 어차피 jsonSchema 만으로 readOnly 를 만드는 유스케이스는 거의 없을걸로 보임. 이건 "검증"의 영역이 아니라 react 의 표현 영역이라고 보인다. |
| 2 나감 비움 기본값 | 나는 지금 방출과 값을 구분하지 않고 말했다. onChange 로 넘어가는 값(방출 표현값)에서 지워지는게 기본값이면 된다. 그럼 궁금한건데, 방출에서 지워지는거랑 unsetOnExit(이름이 좀 이상하긴 하다. 내가 와닿지 않는 이름이라 변경이 필요)으로 진짜 지우는거랑 차이는 재전환시 값의 잔류 여부인건가? |
| 3 폼 전용 키 접두 | 이후 대화로 "나" 방향 확정. 다만, 제안했던 form 전용필드용 프리픽스는 매력적이나, 너무 난해해진다. 제어용 필드들에 대해서만 &를 붙이는 방향으로 가자. (대화: "FormTypeInputProps, FormTypeRendererProps, formType, FormTypeInput, terminal, errorMessages, 전부 &를 붙인다는건가? 그건 좀 과한거같은데") |
| 4 규칙 충돌 순위 | 이게 맞다.(가) 전파 순서에 대한 이야기는 서로 다른 노드에서 하나의 노드로 injectTo 를 할 경우를 생각한건데, 이 경우에는 뒤가 이기는거고, 명령어끼리는 지금 위계가 옳다. 물론 겹치지 않게 짜는게 권장이지만 |

## 되물음에 대한 답 (같은 날, 대화)

| 항목 | 소유자의 답(원문) |
| --- | --- |
| Form 속성의 자리 | 맞다. props 로 전달되는 글로벌 readOnly 와 disunset 만 특별관리를 하자. 코어트리와 유리된 전역동작으로 읽으면 된다. |
| 나감 정책 키 이름 | 권고 수용. unsetOnInactive 으로 한다. |
