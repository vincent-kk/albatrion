{"to":"team-lead","summary":"Antigravity crosscheck answer delivered","message":"Provider: antigravity (via cennad, session_id 2a2084bf-2fff-4476-a600-ade7c293eddf). No error; it read the scratchpad files directly on the first turn, so no fallback/continuation was needed. All 7 checks are present in the answer. Final answer verbatim below.\n\n# 병합안 교차 확인 — antigravity\n\n## 총평 (5줄 이내)\n본 병합안은 선언 순서 무관성(규칙 A의 12건 경고등 증명), 참조 안정성(1.20), 그리고 1:1 이름 정합성(Hint/props의 `type` 정렬)을 엄격하게 정립한 높은 완성도의 설계입니다. 단일 노드 모델(NODE-002)과 P1′(검증 분리 원칙)을 충실히 따르며, 비-union 경로의 오버헤드를 0으로 유지했습니다. 다만 S3에서 null 분기만 존재하는 스키마의 미결정 간극(2.11–2.14)과, 본체 union 노드에 `then` 좁힘이 적용될 때 입력·규칙 A와 검증기 간에 발생하는 런타임 검증 불능 교착 상태는 실무 배포 전 반드시 보완되어야 합니다.\n\n---\n\n## 1 결정성\n\n§2의 절차는 대부분의 경우 하나의 결과로 귀결되나, **S3(C에 `type`이 없는 칸)에서 모든 분기가 `'null'`만 받는 분기인 경우를 미결정(undecided)**으로 남깁니다.\n\n### 미결정 스키마 사례\n```json\n{\n  \"anyOf\": [\n    { \"type\": \"null\" }\n  ]\n}\n```\n(또는 `{ \"oneOf\": [{ \"type\": \"null\" }] }`, `{ \"anyOf\": [{ \"type\": \"null\" }, { \"type\": \"null\" }] }`)\n\n### 미결정 추적 근거\n1. **2.4 (S0 문법)**: 빈 배열이나 미지의 형이 아니므로 통과합니다.\n2. **2.5 (S1 C)**: 칸 본체에 `type`이 없습니다.\n3. **2.9 (S3)**: C에 `type`이 없으므로 게이트 없는 `anyOf` 분기를 확인합니다. 분기가 1개 존재합니다.\n4. **2.10**: 분기 $b_1$의 $\\tau(b_1) = [\\text{'null'}] \\neq \\top$이므로 통과합니다.\n5. **2.11**: \"'null'만 받는 분기(`isNullBranch`)는 nullable을 켭니다\"에 따라 `nullable = true`가 됩니다. 이어 \"목록 U는 **나머지 분기 $\\tau(b)$의 합집합**\"으로 정의되는데, null 분기 외에 남은 분기가 없으므로 **$U = \\emptyset$ (공집합)**이 됩니다.\n6. **2.13**: $\\text{fold}(U)$에 `object`·`array`가 없습니다.\n7. **2.14**: \"그 밖의 경우 칸은 **목록 U의 원시 잎이나 `union` 잎이 되고**\"라고 명시합니다.\n   - 그러나 $U = \\emptyset$이므로 원시 잎이 될 수 없습니다(|fold| = 1 필요).\n   - `union` 잎도 될 수 없습니다(1.5 및 1.10에서 `UnionSchemaType`은 길이 2 이상으로 정의됨).\n   - 2.7(S2)과 달리 2.14(S3)에는 \"단, 모든 분기가 `'null'`만 적었으면 null 노드이다\"라는 탈출 규칙이 없으며, `UNKNOWN_JSON_SCHEMA` 오류로 지정되어 있지도 않습니다.\n   - 따라서 이 스키마는 노드 종류, `schemaType`, 전략이 **정의되지 않은 채(undecided)** 남게 됩니다.\n\n*(참고: 두 번 결정되는 이중 결정(decided twice) 사례는 발견되지 않았습니다. S2와 S3는 C의 `type` 유무로 완전 배타 분기됩니다.)*\n\n---\n\n## 2 예측 가능성(표)\n\n| # | 대상 스키마 | 노드 종류 (`type`) | `schemaType` | `nullable` | 전략 (`strategy`) | 플러그인 없을 때 선택되는 기본 입력 | 문자열 `\"42\"` 입력 시 저장되는 값 | 저자 기대 불일치 여부 및 변경 방향 |\n|---|---|---|---|---|---|---|---|---|\n| 1 | `['string','number']` | `'union'` (2.2, E1) | `['string','number']` (1.5) | `false` (2.1) | `'terminal'` (2.21) | 코어 기본 union (`FormTypeInputString` 감싸개, 4.11) | `\"42\"` (string) | **불일치 주의**: 사용자는 숫자 입력을 기대할 수 있으나, 규칙 A ①(3.4, 3.27)에 따라 문자열 멤버십이 즉시 만족되어 숫자로 변환되지 않음. **기대 변경**(4.229 명세대로 코어 폴백의 최소 한계로 문서화하고 형 선택은 UI 플러그인에 위임). |\n| 2 | `['integer','null']` | `'number'` (2.2, E6) | `'integer'` (1.4, E6) | `true` (2.2, E6) | `'terminal'` (2.21) | `FormTypeInputNumber` (4.5) | `42` (integer, WRITE-075) | **일치**: 저자 의도와 완벽히 부합. |\n| 3 | `{anyOf:[{type:'string'},{type:'number'}]}` | `'union'` (2.14, E11) | `['string','number']` (1.5) | `false` (2.11) | `'terminal'` (2.21) | 코어 기본 union (4.11) | `\"42\"` (string) | 1번과 동일 (기대 변경). |\n| 4 | pydantic `Optional[str]` (`anyOf:[{type:'string'},{type:'null'}]`) | `'string'` (2.11, 2.14, E13) | `'string'` (1.4) | `true` (2.11) | `'terminal'` (2.21) | `FormTypeInputString` | `\"42\"` (string) | **일치**: nullable 단일 string 노드로 깔끔히 축약됨. |\n| 5 | `['object','string']` with `properties` | `'union'` (2.2, E7) | `['object','string']` (1.5) | `false` (2.1) | `'terminal'` (강제, 2.22) | 코어 기본 union (원시 포함이므로 텍스트 박스, 4.12) | `\"42\"` (string) | **불일치**: `properties` 하위 폼 생성을 기대하겠지만 터미널 강제로 자식 노드가 생성되지 않음(2.22). **기대 변경**(소유자 결정 (c) 및 brief-common 43행에 따른 강제 터미널 제약). |\n| 6 | 본체 union + `then` narrowing (`a:['string','number']` + `then a:'number'`) | `'union'` (2.18, E25) | `['string','number']` (1.8, 2.18) | `false` | `'terminal'` (2.21) | 코어 기본 union (4.11) | `\"42\"` (string) | **심각한 불일치**: `then` 조건 활성화 시 `jsonSchema.type`은 `'number'`가 되나 `node.schemaType`은 그대로 union이어서 입력값 `\"42\"`가 string으로 저장됨 → ajv 검증기에서 형 에러 발생. **설계/기대 재검토 필요**(7절 위험 참조). |\n| 7 | `allOf` narrowing (`['string','number']` + `allOf:[{type:'string'}]`) | `'string'` (2.6, 2.8, E18) | `'string'` (1.4) | `false` (2.6) | `'terminal'` (2.21) | `FormTypeInputString` | `\"42\"` (string) | **일치**: 정적 연언 교집합으로 정적으로 단일 string 노드로 축약됨. |\n| 8 | OpenAPI 3.0 `nullable: true` (`{type:'string', nullable:true}`) | `'string'` (2.1) | `'string'` (1.4) | `true` (2.1) | `'terminal'` (2.21) | `FormTypeInputString` | `\"42\"` (string) | **일치**: OAS 3.0 명세대로 nullable이 활성화됨(M5). |\n\n---\n\n## 3 정합성\n\n§1(필드/형), §3(값), §4(입력), §5(검증기) 및 `brief-common.md` §2 간의 정합성을 검토한 결과, 상호 모순 없이 견고하게 정렬되어 있습니다.\n\n1. **목록의 일치**:\n   - `node.schemaType`은 `'null'`이 빠지고 `'integer'`가 `'number'`에 흡수된 동결 배열(1.5).\n   - §3.1의 `UnionSpec.kinds`가 `node.schemaType`과 동일 참조를 공유하며, 규칙 A의 `isMember`/`convert`(3.2–3.4)도 이 목록만 참조합니다.\n   - §4.1 `Hint.schemaType`과 §4.237 플러그인 계약 문구 역시 동일 목록을 전달받습니다.\n2. **nullability의 일치**:\n   - null은 `schemaType` 배열에서 완전히 배제되고 오직 `nullable` 불리언 속성으로만 단일화되었습니다(1.2, 1.5, 3.2, 4.1).\n   - 비우기 조작 시 `nullable ? null : undefined` 방출 규칙(3.2, 4.12, 4.14, brief-common 32행 REACT-027)이 전 구간에서 동일합니다.\n3. **이름의 일치**:\n   - `node.type` = 종류(`'union'`), `hint.type` = `node.type`, `props.type` = `node.type`(4.1–4.3)으로 1:1 대응되어 과거 `hint.type`이 `schemaType`을 가리키던 함정이 완전히 해소되었습니다.\n4. **기본적 사실(brief-common §2)과의 정합**:\n   - 규칙 A의 12건 경고등 증명(3.6), `default` 값의 규칙 A 통과(3.21, brief-common 48행), 객체/배열의 터미널 강제(2.22, brief-common 43행), 검증기의 값 불변 계약(5.1–5.4, brief-common 50행)이 원안 지침과 정확히 일치합니다.\n\n---\n\n## 4 비용\n\n1. **비-union 노드에 대한 추가 비용 (쓰기/커밋)**:\n   - **쓰기 비용**: 0. `interpret`는 union 노드의 경계에서만 실행됩니다(3.8). 기존 단일 노드들은 `BEHAVIORS[type][strategy]` 행에 따라 기존 `WRITE-075` 변환 테이블을 그대로 사용합니다(NODE-002, 3.5).\n   - **커밋 비용**: 0. `valueTypeMismatch` 계산은 커밋 시 raw 값이 변경된 노드에서만 O(1)로 재계산되며(3.13, 355행), 이는 기존 단일 노드의 VALUE-030 메커니즘과 동일합니다.\n   - **검증 비용**: 0. 기본 ajv 플러그인 인스턴스는 `mutating === false`이므로 복사 없이 원본 참조를 전달합니다(5.2, 5.3, 359행).\n2. **동일 참조 약속(Same-reference promise) 훼손 여부**:\n   - **훼손 없음**. 1.20 표에 명시된 대로 `type`, `strategy`, `nullable`, `schemaType`은 노드 생애주기 동안 고정되며, `schemaType` 배열은 청사진 칸별로 동결되어 모든 인스턴스와 `UnionSpec`이 공유합니다(1.5, 1.20, 3.1).\n   - 방출(3.20) 및 `interpret`(3.4) 역시 객체·배열 값을 변환하거나 깊은 복사하지 않고 원본 참조를 그대로 보존합니다.\n\n---\n\n## 5 이주\n\n§6의 M1–M21 목록은 오늘 코드베이스와의 차이점을 충실히 반영하고 있으나, 실제 코드 검증 결과 다음 사항들이 누락되어 있습니다.\n\n1. **`isTerminalNode` 타입 가드 반환 유니온 확장 누락**:\n   - 오늘 코드 `packages/canard/schema-form/src/core/nodes/filter.ts:197-198`:\n     `isTerminalNode(node): node is BooleanNode | NumberNode | StringNode | NullNode`\n   - 새 설계(1.13, NODE-041): `UnionNode`가 터미널 노드로 편입됨에 따라 타입 가드 대상에 `UnionNode`가 추가되어야 합니다.\n   - `isTerminalNode`로 좁힌 후 `switch (node.type)`를 돌리던 기존 소비자 코드는 `case 'union'` 누락으로 TS 에러 또는 런타임 누락을 겪게 되므로 M 목록에 명시되어야 합니다.\n2. **`filter.ts` 가드 판별 기준 필드 변경 (`group` → `strategy`)**:\n   - 오늘 코드 `filter.ts:77, 198`은 `node.group === 'branch'`, `node.group === 'terminal'`을 직접 검사합니다.\n   - 새 설계(1.13, 편집자 2)는 `strategy`를 검사하므로, `AbstractNode.group` 속성 폐기 및 `strategy` 전환이 M 목록에 명시되어야 합니다.\n3. **코어 기본 입력 정의 배열(`formTypeDefinitions`) 요소 추가**:\n   - 오늘 코드 `src/formTypeDefinitions/index.tsx:14-25`는 10개의 정의를 가집니다.\n   - 새 설계(4.11)에서 `{type:'union'}` 정의가 추가되어 11개가 됩니다. 플러그인이 없을 때의 폴백 동작 변화가 §6에 단독 M 항목으로 기술되지 않았습니다(§8 BLUEPRINT-033에만 언급).\n4. **`FormTypeTestObject` 인터페이스 정의 변경**:\n   - 오늘 코드 `src/types/formTypeInput.ts:163`: `type: JSONSchemaType | JSONSchemaType[]`\n   - 새 설계(4.5): `type: SchemaNodeType | SchemaNodeType[]`로 변경되고, `schemaType: JSONSchemaType | JSONSchemaType[]` 필드가 신설됩니다. M11/M12가 런타임 값 변화를 다루지만 타입 인터페이스 선언 자체의 브레이킹 체인지가 M에 독립 분리되어 있지 않습니다.\n\n---\n\n## 6 O1–O6\n\n- **O1 (가 권장: 동의)**: `schemaType`을 넓혀 union만 배열로 두고 `'null'`을 분리하는 안이 기존 단일 노드의 스칼라 호환성(`['integer','null']` → `'integer'`)을 유지하며, 1.6 불변식(`Array.isArray === isUnion`)을 가장 깔끔하게 성립시킵니다.\n- **O2 (가 권장: 동의)**: 값 리터럴에서 형을 역추론하는 것은 P1′(형상 규칙과 검증 의미 분리 원칙)을 정면 위반합니다. 지금 거부하고 추후 완화하는 것은 비파괴적이지만, 역은 파괴적 변화가 됩니다.\n- **O3 (가 권장: 동의)**: 객체/원시 혼합 `oneOf`를 지금 union 터미널로 강제하면 객체 분기의 자식 노드 렌더링 기능을 영구 상실합니다. 소유자의 분기 호스트 미결정 상태를 보존하기 위해 `UNKNOWN_JSON_SCHEMA`로 차단하는 것이 옳습니다.\n- **O4 (가 권장: 동의)**: `coerceTypes` 인스턴스 `bind`를 에러로 거부하면 서버 공유 설정을 재사용하는 사용자의 폼이 즉시 깨집니다. 해당 인스턴스 검증 호출 시에만 격리 사본을 생성(5.4)함으로써 기본 경로 제로 코스트와 데이터 무결성을 모두 달성합니다.\n- **O5 (가 권장: 동의)**: 계산된 목록을 유효 스키마 `type`에 역기입(도장)하면 유효 스키마의 덧씌움 불변성(BLUEPRINT-021)이 깨지고 불필요한 스키마 복제 비용이 발생합니다. `node.schemaType`을 신뢰할 수 있는 단일 출처로 삼는 것이 정석입니다.\n- **O6 (가 권장: 동의)**: `hint.type`과 `props.type`이 `node.schemaType`을 가리키던 오랜 '이름 함정'을 제거하고 모든 계층에서 `type === node.type`으로 통일해야만 향후 유지보수 혼선을 완전히 방지할 수 있습니다.\n\n---\n\n## 7 묻지 않은 위험\n\n**`if/then` 조건문으로 union 필드를 좁힐 때 발생하는 'UI·입력값과 검증기 간의 런타임 검증 불능 교착 상태'**\n\n- **원인**: 2.18 및 E25에 따르면, 본체에 선언된 union 필드(`type: ['string', 'number']`)가 `then` 절에서 단일 형(`type: 'number'`)으로 좁혀질 때, 노드는 union 상태를 유지하고 좁힘은 \"검증 전용\"으로 처리됩니다. 또한 1.7 및 1.8에 따라 청사진은 계산된 `type`을 `schemaType`에 반영하지 않으며, 입력 컴포넌트와 규칙 A는 오직 `node.schemaType`만 읽고 좁혀진 `node.jsonSchema.type`은 읽지 않습니다.\n- **문제 시나리오**:\n  1. 조건부 폼에서 사용자가 조건을 만족시켜 `then` 블록이 활성화됩니다.\n  2. 폼 작성자는 필드가 숫자로 제한되기를 기대하지만, 입력 컴포넌트는 여전히 `schemaType: ['string', 'number']`를 보고 텍스트 입력을 유지합니다.\n  3. 사용자가 키보드로 `\"42\"`를 입력하면, 규칙 A(3.27, 3.4)는 여전히 union 목록을 기준으로 판단하여 문자열 멤버십(Step ①)에 따라 **문자열 `\"42\"`로 저장**합니다.\n  4. 이후 검증기(ajv)가 실행될 때, ajv는 활성화된 `then` 스키마(`type: 'number'`)를 기준으로 검증하므로 `\"42\"`는 **타입 불일치 에러(`should be number`)**를 냅니다.\n- **영향**: 사용자는 화면상 숫자를 정상 입력했음에도 코어가 이를 문자열로 보존하여 검증을 영구히 통과할 수 없는 치명적인 UX 버그에 갇히게 되며, 1년 내 실무 복합 폼 도입 시 가장 먼저 터져 나올 고위험 결함입니다.\n\n---\n\n## 읽은 파일\n1. `/private/tmp/claude-501/-Users-Vincent-Workspace-albatrion/a8be555b-9dd3-4b7e-ac18-a45022199e69/scratchpad/swarm-union/brief-common.md`\n2. `/private/tmp/claude-501/-Users-Vincent-Workspace-albatrion/a8be555b-9dd3-4b7e-ac18-a45022199e69/scratchpad/swarm-union/merged.md`\n3. `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/types/formTypeRenderer.ts`\n4. `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/types/formTypeInput.ts`\n5. `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/types/index.ts`\n6. `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/formTypeDefinitions/index.tsx`\n7. `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/helpers/jsonSchema/extractSchemaInfo/extractSchemaInfo.ts`\n8. `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/core/nodes/filter.ts`\n9. `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/core/nodes/index.ts`\n10. `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/core/nodes/AbstractNode/AbstractNode.ts`\n11. `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/core/nodes/AbstractNode/utils/ValidationManager/ValidationManager.ts`\n12. `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form-antd5-plugin/src/formTypeInputs/FormTypeInputNumber.tsx`\n13. `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form-antd5-plugin/src/formTypeInputs/FormTypeInputString.tsx`","type":"message","recipient":"team-lead","content":"Provider: antigravity (via cennad, session_id 2a2…"}