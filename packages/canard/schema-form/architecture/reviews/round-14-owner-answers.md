# 14라운드 — 소유자의 답 (원문)

2026-09-24. 소유자가 `round-14-owner-review.md`를 deilen 뷰어에서 읽고 줄마다 남긴 답의 원문이다. 편집자는 고치지 않았다. 해석과 반영은 `round-14-values-check.md` §4와 `08-design-a-to-z.md`.

| 항목 | 소유자의 답(원문) |
| --- | --- |
| O-1 `&discriminator` 끌어올림 | 가 로 하죠. 다만, discriminator 를 서로 다르게 선언했거나 discriminator 이 분기마다 다른 타입이나 성질을 가지면 오류로 알려줘야 합니다. |
| O-2 예산 초과 신호의 지속 | 가 로 가죠. 다만, 이런 예산초과된 상태로 동작하는건 되도록 막는 방향을 원하긴 해요. |
| O-3 `UpdateValue` 출처 필드 | 더해도 되긴 하는데, 어차피 이벤트 컨텍스트 데이터니까, update 의 오리진을 구분할 니즈가 있을까요? |
| O-4 `&` 식 런타임 오류·풀리지 않는 `&injectTo` 대상 | 가로 가도 됩니다. 다만 전반적으로 "오류가 나도 동작하는" 형태보다는 안되면 오류를 터트려서 인지시키는 방향이 좋지 않을까 싶네요 |
| O-5 이름 둘과 리스너 예외의 프로덕션 출력 | 이 error 처리 기법에 대해서 확장 조사를 한번 해보세요. 질문 대부분이, 오류가 나도 form 은 동작시키고 console 을 낼까요, 아니면 그냥 터트릴까요 로 귀결되네요. 하나씩 읽고 답할 가치가 없습니다. 베스트케이스와 논리적 완결성을 지닌 방법을 찾으세요 |
| O-6 검증 실행의 합치기 | 가 확정 |
| O-7 Form 속성 `validatorFactory` | 맞긴 한데, 이건 plungin 을 통한 전역 속성이 아니라 특정 커스텀 검증기 등을 추가한 커스텀 인스턴스 주입기임. 제거할 이유가 있나? 설계를 확장하라. 필요한 기능이다. 가 금지, 나로 간다. |
| O-8 `unsetOnInactive`의 하위 트리 | 논리적 정합성을 따져보세요. 권고가 맞는거같긴 한데. 확신을 못하겠군요 |
| O-9 접두 없는 키의 닫힌 목록 | 네 확정하세요 |
| O-10 노드 공유 충돌 경고 | 이건 사실상 오류를 뿜으면서 form 이 터져야 할 것 같은데요. 경고만 일어나고 동작하는것처럼 보이는게 더 위험합니다 |
| O-11 `options` 배열의 병합 | 네 가로 가죠. 근데 그럼 merge 를 못쓰고 새로 구현해야 하는거 아닙니까? mege 함수에 옵션을 넣는게 나을까요? |
