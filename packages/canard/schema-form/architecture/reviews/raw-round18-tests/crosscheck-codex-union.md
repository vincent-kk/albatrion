{"to":"team-lead","summary":"Codex cross-check complete, all Q1-Q8 covered","message":"# 교차 확인 — union (codex)\n\n## 총평\n\n가장 큰 위험은 **검증기가 값을 제자리에서 바꾸는 것(Q8)**입니다. 경고만으로는 참조 안정성을 지킬 수 없습니다.  \n다음은 **객체·배열을 포함한 union의 확장 경계(Q2)**와 **`oneOf`/`anyOf`에서 노드 종류를 정하는 절차(Q3)**입니다. 이 둘은 공개 타입을 확정하기 전에 명시하는 편이 좋습니다.  \n규칙 A의 결과는 선언 순서와 무관합니다. 다만 기본 입력이 유효하지 않은 중간 글자를 노드에 보내는 동작은 `REACT-027`과 충돌합니다.  \n아래의 \"권장\"은 검토 의견이며, 아직 소유자가 확정한 규칙으로 취급하지 않았습니다.\n\n## Q1. 규칙 A의 순서 독립성 — 위험 8위\n\n- **판정:** 성립합니다. 먼저 기존 타입과 일치하는지 검사하고, 변환 후보를 모두 모아 *서로 다른 결과가 정확히 하나*일 때만 채택하면 배열 순열은 결과를 바꾸지 않습니다.\n- **근거:** [BLUEPRINT-033](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/blueprint.md:513>)과 [WRITE-075](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/write.md:1055>)의 전체 변환표에서, 서로 다른 두 타입이 같은 입력을 변환할 수 있는 경우는 **숫자 `1` 또는 `0`과 `string`+`boolean`을 모두 포함한 목록**뿐입니다. 이때 `number`가 목록에 있으면 기존 타입 일치로 값이 유지됩니다. `\"1\"`·`\"0\"`은 `number`만 받고, `\"true\"`·`\"false\"`는 `boolean`만 받습니다. `integer`와 `number`가 같은 숫자를 내는 경우는 접은 뒤 하나로 셉니다.\n- **실패 장면:** `1`을 `['string','boolean']`에 쓰면서 \"첫 성공\"을 택하면 배열 순서에 따라 `\"1\"` 또는 `true`가 됩니다.\n- **대안:** 둘 이상이 받으면 **값을 유지하고 경고**하는 현재 규칙을 권장합니다. 기존 타입을 보존하는 우선권은 이미 첫 단계에 있습니다. 그 뒤의 우선순위는 순서 대신 다른 임의의 정책을 도입할 뿐입니다.\n\n## Q2. 객체·배열까지 확장할 범위 — 위험 2위\n\n- **판정:** **(b) 원시 타입만 지금 지원하고, terminal 객체·배열 union을 위한 경계를 예약**하기를 권장합니다. 전체 값을 한 입력이 다루는 terminal union은 일관되게 정의할 수 있습니다. `object`는 배열이 아닌 객체, `array`는 `Array.isArray`로 판정하고 두 종류에는 자동 변환을 두지 않으면 됩니다.\n- **근거:** terminal union은 `node.type === 'union'`, `strategy === 'terminal'`, 자식 노드 없음으로 표현할 수 있습니다. 따라서 `find('/path/child')`는 자식 노드를 찾지 못합니다. 값 자체의 속성을 읽는 식과 자식 **노드**를 찾는 API를 구별해야 합니다. `['object','null']`·`['array','null']`은 계속 nullable 객체·배열입니다. [NODE-028/042](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/node.md:417>), [NODE-043](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/node.md:643>), [VALUE-034](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/value.md:531>)가 각각 전략, 자식 탐색, 빈 값 투영의 근거입니다.\n- **실패 장면:** 객체 값을 받는 terminal union에 플러그인이 `ChildNodeComponents`나 `find('/path/name')`를 기대하면 입력은 값을 표시해도 하위 필드는 조작할 수 없습니다. `omitEmpty`가 빈 `{}`·`[]`를 방출에서 빼는 경우와 기본값 적용 시점도 전체 값 기준으로 정의해야 합니다. 반대로 오류를 영구화하면 `Union[Cat, str]`처럼 객체와 원시 타입을 섞는 생성기 출력은 변환 없이는 사용할 수 없습니다. 오늘도 해당 다중 `type`은 [extractSchemaInfo.ts](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/helpers/jsonSchema/extractSchemaInfo/extractSchemaInfo.ts:22>)가 해석하지 못하고 [schemaNodeFactory.ts](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/core/nodes/schemaNodeFactory.ts:116>)의 `UNKNOWN_JSON_SCHEMA` 경로로 갑니다. 따라서 그 사례에는 이미 작동하던 폼의 호환성 손실은 없습니다.\n- **대안·예약할 경계:** 지금 `union`을 공개 타입에서 \"원시 값 전용\"으로 고정하지 마십시오. 청사진에는 **허용 타입 목록과 terminal 전략을 별도로 보존**하고, 공개 표면에는 안정된 `acceptedTypes` 같은 읽기 전용 목록을 마련하십시오. `InferValueType`과 `UnionNode`의 값 타입도 향후 객체·배열을 포함할 수 있게 설계해야 합니다. 나중에 허용할 때는 명시적인 whole-value 입력, 자식 탐색 불가, 전체 값 기본값·`omitEmpty` 규칙을 함께 열어야 합니다. 현재 [InferValueType 래퍼](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/types/value.ts:17>)가 외부 패키지에 위임하는 혼합 타입의 정확한 추론 결과는 **모름**입니다.\n\n## Q3. 자체 `type` 없는 원시 `oneOf`/`anyOf` — 위험 3위\n\n- **판정:** 옵션 **나**를 권장합니다. 명시된 분기 `type`을 노드 형상 선언으로 읽는 것은 P1′에 부합합니다. `minimum`·`enum` 같은 분기 제약을 core가 판정할 필요는 없습니다. 다만 타입 없는 `const`·`enum` 값에서 타입을 추론하는 범위까지 넓히지는 마십시오.\n- **근거:** [FRAGMENT-001](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/fragment.md:68>)은 게이트 없는 `oneOf`/`anyOf` 분기를 교차하지 않는 선언으로 다룹니다. [BLUEPRINT-034](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/blueprint.md:536>)도 분기 `type`을 읽어 혼합 오류를 판단합니다. null 분기를 nullable로 읽는 기존 방향과도 맞습니다.\n- **실패 장면:** `{oneOf:[{type:'integer'},{type:'number'}]}`를 단순히 number 노드로 표시하면 폼의 타입 경고등은 꺼져도, 정수 값은 두 분기에 모두 맞아 검증기의 `oneOf` 오류가 날 수 있습니다. 이는 형 불일치가 아닌 검증 오류입니다. `{oneOf:[{const:'a'},{type:'number'}]}`에서 `const`를 타입 선언으로 추측하면 validation 의미를 노드 형상 규칙에 끌어들입니다.\n- **대안·결정 절차:** ① 슬롯 자신의 명시적 `type`이 있으면 그것으로 노드 종류를 정하고, 분기는 검증 제약 또는 기존 variant 조각으로 처리합니다. ② 자체 `type`이 없고 게이트 없는 모든 분기에 명시적인 원시 `type`만 있으면 목록을 합칩니다. `null`을 떼어 nullable로, `integer`+`number`를 number로 접습니다. 남은 종류가 둘 이상이면 union leaf, 하나면 nullable 여부를 가진 단일 leaf, `null`만 있으면 null leaf입니다. ③ 객체 분기가 형상을 선언하면 기존 variant 호스트 규칙을 적용합니다. ④ 게이트가 있거나, 타입 없는 분기가 섞이거나, 객체·배열과 원시 종류가 섞여 유일한 형상을 정할 수 없으면 청사진 오류로 둡니다. 자체 `type` 없이 `enum: ['a',1]`만 있는 슬롯도 명시 타입을 요구하는 편이 P1′에 맞습니다. 객체 variant 호스트를 어디까지 타입 없이 인정하는지의 세부 구현은 이번에 읽은 자료만으로는 **모름**입니다.\n\n## Q4. 기본 입력의 초안 — 위험 4위\n\n- **판정:** **(나)**를 권장합니다. 기본 입력은 글자를 내부 초안으로 유지하고, 해석 결과가 선언된 타입의 값일 때만 노드에 보냅니다.\n- **근거:** [REACT-027](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/react.md:407>)은 미완성 글자를 입력이 소유하고, 노드에는 자기 타입 값 또는 `undefined`만 보내도록 합니다. [BLUEPRINT-033](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/blueprint.md:513>)의 \"문자열 입력 그대로\"를 현재 컴포넌트 구현까지 문자 그대로 적용하면 [FormTypeInputString.tsx](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/formTypeDefinitions/FormTypeInputString.tsx:10>)처럼 매 키 입력을 `onChange`로 보냅니다.\n- **실패 장면:** `['number','boolean']`에서 `42.5`를 입력하는 동안 `42.`는 어느 타입으로도 해석되지 않아 불필요한 `valueTypeMismatch`와 경고가 발생합니다.\n- **대안·계약 문구:** \"`union` 기본 입력은 표시 중인 글자를 초안으로 보유한다. 빈 칸은 `undefined`를 보내고, 입력한 글이 규칙 A에 따라 선언된 타입의 값 하나로 해석될 때만 그 값을 보낸다. 해석되지 않는 초안은 노드에 쓰지 않으며, 초안이 남은 채 blur 되면 표시를 현재 노드 값으로 되돌린다.\" `string`이 허용되면 글자는 기존 문자열 값으로 바로 성립합니다. 첫 타입의 위젯을 고르는 (다)는 선언 순서에 UI 의미를 부여합니다.\n\n## Q5. `then`/`allOf`의 좁힘 — 위험 6위\n\n- **판정:** **(ㄴ) 엄격한 부분집합 좁힘은 검증 전용**을 권장합니다. 활성 조각이 union의 허용 타입을 좁혀도 노드와 입력은 union으로 유지합니다. 입력을 바꾸려는 작성자는 body union 대신 게이트별 선언을 써야 합니다.\n- **근거:** 현재 [BLUEPRINT-011/012](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/blueprint.md:196>)대로 종류가 다른 선언을 합치면 충돌입니다. 따라서 (ㄴ)은 **명시적인 예외**로 기록해야 합니다. (ㄷ)은 게이트 전환 시 입력·노드 수명과 원본 값이 함께 바뀌어, 단순한 JSON Schema 좁힘보다 강한 동작을 만듭니다.\n- **실패 장면:** body가 `number|string`이고 `then`이 `number`일 때, (ㄴ)의 입력이 계속 문자열을 허용하면 작성자가 \"숫자 입력으로 바뀐다\"고 기대한 경우 혼란스럽습니다. validator가 없으면 그 문자열을 막지도 못합니다.\n- **대안:** 이 사용성 비용을 문서와 타입 힌트에 명시하십시오. 좁힘을 입력 선택으로 해석해야 한다는 요구가 우선이면 (ㄱ) 오류를 유지하고, body에서 해당 필드를 빼 `then`/`else`에 각각 선언하도록 안내하는 편이 (ㄷ)보다 일관됩니다. 부분집합이 아닌 재선언은 계속 기존 충돌 규칙을 적용해야 합니다.\n\n## Q6. UI 플러그인 표면 — 위험 5위\n\n- **판정:** 제시된 필드는 거의 충분하지만, **유효 타입 목록을 `jsonSchema.type` 파싱에만 의존시키는 것은 위험**합니다. 안정된 정규화 목록 접근자를 지금 정하는 편이 좋습니다.\n- **근거:** [NODE-041](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/node.md:596>)의 `type === 'union'`·`isUnionNode`, [SURFACE-052](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/surface.md:784>)의 `valueTypeMismatch`, [REACT-027](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/react.md:407>)의 초안 계약이 필요합니다. 현재 `FormTypeInputProps`에는 `jsonSchema`, `node`, `value`, `nullable`, `type` 등이 있으나 새 경고등은 아직 없습니다([formTypeInput.ts](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/types/formTypeInput.ts:29>)). 옵션 나를 택하면 원래 슬롯에는 자체 `type`이 없을 수 있어, `node.jsonSchema.type`의 \"유효·병합 목록\"을 반드시 만들어야 합니다.\n- **실패 장면:** 플러그인이 `jsonSchema.type`을 읽었는데 분기 유래 union에서는 `undefined`이면 위젯을 선택할 수 없습니다. `integer|string`의 `integer` 정보까지 무조건 `number|string`으로 지우면 정수 입력을 고를 단서도 사라집니다.\n- **대안:** 최소 공개 계약은 `node.type`, `isUnionNode`, `node.acceptedTypes: readonly …[]`(정규화·안정 참조), `nullable`, 현재 `value`, `valueTypeMismatch`, 입력 선택용 힌트 `'union'`입니다. `jsonSchema.type`은 검증 의미를 보존하십시오. 분기별 `format`·`enum`을 자동으로 UI 힌트로 합치는 규칙은 P1′의 범위를 넘으므로 필수 계약으로 약속하지 않는 편이 좋습니다.\n\n## Q7. union 안의 `integer` — 위험 7위\n\n- **판정:** 제시한 정련으로 한 타입 목록과 단일 노드를 같게 만들 수 있습니다. `integer`는 유한한 정수 값의 멤버십, `number`는 유한한 수의 멤버십으로 검사하고, 변환 결과가 같은 후보는 하나로 세어야 합니다.\n- **근거:** [WRITE-075](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/write.md:1055>)는 정수 입력의 안전 정수 변환과 기존 숫자를 자르지 않는 규칙을 구별합니다. [BLUEPRINT-034](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/blueprint.md:536>)는 `integer`+`number`를 number로 접습니다.\n- **실패 장면:** `['integer','boolean']`에 `12.5`를 넣고 JavaScript의 `typeof === 'number'`만 검사하면 정합으로 잘못 표시합니다. `['integer','number','boolean']`에 `\"2\"`를 넣고 두 숫자 후보를 별개로 세면 불필요하게 경고합니다.\n- **대안:** 멤버십 검사와 변환 성공 조건을 별도 함수로 명시하고, 접은 목록에서 후보 결과를 비교하십시오. 이 정의에서 확인한 한 타입 반례는 없습니다. 단, `['integer']`의 입력 해석이 단일 integer 노드와 같아지려면 안전 정수 조건까지 동일하게 구현해야 합니다.\n\n## Q8. 검증기의 부작용 — 위험 1위\n\n- **판정:** 제안한 **계약·개발 모드 경고·컴파일 시 스키마 복사만으로는 부족**합니다. 위험 옵션을 켠 사용자 ajv를 경고 후 그대로 실행하면 값 참조 안정성 계약이 깨질 수 있습니다.\n- **근거:** [validatorPlugin.ts](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form-ajv8-plugin/src/default/validatorPlugin.ts:45>)의 `bind()`는 외부 인스턴스를 받고, [createValidatorFactory.ts](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form-ajv8-plugin/src/validator/createValidatorFactory.ts:16>)는 `validate(data)`에 값을 직접 넘깁니다. 스키마 복사도 현재는 최상위 spread뿐입니다. 설치된 ajv 타입에는 `opts` 필드가 선언되어 있어([core.d.ts](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form-ajv8-plugin/node_modules/ajv/dist/core.d.ts:107>)) 옵션 점검 경로는 있습니다.\n- **실패 장면:** 값 객체를 읽어 참조를 보관한 소비자가 검증 후 같은 참조에서 다른 속성이나 타입을 발견할 수 있습니다. 또 중첩 스키마를 공유하는 얕은 복사는 \"컴파일 대상 스키마를 분리한다\"는 보장이 아닙니다.\n- **대안:** 플러그인에서 `coerceTypes`·`useDefaults`·`removeAdditional`을 `bind` 시와 컴파일 직전에 확인하고, 켜져 있으면 **실행을 거부하거나 그 경우에만 값 복사 경로를 선택**하십시오. 기본 경로에는 매 검증 복사를 넣지 않아 속도를 지킬 수 있습니다. 스키마는 컴파일 시 한 번 깊이 분리하십시오. 임의의 제삼자 validator까지 저렴하게 변경 여부를 완전 탐지하는 방법은 **모름**이므로, 그 경계에는 비변이 계약을 명시해야 합니다.\n\n## 우리가 묻지 않은 위험\n\n`valueTypeMismatch === false`가 **스키마 검증 통과**처럼 읽힐 수 있습니다. 특히 `oneOf` 중복 분기, `then`의 부분집합 좁힘, 분기별 `enum`에서 타입은 맞아도 검증은 실패합니다. 공개 문서에서 이 필드를 \"노드가 허용하는 JSON 타입과의 일치 여부\"로 한정해야 합니다([VALUE-030](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/value.md:435>), [ERROR-186](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/error.md:2699>)).\n\n## 읽은 파일 목록\n\n- 설계: [blueprint.md](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/blueprint.md>), [node.md](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/node.md>), [write.md](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/write.md>), [value.md](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/value.md>), [surface.md](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/surface.md>), [react.md](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/react.md>), [fragment.md](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/fragment.md>), [error.md](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/error.md>), [round-18-owner-answers.md](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/reviews/round-18-owner-answers.md>), [round-18-closing.md](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/reviews/round-18-closing.md>), [union-design-prep.md](</private/tmp/claude-501/-Users-Vincent-Workspace-albatrion/a8be555b-9dd3-4b7e-ac18-a45022199e69/scratchpad/drafts/union-design-prep.md>).\n- 현재 코드·지침: [CLAUDE.md](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/CLAUDE.md>), [extractSchemaInfo.ts](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/helpers/jsonSchema/extractSchemaInfo/extractSchemaInfo.ts>), [schemaNodeFactory.ts](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/core/nodes/schemaNodeFactory.ts>), [formTypeInput.ts](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/types/formTypeInput.ts>), [value.ts](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/types/value.ts>), [FormTypeInputString.tsx](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/formTypeDefinitions/FormTypeInputString.tsx>), ajv8 플러그인의 [validatorPlugin.ts](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form-ajv8-plugin/src/default/validatorPlugin.ts>)·[createValidatorFactory.ts](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form-ajv8-plugin/src/validator/createValidatorFactory.ts>).\n- 요청하신 `S/reports/standards-research-1.md`, `standards-research-2-branch.md`, `t1a-union-consistency.md`는 지정 경로에 디렉터리가 없어 읽지 못했습니다. 외부 라이브러리 비교는 제공하신 brief와 확인 가능한 초안의 요약 범위를 넘겨 독립 검증 사실로 주장하지 않았습니다.\n\n---\nprovider: codex (session_id 80c3ebc2-3a89-4823-ab30-a3ab2d28ed36) — no error. All Q1–Q8 already included 판정/근거/실패 장면/대안 on the first turn, so no follow-up was needed. Note: codex could not find `S/reports/standards-research-1.md`, `standards-research-2-branch.md`, or `t1a-union-consistency.md` at the given scratchpad path (directory absent) and explicitly flagged this rather than fabricating their content.","type":"message","recipient":"team-lead","content":"# 교차 확인 — union (codex)\n\n## 총평\n\n가장 큰 위험은 …"}

---

{"to":"team-lead","summary":"Codex Q2 follow-up reply (option c specified)","message":"## Q2 재판정: (c) 지금 허용, terminal 전용\n\n소유자께서 주신 요구를 반영하면 **`object`·`array`를 포함한 혼합 union을 지금 허용**하는 편이 맞습니다. 한 입력이 필드 값 전체를 소유한다는 terminal 계약으로 일관되게 설명할 수 있습니다. 앞서 권장한 (b)는 이 사업 요구에 비해 지나치게 좁았습니다.\n\n### 청사진과 값 규칙\n\n1. `type` 배열에서 `null`을 떼어 `nullable` 플래그로 두고, `integer`와 `number`가 함께 있으면 `number`로 접습니다. 접은 집합이 한 종류면 기존 단일 종류 규칙을 씁니다. 따라서 `['object','null']`은 **nullable object**이며 기존처럼 `branch` 또는 `terminal`을 선택할 수 있습니다.\n2. 접은 집합이 둘 이상이면 `node.type === 'union'`입니다. 그 안에 `object`나 `array`가 하나라도 있으면 `node.strategy === 'terminal'`을 **강제**합니다. `['object','array']`도 같은 규칙입니다. `options.terminal: true`는 중복 선언으로 허용하고, `false`는 청사진 오류로 처리합니다. 인라인 `FormTypeInput`도 이 결정을 바꾸지 않습니다. 이는 행이 하나인 종류의 전략을 행에서 정한다는 [NODE-047](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/node.md:722>)과 맞습니다.\n3. 이 슬롯의 `properties`·`items`는 **검증 스키마로 유지하되 자식 노드를 만들지 않습니다.** 예컨대 `{type:['object','string'], properties:{code:{type:'number'}}}`에서 `/field/code` 노드는 없습니다. core가 `code`의 개별 기본값이나 입력을 생성하지도 않습니다. 작성자가 그 내부 필드를 폼 노드로 조작하려면 별도의 객체 branch 선언이 필요합니다. 자식 노드가 생긴다고 오해하게 할 내부 form 전용 `controls`·`presentation` 선언은 조용히 무시하지 말고 청사진에서 진단해야 합니다.\n4. 규칙 A의 첫 단계에서는 null이 아닌 비배열 객체를 `object`, 배열을 `array`로 판정하여 **값과 참조를 그대로 유지**합니다. 그 둘로 향하는 변환은 없습니다. 그 밖의 타입에 대해서만 [WRITE-075](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/write.md:1055>)를 적용합니다. `\"{\"code\":1}\"` 같은 문자열을 객체로 JSON 파싱하는 규칙은 추가하지 않습니다. `string`이 목록에 있으면 그 문자열은 첫 단계에서 그대로 남습니다. 변환 후보가 없거나 여럿이면 기존처럼 보존하고 `valueTypeMismatch`를 켭니다([BLUEPRINT-033](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/blueprint.md:513>)).\n\n### 입력과 공개 타입\n\n**기본 fallback은 현재 문자열 컴포넌트를 그대로 재사용할 수 없습니다.** 현재 구현은 값을 `<input>`의 `defaultValue`로 전달하므로 객체를 의미 있는 형태로 표시하지 못합니다([FormTypeInputString.tsx](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/formTypeDefinitions/FormTypeInputString.tsx:10>)). 최소 계약으로, 현재 값이 객체·배열이면 JSON 직렬화 결과를 **읽기 전용 표시**로 보여 주고 비우기 동작을 제공하십시오. 직렬화할 수 없는 값은 표시 오류를 분리해야 합니다. 현재 값이 원시 타입이거나 없으면 기존 텍스트 입력과 초안 규칙을 사용합니다. 이 fallback만으로 객체·배열의 생성·편집까지 약속하지 않습니다. 그 일을 원하는 UI 플러그인은 목록을 보고 whole-value 입력을 제공하며, 객체·배열 값을 직접 보냅니다. 이 제한은 `formTypeDefinitions`가 최소 구현이라는 소유자 설명과도 맞습니다.\n\n`InferValueType<{type:['object','string','number']; properties:…}>`는 선언된 객체 모양 `| string | number`를, `['array','string']`은 `items`로 추론한 배열 `| string`을 돌려야 합니다. 값에 따라 자식 노드가 생긴다는 타입은 제공하지 않습니다. 확인한 하위 [`@winglet/json-schema`의 InferValueType](</Users/Vincent/Workspace/albatrion/packages/winglet/json-schema/src/types/value.ts:145>)은 배열 원소에 대한 분배 구조가 있지만, `@canard/schema-form`의 현재 [NormalizeType 래퍼](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/types/value.ts:10>)는 readonly 배열을 `type?: string[]`로 넓힙니다. 혼합 타입의 정밀 추론을 공개 계약으로 삼으려면 이 래퍼와 `JSONSchema` 허용 타입을 함께 고쳐야 합니다. UI에는 `node.type`, `strategy`, `nullable`, 현재 값, 경고등에 더해 순서에 의미를 부여하지 않는 안정된 `acceptedTypes` 목록을 주는 편이 안전합니다.\n\n### 탐색·식·방출\n\n- `find('/field')`는 union 노드를 돌려주고, `find('/field/code')`와 `findNodes`는 자식 노드를 찾지 못합니다. 이는 terminal 일반 규칙 [NODE-020](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/node.md:305>) 그대로입니다.\n- `controls` 식은 **노드 탐색이 아니라 방출 값**을 경로로 읽으므로, 방출된 객체의 `./field/code`나 배열의 `./field/0`은 읽을 수 있습니다. 값이 없거나 `omitEmpty`로 빠졌으면 `undefined`입니다. 이미 terminal 내부 경로와 `extras`를 값 수준으로 읽도록 정한 [CONTROLS-080](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/controls.md:1189>)에 규칙 변경은 필요하지 않습니다. whole-value 쓰기 때 그 내부 경로를 읽는 식의 의존성이 갱신되는지는 구현 시험에 넣어야 합니다.\n- `omitEmpty`는 **현재 값**이 `''`·빈 `{}`·빈 `[]`인지 보고 기존 투영을 적용하십시오. union이라는 종류만 보고 빈 그릇을 만들거나 값을 바꾸면 안 됩니다. `omitEmpty: false`이면 빈 객체·배열도 그대로 방출합니다. 루트 union에 방출 값이 없을 때는 객체·배열 *노드*용 빈 그릇을 합성하지 않고 `undefined`입니다([VALUE-034](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/value.md:531>)). 기본값도 union 필드의 **whole value**로 적용하며, `properties`·`items`를 순회해 자식 기본값을 채우지 않습니다.\n\n### 이주와 수정 범위\n\n현재 `['object','string']`과 `['object','array']`는 [extractSchemaInfo.ts](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/helpers/jsonSchema/extractSchemaInfo/extractSchemaInfo.ts:24>)에서 해석되지 않아 [schemaNodeFactory.ts](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/core/nodes/schemaNodeFactory.ts:116>)의 `UNKNOWN_JSON_SCHEMA` 경로로 갑니다. 새 규칙은 이 스키마를 처음으로 렌더하므로 이주 문서에는 **terminal만 생기며 내부 `find()`는 지원하지 않는다**고 적어야 합니다. `oneOf`로 생성된 혼합 스키마까지 허용하는 것은 별도 범위입니다. Q3의 분기 타입 수집 규칙을 혼합 종류까지 확대하지 않으면 그런 생성기 출력은 여전히 오류입니다.\n\n| 원장 영역 | 필요한 반영 |\n|---|---|\n| [BLUEPRINT-032/033/034](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/blueprint.md:492>), [NODE-041/047](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/node.md:596>) | 혼합 union 허용, nullable 접기, terminal 강제, `options.terminal: false` 오류 |\n| [WRITE-075](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/write.md:1055>), [VALUE-030/034](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/value.md:435>) | 객체·배열 멤버십과 참조 보존, 빈 whole value의 투영을 명확히 함 |\n| [REACT-027](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/react.md:407>), 공개 타입·입력 표면 | 객체·배열 fallback 표시, `acceptedTypes`, `InferValueType`과 `UnionNode` |\n| [LANDING-129](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/landing.md:1870>) | 기존 `UNKNOWN_JSON_SCHEMA`에서 terminal union으로 바뀌는 사례 추가 |\n\n**청사진·종류·입력 바깥의 핵심 규칙을 새로 만들 필요는 없습니다.** `find()`는 NODE-020, 값 경로 식은 CONTROLS-080, `omitEmpty`는 VALUE-034를 그대로 적용합니다. 다만 그 적용 결과와 타입 추론·이주 범위는 해당 원장 및 공개 타입에 명시해야 합니다.\n\n**허용하지 않을 한 가지:** 혼합 union에 `options.terminal: false`를 주어 값의 현재 타입에 따라 자식 노드를 만들거나 없애는 동작입니다. 같은 경로의 노드 형상과 `find()` 결과가 값 쓰기만으로 달라져 정적 청사진·노드 수명 계약을 깨고, 객체에서 문자열로 바뀔 때 자식 원본의 처리까지 새로 정의해야 하기 때문입니다.\n\n---\nprovider: codex (same session, turn 2) — no error. Answer covers everything asked: blueprint rule, rule A membership, default fallback input, InferValueType, find()/expressions, omitEmpty, migration, ledger areas, out-of-scope-layer check, and the one refusal with reason.","type":"message","recipient":"team-lead","content":"## Q2 재판정: (c) 지금 허용, terminal 전용\n\n소유자…"}

---

{"to":"team-lead","summary":"Codex Q2 turn 3 reply (option c, TERMINAL_OPTION_UNSUPPORTED etc)","message":"## Q2 재판정 — (c) 혼합 `type`을 지금 허용하고 terminal로 고정\n\n- **판정:** 소유자께서 제시한 `object|string|number`, `array|string` 요구에는 (c)가 적합합니다. **하나의 union 노드가 값 전체를 소유**하도록 정의하면 기존 terminal 계약을 적용할 수 있습니다. 앞선 (b) 권장은 철회합니다.\n- **근거:** 원장은 `union`에 terminal 행 하나를 두고, 행이 하나인 종류에서는 전략을 그 행에서 정합니다([BLUEPRINT-034](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/blueprint.md:536>), [NODE-047](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/node.md:722>)).\n- **실패 장면:** 종류는 `union`인데 현재 값이 객체라는 이유로 자식 노드를 생성하면, 같은 경로의 `find()` 결과와 입력 수명이 값 변경에 따라 달라집니다.\n- **대안:** 아래 규칙을 하나의 terminal 계약으로 확정하십시오.\n\n### 1. 청사진, 자식 선언, 전략 충돌\n\n`type`에서 `null`을 빼 `nullable`로 기록하고, `integer`와 `number`가 함께 있으면 `number`로 접습니다. 접은 종류가 하나면 기존 단일 종류입니다. 따라서 `['object','null']`은 nullable object이며 **강제 terminal union이 아닙니다**. 접은 종류가 둘 이상이면 `union`이고, `object` 또는 `array`가 들어 있어도 `strategy`는 항상 `terminal`입니다.\n\n그 슬롯에 적은 `properties`·`items`는 원본 검증 스키마에 남깁니다. 해당 값이 객체·배열일 때 validator가 사용할 제약이지, core가 자식 노드·자식 입력·자식 기본값을 만드는 선언은 아닙니다. 내부에 폼 전용 `controls`나 `presentation`을 두어 자식 동작을 기대하는 작성 사례는 조용히 무시하지 말고 청사진에서 진단해야 합니다.\n\n`options.terminal: true`는 허용되는 중복 명시입니다. **`options.terminal: false`는 무시하지 않고 청사진 오류 `TERMINAL_OPTION_UNSUPPORTED`**로 처리하십시오. 인라인 `FormTypeInput`이 함께 있어도 오류입니다. 인라인 입력만 있으면 그것을 렌더하되 전략은 이미 terminal로 확정되어 있습니다. `TERMINAL_STRATEGY_MISMATCH`는 활성 선언들의 전략을 비교하는 규칙이며, 행이 하나뿐인 union의 명시적 `false`에 적용할 코드가 아닙니다([NODE-028/042/047](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/node.md:417>), [ERROR-200](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/error.md:2917>)). 별도 선언이 같은 이름에 `object` 노드와 `union` 노드를 동시에 요구하면 그것은 전략 오류가 아니라 기존 **종류 충돌**입니다.\n\n### 2. 값 해석과 기본 입력\n\n규칙 A에서 null이 아닌 비배열 객체는 `object`, 배열은 `array`의 기존 타입 일치로 처리하고 **원래 참조를 유지**합니다. 객체·배열로 향하는 자동 변환은 없습니다. `\"{\"a\":1}\"`을 객체로 파싱하지 않으며, `string`도 허용된 목록이라면 그 문자열은 그대로 유지합니다. 숫자·문자열·불리언 사이에서만 [WRITE-075](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/write.md:1055>)의 변환 후보를 검사합니다. 어느 종류에도 맞지 않으면 값을 보존하고 경고등을 켭니다.\n\n현재의 [기본 문자열 입력](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/formTypeDefinitions/FormTypeInputString.tsx:10>)을 객체·배열 값에 그대로 쓰는 것은 거부해야 합니다. fallback은 그 값일 때 `JSON.stringify`한 내용을 **읽기 전용으로 표시**하고 비우기 동작을 제공하는 최소 표현이어야 합니다. 직렬화 실패도 표시 오류로 다룹니다. 객체·배열의 생성과 편집은 목록을 읽어 whole-value 값을 보내는 UI 플러그인이 맡습니다. 값이 원시 타입이거나 없을 때는 텍스트 입력의 초안 계약을 적용합니다. 이 제한을 명시해야 fallback이 객체 편집기인 것처럼 보이지 않습니다.\n\n### 3. 공개 타입, 탐색, 식\n\n`InferValueType`은 예를 들어 `['object','string','number']`와 `properties`에서 **선언된 객체 값 타입 `| string | number`**, `['array','string']`와 `items`에서 **배열 값 타입 `| string`**을 추론해야 합니다. 자식 *노드* 타입은 만들지 않습니다. 하위 [`@winglet/json-schema` 구현](</Users/Vincent/Workspace/albatrion/packages/winglet/json-schema/src/types/value.ts:145>)에는 타입 배열 원소별 값 타입을 합치는 구조가 있지만, 현재 schema-form의 [`NormalizeType`](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/types/value.ts:10>)은 readonly 배열의 `type`을 선택적 `string[]`로 넓힙니다. 이 래퍼와 혼합 배열을 허용할 공개 `JSONSchema` 타입을 함께 수정해야 정밀 추론을 약속할 수 있습니다.\n\n`find('/field')`는 union 노드를 찾지만 `find('/field/key')`는 `null`입니다. terminal 아래에는 자식 노드가 없다는 [NODE-020](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/node.md:305>) 그대로입니다. 반면 `computed`에 해당하는 새 `controls` 식은 **방출 값 트리**를 읽습니다. 따라서 방출된 객체의 `./field/key`나 배열의 `./field/0`은 읽을 수 있고, 값이 투영에서 빠지면 `undefined`입니다. [CONTROLS-080](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/controls.md:1189>)은 이미 terminal 내부의 값 경로를 허용합니다. whole-value 쓰기가 내부 경로를 읽는 식을 다시 평가하게 되는지만 구현 시험으로 확인하면 됩니다.\n\n### 4. `omitEmpty`, `default`, 이주\n\n`omitEmpty`는 union의 종류 이름이 아니라 **현재 whole value**를 보고 적용합니다. 빈 `{}`·`[]`는 기본 설정에서 방출에서 빠지고, `omitEmpty: false`이면 그대로 나갑니다. `node.value`의 원본을 지우지는 않습니다. 루트 union의 방출이 없다면 루트의 종류는 object/array가 아니므로 `outputValue`는 `undefined`입니다. 배열 아이템에서 방출되지 않은 union leaf의 자리는 기존 leaf 규칙대로 채워집니다([VALUE-034](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/value.md:531>)). 빈 객체·배열을 유의미하게 사용하는 작성자는 `omitEmpty: false`가 필요하다는 점을 이주 안내에 적어야 합니다.\n\n`controls.default > default`는 **union 노드 전체의 값**입니다. 노드가 새로 생기고 그 값이 없을 때 한 번 적용하며, `properties`·`items`의 자식 기본값을 core가 분배하지 않습니다. 특히 입력으로 받은 `{}`는 terminal union에 **이미 존재하는 값**이므로 whole-value `default`로 덮지 않습니다. 객체 *호스트*의 `{}`에 기본값을 분배하는 [WRITE-082](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/write.md:1186>)의 특례와 구별해야 합니다. 타입별 빈 값을 임의 생성하지 않는 [WRITE-089](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/write.md:1329>)는 그대로입니다.\n\n오늘 `['object','string']`·`['object','array']`는 [extractSchemaInfo.ts](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/helpers/jsonSchema/extractSchemaInfo/extractSchemaInfo.ts:24>)가 해석하지 못해 [schemaNodeFactory.ts](</Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/core/nodes/schemaNodeFactory.ts:116>)의 `UNKNOWN_JSON_SCHEMA`로 갑니다. 새 설계에서 이들은 처음으로 terminal 노드가 됩니다. 기존에 허용되던 `['object','null']`·`['array','null']`은 기존 nullable 노드로 남깁니다. 자체 `type`이 없는 혼합 `oneOf`/`anyOf`까지 허용하려면 Q3의 **분기 타입 수집**도 같은 범위로 넓혀야 하며, 이번 `type` 배열 규칙만으로 자동 해결되지는 않습니다.\n\n### 원장 범위와 거부할 경계\n\n| 반영할 영역 | 내용 |\n|---|---|\n| `BLUEPRINT-032/033/034`, `NODE-041/047`, `ERROR-200` | 혼합 타입 허용, 접기, terminal 강제, `options.terminal: false` 오류 |\n| `WRITE-075`, `VALUE-030/034`, `WRITE-082/089` | 객체·배열 멤버십, 빈 값 투영, whole-value 기본값의 적용 범위 |\n| `REACT-027`, 공개 타입·입력 표면 | fallback 표시, UI 입력 계약, `InferValueType`과 허용 타입 목록 |\n| `NODE-020`, `CONTROLS-080`, `LANDING-129` | 탐색·식 규칙의 적용 사례와 `UNKNOWN_JSON_SCHEMA` 이주 사례 |\n\n**청사진·종류·입력 바깥의 탐색, 식, 투영, 기본값 규칙 자체를 바꿀 필요는 없습니다.** 다만 위처럼 객체 호스트의 특례와 union terminal의 결과가 다른 지점을 원장에 명시하고 시험해야 합니다.\n\n**끝까지 허용하지 않을 한 가지는 혼합 union의 `branch` 전략입니다.** 특히 `options.terminal: false`로 현재 값이 객체일 때만 자식을 만들게 하면 정적 청사진, `find()`의 노드 신원, 게이트 전환과 자식 원본의 수명이 모두 값의 타입에 종속됩니다. 이는 이번 whole-value union의 일관된 경계를 깨므로 명시적인 청사진 오류가 맞습니다.\n\n---\nprovider: codex (same session 80c3ebc2-3a89-4823-ab30-a3ab2d28ed36, turn 3) — no error.","type":"message","recipient":"team-lead","content":"## Q2 재판정 — (c) 혼합 `type`을 지금 허용하고 te…"}