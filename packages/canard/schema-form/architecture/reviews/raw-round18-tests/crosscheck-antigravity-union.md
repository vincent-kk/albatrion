{"to":"team-lead","summary":"Antigravity union cross-check — complete answer","message":"Provider: antigravity (cennad, project_root=/Users/Vincent/Workspace/albatrion), no error. All Q1–Q8 arrived with 판정/근거/실패 장면/대안 filled on the first turn, so no follow-up was needed. No repository file was edited.\n\nFinal answer (verbatim):\n\n# 교차 확인 — union (antigravity)\n\n## 총평\n1. **가장 큰 위험은 다중 선언(`then`/`allOf`)에서의 union 좁힘 충돌(Q5)과 제네레이터 출력(`anyOf` 원시 분기, Q3) 처리 부재로 인한 폼 마운트 실패입니다.**\n2. 소유자의 **규칙 A(순서 무관, 유일 변환 일치 시 변환, 모호 시 원형 보존 및 경고등)**는 수학적으로 순서 독립성이 완벽히 성립하며(Q1), ajv 결합을 끊어 P1′과 완결성을 훌륭하게 만족합니다.\n3. 객체/배열의 union 혼합(Q2)은 코어 복합 구조(자식 노드 탐색 `find`, 포인터 바인딩, `omitEmpty`, 델타 계산)를 근본적으로 파괴하므로 **'원시 타입 한정 터미널 잎' 원칙을 고수**하되, 최상위 제약 해제는 `options.terminal` 명시 시 단일 입력에 위임하는 확장점으로 유보해야 합니다.\n4. 기본 입력의 실시간 스트리밍(Q4)은 REACT-027(입력이 초안을 소유)과 정면 충돌하여 소음(false-positive warning)을 유발하므로 **규칙 A로 검증된 확정값만 노드로 방출하는 계약(나)**으로 일치시켜야 합니다.\n\n---\n\n## Q1. 규칙 A의 순서 독립성 (Order Independence)\n\n| 항목 | 내용 |\n|---|---|\n| **판정** | **확인 (Refute 걱정, Confirm 순서 무관성).** 규칙 A는 `type` 배열의 선언 순서에 **전혀 영향을 받지 않습니다.** |\n| **근거** | 1. **1단계 (멤버십 검사):** 값 $v$가 선언된 타입 집합 $T$에 속하는지 여부는 집합 멤버십($v \\in \\bigcup_{t \\in T} \\text{Domain}(t)$)이므로 순서와 무관합니다.<br>2. **2단계 (유일 변환자 검사):** $T$의 모든 타입에 대해 변환 가능 여부를 판정하여 성공한 타겟 집합 $S = \\{t \\in T \\mid \\text{canCoerce}(v, t)\\}$를 구합니다. $|S| = 1$일 때만 해당 타입으로 변환하고, $|S| = 0$이거나 $|S| \\ge 2$이면 변환하지 않습니다. $S$는 원소 순서와 무관한 집합이므로 선언 순서가 개입할 여지가 없습니다.<br>3. **입력/타입 조합 전수 조사 (WRITE-075 S1 기준):**<br>- `number`: 오직 유한한 수 형태의 string만 number로 수용.<br>- `string`: 유한한 number와 boolean을 string으로 수용.<br>- `boolean`: 정확히 string `\"true\"`/`\"false\"`와 number `1`/`0`만 boolean으로 수용.<br>- *충돌 가능 조합 1:* 입력이 수 `1` 또는 `0`이고, 목록에 `string`과 `boolean`이 동시에 포함된 경우 (`1` → `\"1\"` or `true`). 변환 가능 집합 $S = \\{\\text{string}, \\text{boolean}\\}$으로 $|S| = 2$.<br>- *충돌 가능 조합 2:* 문자열 `\"1\"` 또는 `\"0\"`은 boolean 대상이 아님(`\"true\"`/`\"false\"`만 수용). 따라서 `['number', 'boolean']`에 `\"1\"`이 오면 $S = \\{\\text{number}\\}$로 유일하게 변환 성공. 모호하지 않음. |\n| **실패 장면** | `type: ['string', 'boolean']`인 슬롯에 초기값/외부 주입으로 수 `1`이 들어온 경우:<br>- ajv `coerceTypes` 방식: 앞선 타입(`string`)을 택해 `\"1\"`로 변환.<br>- 규칙 A 방식: 두 타입 모두 변환을 수용하므로 변환하지 않고 원본 `1` 유지 및 `valueTypeMismatch: true` 경고등 점등. |\n| **대안** | **소유자의 규칙 A (\"유일하지 않으면 유지하고 경고등\")를 원안대로 채택 권장.**<br>- \"항등성 보존 선택\"이나 임의 타이 브레이크는 순서 독립성을 지키더라도 \"어느 쪽이 사용자의 의도인지 core가 자의적으로 추측하지 않는다\"는 S1/P1′ 원칙을 위반합니다.<br>- 모호한 타입 결정권은 core가 아닌 **UI 플러그인(입력 컴포넌트)**에게 있습니다. |\n\n---\n\n## Q2. 범위: 원시 타입 전용 vs 터미널 객체/배열 포함\n\n| 항목 | 내용 |\n|---|---|\n| **판정** | **(b) 원시 타입 우선 확정 + 터미널 객체/배열은 명시적 확장점(`options.terminal: true` 시 단일 잎)으로 예약.** |\n| **근거** | 1. **구조적 충돌:** 객체/배열이 브랜치 노드가 되면 자식 노드 트리(`children`), 경로 탐색(`find('/path')`), `omitEmpty`, 델타 계산이 동작합니다. 그러나 원시 타입과 섞이거나 `{type: ['object', 'string']}`인 상태에서 동적으로 런타임 값에 따라 브랜치와 터미널을 오간다면 고정 청사진(BLUEPRINT-028/042) 원칙이 무너집니다.<br>2. **터미널 조건부 허용 시의 정합성:** 만약 `options.terminal: true`이거나 커스텀 폼 타입 입력이 제공되어 **자식 노드가 애초에 생성되지 않는 완전한 잎(Leaf) 노드**라면, `object`/`array`가 포함되어도 코어의 트리 구조를 깨뜨리지 않습니다(변환 없이 `typeof === 'object'` 판정만 수행).<br>3. **현행 코드 실태:** `extractSchemaInfo.ts:24-30`에서 배열 길이가 2를 넘거나 `null`이 아닌 다중 타입은 모두 `null`을 반환하여 `schemaNodeFactory.ts:116`에서 `UNKNOWN_JSON_SCHEMA` 에러를 던집니다. 즉 현행 동작은 완전 차단 상태입니다. |\n| **실패 장면** | `options.terminal: true` 없이 `type: ['object', 'string']`을 허용할 경우:<br>`form.find('/user/name')`과 같은 서브트리 조회 시, 현재 값이 문자열이면 대상 노드가 없고 객체면 존재하는 등 트리 형상이 런타임 값에 종속되어 `degraded` 상태 또는 널 참조 크래시 발생. |\n| **대안** | - **PR-1/PR-2:** `type`에 `object` 또는 `array`가 원시 타입과 섞여 있는 경우, 원칙적으로 청사진 오류(`UNKNOWN_JSON_SCHEMA`) 유지.<br>- **확장점 예약 (Future-proof):** 추후 허용 시 breaking change가 없도록, 노드 표면에서 `node.type === 'union'`일 때 `node.children`은 항상 빈 객체(또는 미정의)로 고정하고, `node.strategy === 'terminal'`로 고정. 복합형 union은 \"오직 터미널 전략(`options.terminal: true` 또는 단일 전용 인라인 입력 바인딩)일 때만 유효한 단일 잎 노드\"로 열어두도록 문서화 예약. |\n\n---\n\n## Q3. 자기 `type` 없는 원시 `oneOf`/`anyOf` (옵션 나)\n\n| 항목 | 내용 |\n|---|---|\n| **판정** | **수용 권장 (옵션 나). 단, variant 호스트와의 판별 절차를 명확히 형식화해야 함.** |\n| **근거** | 1. **P1′ 부합성:** P1′은 \"형상을 결정하는 문법만 읽는다\"입니다. OpenAPI 3.0, Pydantic, TypeBox 등 주요 제네레이터는 `string | number`를 `anyOf: [{type: 'string'}, {type: 'number'}]`로 출력합니다. 이를 오류로 처리하면 실제 스키마 생태계에서 폼이 서지 않아 실용성을 잃습니다.<br>2. **이미 존재하는 접기 규칙과의 일관성:** 이미 null 분기(`{anyOf: [{type: 'string'}, {type: 'null'}]}`)는 nullable 플래그로 추출하고 있습니다(`isNullBranch`). 이를 원시 타입 다중 추출로 일반화하는 것은 논리적 연장선입니다. |\n| **실패 장면** | Pydantic v2에서 생성된 스키마 `{ anyOf: [{ type: 'string', minLength: 1 }, { type: 'number', minimum: 0 }] }`를 로드할 때:<br>- 현행/미지원 시: 자기 `type`이 없고 분기 타입이 달라 청사진 오류 발생.<br>- 옵션 나 적용 시: `type: ['string', 'number']`를 가진 `union` 잎 노드로 성공적으로 빌드되고, `minLength`/`minimum`은 ajv 검증기에 온전히 위임됨. |\n| **대안** | **엄밀한 슬롯 분류 절차 (Decision Procedure) 확립:**<br>1. **명시적 `type`이 있는 경우:** 해당 `type`의 규칙을 따름.<br>2. **명시적 `type`이 없는 슬롯에 `oneOf`/`anyOf`가 존재할 때:**<br>  - a. 모든 분기가 원시 타입(`string`, `number`, `integer`, `boolean`, `null`)이거나 `{const: primitive}`, `{enum: [primitives]}`로만 구성된 경우:<br>    - `null` 분기/값 제거 → `nullable: true` 플래그 설정.<br>    - `integer` → `number` 접기.<br>    - 남은 고유 타입이 1개면 **해당 단일 원시 노드**, 2개 이상이면 **`union` 노드**로 합성.<br>  - b. 분기 중 하나라도 `type: 'object'`(또는 `properties`)나 `type: 'array'`(또는 `items`)를 포함하는 경우:<br>    - `controls.discriminator` 또는 판별자 규칙을 따르는 **variant 호스트(구 union 호스트)**로 취급.<br>  - c. `const`/`enum`만 있고 타입이 없는 경우: 들어있는 값 리터럴의 JS 타입을 검사하여 위 규칙 a, b로 귀속.<br>  - d. `oneOf` 분기 간 중복(`integer`와 `number` 등): 코어는 `number`로 통합하고, 유효성 판정은 검증기 플러그인에 전적 위임. |\n\n---\n\n## Q4. 기본 입력과 입력 초안(Draft) 계약 (REACT-027)\n\n| 항목 | 내용 |\n|---|---|\n| **판정** | **(나)안 채택 권장.** 기본 입력은 규칙 A로 평가하여 나열된 타입 중 하나로 해석되는 값만 노드로 방출하고, 미완성 초안은 입력 내부 로컬 상태로 유지한다. |\n| **근거** | 1. **REACT-027 계약 준수:** \"초안은 입력이 스스로 들고, 노드에는 자기 형의 값이나 없음만 보낸다.\"<br>2. **경고등 소음 방지:** `['number', 'boolean']` 스키마에서 사용자가 `42.5`를 입력하기 위해 `4` → `42` → `42.`을 타이핑할 때, `42.` 시점에 경고등과 warning 이벤트가 발화하는 것은 명백한 UX/DX 결함입니다.<br>3. `string`이 포함된 union(`['string', 'number']`)에서는 모든 텍스트가 string으로 즉시 유효하므로 지연 없이 스트리밍되어 기존 (가)안과 동일하게 동작합니다. |\n| **실패 장면** | (가)안 유지 시: `type: ['number', 'boolean']` 필드에 사용자가 소수점을 입력하는 순간 `VALUE_TYPE_MISMATCH` 경고등이 켜지고 `onError` 콜백이 호출되어 화면에 에러 플래그가 깜빡거림. |\n| **대안** | **계약 텍스트 명문화:**<br>> \"기본 union 입력 컴포넌트는 사용자의 텍스트 입력을 로컬 draft로 보유한다. 입력 변경 시 규칙 A에 따라 후보 타입 중 정확히 하나로 변환 가능하거나 멤버십을 만족하는 경우에만 `onChange(convertedValue)`를 노드로 방출한다. 어떤 타입으로도 정합하지 않는 과도기 텍스트(예: 수 입력 도중의 `42.`)는 draft로만 유지하며 노드로 방출하지 않는다. 포커스를 잃었을 때(blur) draft가 여전히 어떤 타입으로도 해석 불가능하다면 입력을 노드의 기존 커밋 값(또는 빈 값)으로 롤백한다.\" |\n\n---\n\n## Q5. 본체 union을 `then`/`allOf`에서 좁히기 (Narrowing)\n\n| 항목 | 내용 |\n|---|---|\n| **판정** | **(ㄴ) 좁힘은 검증기(ajv)에 위임하며 코어 노드는 `union`을 유지.** (장기적으로 필요 시 (ㄷ) 검토) |\n| **근거** | 1. **P1′ 및 성능 원칙:** 게이트 전환 시마다 노드를 파괴/재생성(re-create)하거나 종류를 `union` ↔ `number`로 교체하는 비용을 방지하고, \"동일 참조 유지\" 가치를 보호합니다.<br>2. **BLUEPRINT-011/012 정합성:** 정적 스키마 트리에서 동일 슬롯의 본체와 조건부가 서로 다른 노드 종류(kind)를 선언하여 충돌 오류를 내는 대신, `then`의 스키마는 검증 오버레이로 작동하여 ajv가 평가하게 하는 것이 가장 안전합니다.<br>3. UI 입력은 이미 복수 타입을 허용하고 있으므로, `then`이 활성화되었을 때 사용자가 허용되지 않는 타입(예: string)을 입력하면 검증기가 거부(validation error)함으로써 자연스럽게 폼 제출이 차단됩니다. |\n| **실패 장면** | (ㄱ) 채택 시: 조건부로 특정 필드를 필수/특정 타입으로 한정하는 일반적 스키마 패턴(`if: { ... }, then: { properties: { val: { type: 'number' } } }`)에서 폼이 정착 오류(`degraded`)를 일으키며 붕괴. |\n| **대안** | - **기본 동작 (ㄴ):** 조건부 블록(`then`/`else`)의 `type` 선언이 본체 `union`의 부분집합인 경우, 노드 종류를 변경하지 않고 union 노드로 유지하며 해당 제약은 검증기 플러그인에 오버레이로 전달.<br>- **고급 요구 (형 고르기):** 런타임에 입력 위젯 자체를 전환해야 하는 엄밀한 형 고르기가 필요한 경우, 소유자의 지침대로 본체 선언을 비우고 `then`과 `else` 양쪽에 각각 단일 타입을 선언하여 게이트 분기 전환 매커니즘을 타도록 유도. |\n\n---\n\n## Q6. UI 플러그인이 필요로 하는 공개 표면\n\n| 항목 | 내용 |\n|---|---|\n| **판정** | **충분함. 단, `node.types` (또는 `node.kinds`) 읽기 전용 접근자 1개 추가 강력 권장.** |\n| **근거** | - **현재 제공 예정:** `node.type === 'union'`, `isUnionNode(node)`, `node.jsonSchema.type` (배열), `node.nullable`, `node.valueTypeMismatch`, `node.value`.<br>- **결함 가능성:** UI 플러그인이 매번 `node.jsonSchema.type`을 원시 파싱해야 한다면, `integer`가 `number`로 접혔는지 여부, `null`이 플래그로 빠졌는지 여부를 플러그인마다 제각각 구현해야 하므로 파편화와 버그가 발생합니다. |\n| **실패 장면** | UI 플러그인이 `node.jsonSchema.type`을 단순 확인하여 `integer`에 맞는 별도 정수 입력을 띄우려 하거나, `['string', 'null']` 스키마가 들어왔을 때 이를 `union` 입력으로 오인하여 렌더링하는 사태 발생. |\n| **대안** | 노드 인터페이스에 정제된 타입 목록 접근자 추가:<br>```ts\nexport interface UnionSchemaNode extends SchemaNodeBase {\n  readonly type: 'union';\n  /** 접기(integer->number) 및 null 제거가 완료된 유효 원시 타입 목록 */\n  readonly types: readonly ('string' | 'number' | 'boolean')[];\n  readonly nullable: boolean;\n  readonly valueTypeMismatch: boolean;\n}\n``` |\n\n---\n\n## Q7. `union` 내부의 `integer` 처리 및 단일 노드 정합성\n\n| 항목 | 내용 |\n|---|---|\n| **판정** | **확인 (Confirm). 반례 없음.** |\n| **근거** | 1. **JSON Schema 명세 일치:** JSON Schema 2020-12 §6.1.1에서 `integer`는 수학적으로 소수부가 0인 `number`의 부분집합입니다.<br>2. **판정 및 변환:**<br>  - 멤버십 판정: 값 $v$가 정수이면 `integer`와 `number` 둘 다 만족하지만, 접힘 규칙에 의해 단일 `number` 범주로 통합 계산됩니다.<br>  - WRITE-075에 따른 변환: string `\"42\"` → 수 `42`. `integer` 변환과 `number` 변환 결과가 동일한 값 `42`이므로 유일 변환으로 카운트됩니다 (동일 값 중복 카운트 방지).<br>  - `['integer', 'number']`는 청사진 단계에서 `number` 단일 노드로 접히므로 단일 노드 동작과 100% 동일합니다.<br>3. `['integer', 'boolean']`에 `\"1.5\"`가 들어오면 `integer` 변환은 실패(안전한 정수 아님)하고 `boolean` 변환도 실패하므로 경고등이 켜지며, 이는 단일 `integer` 노드의 동작과 정확히 일치합니다. |\n| **실패 장면** | 없음. (동일 값으로 변환되는 복수 타겟을 단일 성공으로 취급하는 규칙 덕분에 충돌이 발생하지 않음) |\n| **대안** | 설계 원안 유지. |\n\n---\n\n## Q8. 검증기 부수효과 (Validator Side Effects / ajv 변이 방지)\n\n| 항목 | 내용 |\n|---|---|\n| **판정** | **타당함. 제안된 규약(\"검증기는 값/스키마를 변이하지 않는다\")에 추가로, 개발 모드(dev) `Object.freeze` 얕은 1회 검증 방어책 병행 권장.** |\n| **근거** | 1. `createValidatorFactory.ts`를 확인한 결과, ajv 컴파일 시 `{ ...jsonSchema, $async: true }`로 스키마는 얕은 복사되지만, `validate(data)` 호출 시 넘어가는 `data`는 코어 내부 값의 참조 그대로 전달됩니다.<br>2. ajv의 `coerceTypes: true`, `useDefaults: true`, `removeAdditional: true` 옵션이 켜져 있으면 ajv는 원본 객체/배열의 프로퍼티를 인플레이스(in-place)로 변이시킵니다.<br>3. 프로덕션 매 검증마다 `structuredClone`이나 deep freeze를 거치는 것은 라이브러리의 핵심 가치인 \"속도 최우선, 최소 연산\"을 심각하게 훼손합니다. |\n| **실패 장면** | 소비자가 `new Ajv({ useDefaults: true, coerceTypes: true })`로 생성된 ajv 인스턴스를 바인딩한 후 검증 실행 시, 코어 노드가 들고 있는 상태 값이 코어 몰래 변이되어 폼의 리액티브 상태 머신과 불일치 발생. |\n| **대안** | 1. **계약 및 Bind 시점 검사 (기존 제안):** `bind(validator)` 시점에 `ajv.opts.coerceTypes` 또는 `useDefaults`가 활성화되어 있으면 경고 로그(`console.warn`) 출력.<br>2. **저비용 런타임 탐지 (Dev-only check):**<br>   `process.env.NODE_ENV !== 'production'` 환경에서 최초 1회 또는 샘플링 방식으로 `Object.freeze(Array.isArray(data) ? [...data] : typeof data === 'object' && data !== null ? { ...data } : data)`를 전달하여, 변이 시도 시 TypeError가 발생하도록 유도하여 조기 발견. |\n\n---\n\n## 우리가 묻지 않은 위험 (Unasked Risks)\n\n1. **`defaultValue` 처리에서의 다중 타입 모호성:**\n   - 필드에 `default: 0`이 선언되어 있고 스키마가 `type: ['string', 'boolean']`인 경우, 초기화 단계에서 규칙 A를 타게 되면 `0`은 `string`(`\"0\"`)과 `boolean`(`false`) 모두 변환 가능하여 변환되지 못하고 `valueTypeMismatch` 경고등이 켜진 채 폼이 마운트될 수 있습니다. 스키마 기본값 파싱 시에도 규칙 A가 동일하게 적용되는지 명문화가 필요합니다.\n2. **`formTypeInputMap` 매핑 키 우선순위:**\n   - 렌더러가 입력을 결정할 때 `formTypeInputMap`을 조회합니다. `type: 'union'`인 노드에 대해 사용자가 `union`이라는 키로 입력을 등록할 수 있는지, 아니면 개별 원시 타입(예: `string`)으로 등록된 입력이 폴백되는지 순서가 확정되어야 UI 플러그인 저작자가 혼선을 겪지 않습니다 (`Hint.type: 'union'` 지원 필요).\n\n---\n\n## 읽은 파일 목록\n- `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/reviews/round-18-owner-answers.md`\n- `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/reviews/round-18-closing.md`\n- `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/blueprint.md`\n- `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/node.md`\n- `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/write.md`\n- `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/value.md`\n- `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/surface.md`\n- `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/error.md`\n- `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/react.md`\n- `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form/architecture/ledger/fragment.md`\n- `/private/tmp/claude-501/-Users-Vincent-Workspace-albatrion/a8be555b-9dd3-4b7e-ac18-a45022199e69/reports/t1a-union-consistency.md` (경로 표기 확인 필요 — brief의 `S/reports/...`와 `scratchpad` 세그먼트 누락 차이가 있음, 원문 그대로 인용)\n- `/private/tmp/claude-501/-Users-Vincent-Workspace-albatrion/a8be555b-9dd3-4b7e-ac18-a45022199e69/scratchpad/drafts/union-design-prep.md`\n- `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/helpers/jsonSchema/extractSchemaInfo/extractSchemaInfo.ts`\n- `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form/src/core/nodes/schemaNodeFactory.ts`\n- `/Users/Vincent/Workspace/albatrion/packages/canard/schema-form-ajv8-plugin/src/validator/createValidatorFactory.ts`\n\nNote: I added the parenthetical on the t1a-union-consistency.md line above because antigravity's listed path drops the `scratchpad` segment present in the brief's `S` alias (`.../a8be555b-.../reports/...` vs `.../a8be555b-.../scratchpad/reports/...`) — flagging it rather than silently correcting, since you asked for the answer unchanged. Everything else is quoted exactly as returned.","type":"message","recipient":"team-lead","content":"Provider: antigravity (cennad, project_root=/User…"}

---

{"to":"team-lead","summary":"Antigravity Q2 follow-up — option (c) spec, verbatim","message":"Same antigravity conversation continued (session_id 13e658f1-abd0-4701-afb8-7bfdd2c92bd1, turn 2), no new conversation started, no repo file edited. No error. Reply (verbatim):\n\n# Q2 구체화 검토: 옵션 (c) 'allow now, terminal only'의 구체적 명세와 평가\n\n소유자의 문제의식(\"`object|string|number`나 `array|string` 같은 다중 타입 필드는 흔한 비즈니스 요구사항이며, 복잡한 `if/then/else`나 `oneOf`를 강제하기보다 **단일 터미널(전체 값) 노드**로 처리하는 것이 훨씬 일관적이다\")에 따라, **옵션 (c)를 현시점에 허용할 때의 완전하고 구체적인 설계 명세**와 그에 수반되는 영향 범위를 분석합니다.\n\n---\n\n## 1. 옵션 (c) 단일 터미널 union 노드의 구체적 명세\n\n### 1-1. 청사진 규칙 (Blueprint Rule)\n- **타입 집합 접기 및 정합 (Folded Set):**\n  - 선언의 `type` 배열에서 `null`을 제거하여 노드의 `nullable: true` 플래그로 추출합니다.\n  - `integer`는 `number`로 접습니다.\n  - 정제된 타입 집합 $T \\subseteq \\{\\text{'string'}, \\text{'number'}, \\text{'boolean'}, \\text{'object'}, \\text{'array'}\\}$의 원소 수가 2개 이상이면 노드의 종류는 **`union`**이 됩니다.\n- **강제 터미널 전략 (Forced Terminal):**\n  - 접힌 집합 $T$에 `'object'` 또는 `'array'`가 단 하나라도 포함된 경우, 이 노드는 **무조건 `strategy: 'terminal'`로 강제**됩니다.\n  - 이 노드는 자식 노드(`children`)를 일절 생성하지 않으며 잎(Leaf) 노드로 고정됩니다.\n- **슬롯에 동반된 `properties` / `items`의 취급:**\n  - **폼 트리 형성에서는 완전 무시(검증 오버레이로만 잔존):**\n    - 해당 슬롯에 `properties`나 `items`가 작성되어 있더라도 코어 청사진은 자식 노드 트리를 구축하지 않습니다.\n    - `properties`나 `items`는 오직 검증기 플러그인(ajv)으로 전달되는 유효 스키마(`node.jsonSchema`)에만 보존되어, 입력 컴포넌트가 객체/배열 전체를 방출했을 때의 내부 스키마 검증용으로만 동작합니다.\n- **`options.terminal`과의 상호작용 (Interaction with `options.terminal`):**\n  - 이미 강제 터미널이므로 `options.terminal: true`와 자연스럽게 합치됩니다.\n  - 만약 작성자가 `options.terminal: false`를 명시하더라도, **복합 union 노드에서는 이를 무시하고 청사진 경고/오류 없이 `terminal`을 강제**합니다 (브랜치 형성을 시도할 경우 런타임 값에 따라 트리가 붕괴되므로 허용 불가).\n\n---\n\n### 1-2. 규칙 A의 동작 (Rule A on Object/Array Union)\n- **멤버십 검사:**\n  - `object`: `typeof v === 'object' && v !== null && !Array.isArray(v)`\n  - `array`: `Array.isArray(v)`\n  - 원시 타입: 기존 JS 원시 타입 판정(`typeof` 및 `number`의 유한성).\n  - 값이 $T$ 내의 어떤 타입에라도 해당하면 **원형 그대로 유지**합니다.\n- **변환표 (Coercion):**\n  - **`object`와 `array`는 변환 규칙이 없습니다(No Coercion).**\n    - 문자열 `\"{}\"`나 `\"[1, 2]\"`를 `JSON.parse`하여 객체/배열로 바꾸지 않습니다(코어 완결성 및 안정성 원칙).\n    - 객체나 배열을 `JSON.stringify`하여 문자열로 변환하지도 않습니다.\n  - 따라서 변환 시도는 오직 목록 내의 **원시 타입 후보들 간(WRITE-075 S1)**에서만 일어납니다.\n  - 예: `type: ['object', 'number']` 슬롯에 문자열 `\"42\"`가 들어오면, `object` 변환은 없고 `number` 변환만 성공($|S|=1$)하므로 `42`로 변환됩니다. 만약 `\"{}\"` 문자열이 들어오면 `number` 변환 실패, `object` 변환 없음($|S|=0$)이므로 원형 보존 및 `valueTypeMismatch: true`가 켜집니다.\n\n---\n\n### 1-3. 기본 폴백 입력 (Default Fallback Input)과 `[object Object]` 방어\n- **문제점:**\n  - 기본 입력인 단순 문자열 입력(`FormTypeInputString`)에 객체나 배열이 들어오면 `<input value={value} />`에 의해 브라우저에서 `[object Object]`로 강제 렌더링되거나 React 에러가 발생합니다.\n- **해결 명세:**\n  - 기본 fallback 입력이 `isUnionNode`이면서 현재 값의 `typeof === 'object'`(배열 포함)인 값을 받았을 때는:\n    1. UI 플러그인이 전용 입력을 제공하지 않은 미바인딩 상태라면, 입력을 비활성화(disabled)하고 안내용 읽기 전용 표기(예: `JSON.stringify(value)` 또는 `(Complex Object)`)를 렌더링합니다.\n    2. 또는 REACT-027 계약에 따라 **`FormTypeInputString`의 `value` prop 바인딩 시 `typeof value === 'string' ? value : ''`로 방어**하고, 문자열이 아닐 때는 무효 표지/경고등 상태로 렌더링하여 `[object Object]` 문자열로 오염되는 것을 원천 차단합니다.\n  - 작성자에게는 문서와 경고를 통해 \"객체/배열이 포함된 union 슬롯은 반드시 전용 복합 잎 입력 컴포넌트(JSON 에디터, 태그 인풋 등)를 바인딩하라\"고 안내합니다.\n\n---\n\n### 1-4. 공용 기능과의 상호작용\n\n| 영역 | 동작 및 명세 |\n|---|---|\n| **`find('/path')`** | 이 노드는 **터미널 노드**이므로, 이 노드 아래의 하위 경로(예: `/field/nestedKey`)에 대한 `form.find()` 호출은 자식 노드가 없으므로 항상 `undefined`를 반환합니다. (기존 단일 터미널 객체 노드의 동작과 100% 동일) |\n| **게이트 / 표현식 (Expressions)** | 게이트(`if`, `controls.active`)나 표현식이 이 노드의 값을 읽을 때는 하위 노드 참조가 아닌 **노드의 전체 루트 값(`node.value`)**을 읽습니다. JS 객체 참조 접근이므로 값 타입이 객체든 문자열이든 정상 동작합니다. |\n| **`omitEmpty`** | 터미널 잎이므로 잎 노드의 `omitEmpty` 규칙이 적용됩니다. 빈 문자열(`\"\"`), 빈 객체(`{}`), 빈 배열(`[]`)이 방출 값에서 제거될지 여부는 `omitEmpty` 설정에 따라 잎 레벨에서 일관되게 평가됩니다. |\n| **`InferValueType`** | TS 타입 추론 시, 정제된 타입들의 합집합으로 정확히 추론됩니다.<br>`type: ['object', 'string']` → `Record<string, unknown> \\| string \\| (nullable ? null : never)`. |\n| **이주 (Migration)** | 기존에는 `extractSchemaInfo.ts`에서 길이가 2를 넘거나 `null`이 아닌 다중 타입은 `null`을 반환하여 `UNKNOWN_JSON_SCHEMA` 에러가 발생했으나, 새 설계에서는 에러 없이 **단일 터미널 union 노드**로 폼이 정상 마운트됩니다. |\n\n---\n\n## 2. 영향받는 원장(Ledger) 영역과 변경 필요성\n\n### 영향을 받는 원장 항목\n1. **BLUEPRINT-032 / BLUEPRINT-034:**\n   - \"원시 타입만의 `type` 배열이 `union` 잎이 된다\"는 제한을 해제하고, \"`object`/`array`를 포함한 다중 타입도 `union` 종류의 터미널 잎 노드가 된다\"로 개정.\n   - 18C-02 (5)번의 \"원시 타입과 객체/배열의 섞임은 청사진 오류\"라는 규정을 공식 철회.\n2. **NODE-028 / NODE-042:**\n   - 터미널 전략 결정 순서에 \"노드의 접힌 타입 집합에 `object` 또는 `array`가 포함된 `union`인 경우 무조건 `terminal`로 강제한다\"는 규칙 추가.\n3. **REACT-027:**\n   - 기본 fallback 입력이 객체/배열 형태의 union 값을 만났을 때 `[object Object]` 문자열로 변환되지 않도록 하는 렌더링 방어 계약 추가.\n\n### Blueprint / Kind / Input 계층 외 다른 규칙의 변경 필요성 여부\n- **없음.**\n  - `interpret` 및 값 수명주기, 이벤트(`UpdateValue`), 트랜잭션 커밋, 델타 계산, 렌더 계층 바인딩 등은 **이미 모든 터미널(Terminal) 노드에 대해 동일한 인터페이스**로 추상화되어 있습니다.\n  - 코어 아키텍처 관점에서 이 노드는 \"허용하는 값의 도메인이 넓어진 단일 잎 노드\"에 불과하므로 외부 계층의 추가 변경은 전혀 발생하지 않습니다.\n\n---\n\n## 3. 절대 허용해서는 안 되는 단 한 가지와 그 이유\n\n> **절대 불허해야 하는 것: \"`object`나 `array`가 포함된 union 노드가 자식 노드 트리를 갖는 '브랜치(Branch)' 노드가 되는 것\"**\n\n### 이유\n1. **동적 형상 파괴 (Dynamic Structural Collapse):**\n   - 만약 `{type: ['object', 'string'], properties: { a: { type: 'string' } }}`에 대해 자식 노드 `/a`를 생성하도록 허용한다면, 사용자가 문자열을 입력했을 때 자식 노드 `/a`의 값은 무엇이 되어야 하는가? \n   - 사용자가 다시 객체를 입력했을 때 자식 노드를 부활시킬 것인가?\n2. **코어 불변식 위반:**\n   - Canard의 대원칙은 \"폼 트리의 형상은 런타임 데이터가 아니라 청사진이 정적으로 결정한다(BLUEPRINT-042)\"입니다.\n   - 런타임 값의 타입에 따라 브랜치와 터미널을 동적으로 오가거나 자식 노드가 유령처럼 붙었다 떨어지는 구조는 상태 머신의 정합성, 메모이제이션, 경로 캐시, 렌더 바인딩을 근본적으로 파괴합니다.\n\n따라서 **\"객체/배열이 섞인 union은 무조건 단일 전체 값(Whole-value)을 다루는 터미널 잎 노드로만 존재한다\"**는 제약만이 옵션 (c)를 안전하게 성립시키는 유일한 방파제입니다.","type":"message","recipient":"team-lead","content":"Same antigravity conversation continued (session_…"}

---

{"to":"team-lead","summary":"Antigravity turn 3 — Q2 option (c) with 판정/근거/실패장면/대안","message":"Same antigravity conversation continued (session_id 13e658f1-abd0-4701-afb8-7bfdd2c92bd1, turn 3). No error, no repository file edited. Reply (verbatim):\n\n# Q2 심화 검토 — 옵션 (c) 'allow now, terminal only'의 구체적 명세\n\n---\n\n### [판정]\n**조건부 채택 (Qualified Acceptance).**  \n소유자의 의도대로 `object|string|number` 및 `array|string`을 **단일 터미널(전체 값) 잎 노드(Terminal Leaf Node)**로 즉시 허용하는 설계는 코어 트리의 불변성을 깨지 않고 완결성 있게 성립합니다.  \n단, **\"해당 슬롯이 브랜치(자식 노드를 가진 노드)가 되는 경로를 100% 차단하고, 순수 잎 노드로만 동작한다\"**는 강제 규칙이 전제되어야 합니다.\n\n---\n\n### [근거]\n\n1. **청사진 규칙 (Blueprint Rules):**\n   - **접힌 집합(Folded Set) 및 노드 종류 결정:**\n     - 선언된 `type` 배열에서 `null`을 제거하여 노드의 `nullable: true` 플래그로 격리합니다.\n     - `integer`는 `number`로 접습니다.\n     - 접힌 집합 $T \\subseteq \\{\\text{'string'}, \\text{'number'}, \\text{'boolean'}, \\text{'object'}, \\text{'array'}\\}$의 원소 수가 2개 이상이면 노드 종류는 **`union`**이 됩니다.\n   - **강제 터미널 전략 (Forced Terminal):**\n     - 집합 $T$에 `'object'` 또는 `'array'`가 포함된 경우, NODE-028/042의 판정 순서(`options.terminal` → 렌더 판정 → `type`)와 무관하게 **무조건 `strategy: 'terminal'`로 강제**됩니다.\n   - **슬롯에 작성된 `properties` / `items`의 취급:**\n     - 청사진은 이 슬롯 아래로 자식 노드를 전혀 빌드하지 않습니다(`children: {}`).\n     - 작성된 `properties`와 `items`는 코어 트리 형상 결정에서 **완전 무시**되며, 오직 유효 스키마(`node.jsonSchema`)에만 보존되어 ajv 검증기 플러그인 컴파일용으로 전달됩니다.\n   - **`options.terminal: false` 및 인라인 `FormTypeInput`과의 상호작용:**\n     - 작성자가 명시적으로 `options: { terminal: false }`를 지정하더라도, 다중 복합 타입 union 슬롯에서는 **청사진 오류(`SCHEMA_ERROR.CANNOT_BRANCH_UNION`)**로 거부합니다 (런타임 값에 따라 브랜치와 터미널을 오갈 수 없기 때문).\n     - 인라인 `presentation.FormTypeInput`이 제공되면 해당 커스텀 컴포넌트가 바인딩되며, 없을 경우 패키지 기본 입력으로 폴백됩니다.\n\n2. **해석 및 규칙 A (Rule A on Object/Array Union):**\n   - **멤버십 검사:**\n     - `object`: `typeof v === 'object' && v !== null && !Array.isArray(v)`\n     - `array`: `Array.isArray(v)`\n     - 원시 타입: 기존 JS 원시 타입 판정.\n     - 값이 집합 $T$ 내의 한 타입에 속하면 **원형 그대로 유지**합니다.\n   - **변환 규칙 (No Coercion for Complex Types):**\n     - `object`와 `array`는 WRITE-075 변환표에 변환기가 존재하지 않습니다(JSON 파싱/스트링화 금지).\n     - 변환은 오직 목록 내의 **원시 타입 후보군**에 대해서만 시도합니다.\n     - 예: `type: ['object', 'number']`에 `\"123\"`이 입력되면 → `number` 변환만 성공($|S|=1$)하여 `123`이 됨. `\"{}\"`가 입력되면 변환 실패($|S|=0$)로 원형 유지 및 `valueTypeMismatch: true`.\n\n3. **기본 폴백 입력 (Default Fallback Input)과 `[object Object]` 방어:**\n   - 기본 입력(`FormTypeInputString`)은 단순 텍스트 입력창이므로 객체/배열이 주입될 경우 브라우저 렌더러가 `value`를 문자열로 캐스팅하여 `[object Object]`를 노출하는 결함이 발생합니다.\n   - **방어 명세:**\n     - `FormTypeInputString`은 `typeof value === 'object' && value !== null`인 경우, `value` prop에 직접 객체를 넘기지 않고 빈 문자열(`\"\"`) 또는 비활성화 상태의 플레이스홀더(`[Complex Value]`)로 방어 렌더링합니다.\n     - REACT-027 계약에 따라 편집 불가능한 비-문자열 상태에서는 편집을 차단하거나, 리셋 버튼을 통해 `undefined`로 비울 수 있게 합니다.\n\n4. **공용 인터페이스 상호작용:**\n   - **`find('/path')`:** 이 노드는 완전한 터미널 노드이므로, 하위 경로(예: `/slot/name`)로의 `find()` 탐색은 **항상 `undefined`를 반환**합니다 (단일 터미널 객체 노드와 동일).\n   - **`computed` 수식 및 게이트 판정:** 표현식(`controls.value`, `controls.active` 등)이 이 노드를 읽을 때는 노드의 방출 값(`node.value`) 전체를 단일 참조로 읽습니다. 객체 프로퍼티 접근(`val?.name`)은 표현식 런타임에서 안전하게 평가됩니다.\n   - **`omitEmpty`:** 터미널 잎 노드의 규칙을 따릅니다. 값이 `{}`(빈 객체)이거나 `[]`(빈 배열)인 경우, 폼의 `omitEmpty` 설정에 따라 최상위 방출 값에서 생략될지 유지될지가 일관되게 결정됩니다.\n   - **`default` 처리:** 스키마의 `default` 값이 객체/배열인 경우 멤버십 검사(1단계)에서 즉시 통과하므로 원형 그대로 초기값으로 주입됩니다.\n   - **`InferValueType`:** TypeScript 타입 추출 시 정확한 유니언으로 매핑됩니다:\n     `type: ['object', 'string']` → `Record<string, unknown> | string | (nullable ? null : undefined)`.\n   - **이주 (Migration):** 기존 `extractSchemaInfo.ts`에서 `UNKNOWN_JSON_SCHEMA` 에러를 던지며 서지 않던 폼이, 이제 정상적으로 **터미널 union 노드**로 마운트됩니다.\n\n5. **영향받는 원장(Ledger) 영역:**\n   - **BLUEPRINT-032 / BLUEPRINT-034:** \"원시 타입만의 다중 배열\" 문구를 \"원시 및 복합형을 포함한 다중 배열은 `union` 노드가 된다\"로 개정, 18C-02 (5)번의 오류 규정 철회.\n   - **NODE-028 / NODE-042:** 터미널 결정 우선순위에 \"다중 타입 집합에 `object` 또는 `array`가 포함된 경우 무조건 `terminal`로 강제한다\"는 규칙 추가.\n   - **REACT-027:** 기본 문자열 입력의 비-원시값 렌더링 방어 계약 명시.\n   - *결론:* **Blueprint / Kind / Input 계층 외의 다른 계층(트랜잭션, 이벤트, 렌더링 라이프사이클 등) 규칙은 변경할 필요가 전혀 없습니다.**\n\n---\n\n### [실패 장면]\n\n작성자가 `type: ['object', 'string']`으로 선언하고 아래에 `properties: { foo: { type: 'string' } }`를 함께 적은 뒤, `options: { terminal: false }`를 지정하여 하위 필드 `/foo`가 별도 입력 필드로 렌더링되기를 기대하는 경우:\n- **실패:** 폼 마운트 시 청사진 단계에서 `SCHEMA_ERROR.CANNOT_BRANCH_UNION` 에러가 발생하며 폼이 서지 않음.\n- **원인:** 코어는 동적 브랜칭을 불허하므로, 복합 union 슬롯에 브랜치 전략을 강제하려는 시도를 사전에 차단함.\n\n---\n\n### [절대 허용해서는 안 되는 단 한 가지와 이유]\n\n> **절대 불허할 항목:**  \n> **\"`object`나 `array`가 포함된 union 노드가 런타임 값이나 스키마 선언에 따라 자식 노드 트리를 형성하는 '브랜치(Branch)' 노드가 되는 것\"**\n\n- **이유 (Failure to Maintain Invariants):**\n  1. **정적 청사진 원칙(BLUEPRINT-042) 붕괴:** Canard의 가장 중요한 설계 가치는 \"트리의 형상은 런타임 데이터에 좌우되지 않고 청사진이 정적으로 확정한다\"입니다. 브랜치를 허용하면 현재 값이 문자열일 때와 객체일 때 트리의 자식 노드들이 생겼다 사라지는 동적 형상 전환이 일어나며, 이는 메모이제이션 캐시, 경로 참조, `find()`의 일관성을 근본적으로 파괴합니다.\n  2. **하위 상태의 고아화(Orphan State):** 객체 상태에서 하위 `/foo` 필드에 값을 입력했다가 상위 노드에 문자열을 덮어쓴 뒤 다시 객체로 돌아왔을 때, 하위 트리의 이전 입력 상태와 유효성 에러를 복원할 것인지 폐기할 것인지에 대한 복잡도가 기하급수적으로 폭증합니다.\n- **대안:**  \n  복합 브랜치 형상이 필요하다면 반드시 업계 표준대로 **명시적 variant 호스트(`oneOf`/`anyOf` + `controls.discriminator`)나 `if/then/else`**를 사용해야 하며, `type` 배열을 통한 복합 union은 오직 **단일 위젯이 전체 값을 책임지는 터미널 잎(Terminal Leaf)**으로만 엄격히 제한해야 합니다.","type":"message","recipient":"team-lead","content":"Same antigravity conversation continued (session_…"}