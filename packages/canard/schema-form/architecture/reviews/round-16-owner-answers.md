# 16라운드 — Vincent의 답

2026-09-24. `round-16-owner-review.md`의 확인 열에 대한 답이다. 답은 원문 그대로 옮겼고, 오른쪽은 편집자의 반영 방향이다.

| 확인 | 물음(요지) | 답(원문) | 반영 |
| --- | --- | --- | --- |
| 1 | e2e는 `jsdom`에서 돈다 | "예" | 확정 |
| 2 | `reset`은 로드다 | "기본적으로 값에 대한 리셋이긴 한데요, 기존 사용방식을 참고해서 로드로 충분한지 검토해보세요. 불필요한 캐시 리빌드를 원하진 않습니다만, 사용자가 key를 사용한 리셋보다 효율적이고 안전한 방법을 얻길 바라긴 합니다" | 16라운드 스웜 수렴(편집자 결정)으로 닫음(09 §2.6). 로드로 충분하다. 같은 스키마(같은 객체, 또는 JSON 부분이 키 순서까지 깊게 같고 함수·컴포넌트 칸이 참조로 같음)면 로드이고 캐시를 다시 만들지 않는다. 스키마가 실제로 바뀐 경우만 reset 호출 안에서 새로 만든다. `key`보다 안전한 쪽: 값·상호작용·오류 표시는 막 마운트한 폼과 같고, 대체된 입력의 늦은 쓰기를 버리며, 같은 처리기의 prop 갱신도 반영한다. 되묻는 것은 없다 |
| 3 | 배달 경로 제안 | "예" | 확정(제안 → 결정) |
| 4 | 옛 스토리 49파일의 처분 | "예. 전체 정리 허용합니다" | 확정. 옛 스토리는 전부 정리할 수 있다 |
| 5 | React 18 계속 지원 | "예" | 확정 |
| 6 | 벤치 게이트의 문장 | "예, 대표적으로 if-then 을 사용한 무제한 스카마 확장 문법은 더 느릴수밖에 없을겁니다. 대신 통제 가능하고 일정 수준 내에서만 느리길 바랍니다" | 확정. 옛 판보다 느린 것(대표적인 보기는 `if`-`then`)은 통제 가능하고 일정 수준 안이어야 한다. '통제 가능'의 뜻은 편집자 도출(09 §6.1), '일정 수준'의 형태와 수치는 ADR 0009의 미결 |
| 7 | 렌더 시나리오 17파일의 단언 유지 | "예" | 확정. 08 §18 넷째의 예외 |
| 8 | 시나리오 모듈은 비공개 패키지 | "예 적절한 이름을 써주세요" | 확정. 이름은 편집자가 형제 이름을 따라 정한다 |
| 9 | 릴리스 테스트는 `@aileron/production-testbed` | "정해지지 않았습니다. changeSet 을 사용한 표준 방법으로 바꾸고자 합니다. 릴리즈 테스트도 다시 작성해야 합니다. 지금 구조는 github actions를 보세요" | 16라운드 스웜 수렴(편집자 결정)으로 닫음(09 §6.2). changesets 표준 방식으로 바꾸고(`fixed` 무리 여덟, 배포는 오늘의 스크립트와 `changeset tag`, `publish-npm-packages.yml` 한 파일, 기본 토큰, 판 올림 PR 병합 때 자동 배포, 배포 직전 시험 관문), 릴리스 테스트는 포장된 산출물로 다시 쓴다. 판 번호는 아래 추가 확인의 답으로 1.0.0-beta 뒤 1.0.0 |
| 10 | 편집자 결정 여섯 | "에. 최소생성, 메모리안정, 캐싱을 통한 속도 및 재생성 방지가 고속성 원칙에 포함됩니다." | 확정. 고속성의 뜻을 넓혀 08 §1에 적는다(원장에는 가치 표가 없다) |

덧붙인 말(원문): "최초부터 이 form의 목적은 모바일에서도 실행가능한 수준의 안정성과 경제성이라서요"

이어진 말(원문): "음? 다 정책판단 문제는 아닌 것 같은데? 에이전트 스웜으로 수렴시켜보겠니?" — 답 2·9의 결정 점을 가치·런타임·릴리스 세 도출과 반박으로 수렴시켰다(16라운드 스웜 수렴(편집자 결정), 09 §2.6·§6.2). 기준 셋을 모두 채워 소유자에게 남은 정책 질문은 PR-8의 판 번호 하나였다.

추가 확인 — PR-8의 판 번호(1.0.0 / 1.0.0-beta 뒤 1.0.0 / 0.17.0): "(나) 1.0.0-beta를 먼저 내고 1.0.0 예상합니다" → 확정. 반영: 09 §6.2의 열넷째와 §8의 열째, 08 §14 머리와 §17, HANDOFF, README.
