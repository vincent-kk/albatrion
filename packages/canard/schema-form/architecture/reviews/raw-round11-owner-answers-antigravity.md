# 11라운드 원문 — 소유자 답 정합성 검증 (antigravity)

2026-09-23. 소유자의 요청("나의 응답을 다시 정합성 검증을 돌려. 나는 나 자신조차 정합성을 깨는걸 원치 않는다")으로 돌린 교차검증의 원문이다. 제공자 antigravity, 세션 `7d302cee-012a-4b63-8f61-4c1b21724dcb`, 호출 3회(둘째 호출은 세션 오류로 실패, 셋째로 재시도). 최초 응답에서 세 쌍(E-13↔D-14, A-3↔B-22↔P1′, 고지 의무 그룹)이 빠져 같은 형식으로 보강을 요청했고 발견 9–11로 더해졌다. 편집자는 고치지 않았다. 판정과 반영은 `round-11-owner-answers-check.md`에 있다.

---

### 1. 총평

소유자(Vincent)께서 26개 항목에 남기신 답변은 전반적으로 '폼 코어의 해석 최소화(원리 P1)'와 '예약 층을 통한 명시적 제어(원리 P2, 소유자 축 6항)'라는 핵심 설계 축을 확고하게 지탱하고 있습니다. [확인]
그러나 브랜치 비활성화 시점의 값 제어를 요구한 답변(C-15)은 '형상 변화는 값 쓰기가 아니다'라는 대원칙(P4, 소유자 축 4항)과 정면으로 충돌하며, 루트 스키마 우선권(B-12) 및 에지 전이(E-21) 등 일부 답변에서 질문의 기술적 함의와 소유자의 의도가 어긋나 있거나 모호한 지점이 확인됩니다. [확인]
따라서 소유자께서 본인의 설계 원칙을 스스로 훼손하지 않기 위해서는, 충돌이 발생하는 5개 핵심 쟁점에 대해 본 보고서의 선택지를 바탕으로 최종 정리를 내리셔야 합니다. [추정]

---

### 2. 발견 표 (심각도 순)

| 번호 | 심각도 | 관련 답 | 무엇이 어긋나는가 (원문 인용) | 왜 | 닫는 방법 (선택지 2–3개, 원리에서 도출 가능하면 그 답) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **발견 1** | **치명적 (Critical)** | C-15 ↔ C-11 ↔ A-1 ↔ 소유자 축 4항 · P4 | C-15: *"값을 빼거나, 넣는 값도 일부 있긴 해야합니다. 예컨대 A 브랜치가 비활성화 될때, A의 값을 제거할지 남겨둘지, 혹은 변경할지, 그리고 input을 초기화할지 등을 제어해야 해서요. 좀 복잡해져도 거의 풀펑션을 지원하길 바랍니다."*<br>↔ C-11: *"네. 따로 두지 않습니다."*<br>↔ A-1: *"예상이 맞다."* (다시 채우지 않음)<br>↔ 축 4항: *"JSON Schema 설정에 의한 폼 변환과 필드 노출 변환에서 값을 조작하지 않는다."* [확인] | 브랜치가 꺼질 때 값을 삭제·변경·초기화하겠다는 요구는 '비활성화는 쓰기가 아니며 원본은 보존된다(P4, 축 4항)'는 원리 원장을 정면으로 파괴합니다. 또한 브랜치가 다시 켜질 때 지워진 값을 다시 채우지 않는 규칙(A-1)과 결합하면 데이터 복구가 불가능해지며, 부모의 `&children` 문법과 분기 조각의 라이프사이클 훅이 뒤엉키게 됩니다. [추정] | **선택지 1 (원리 도출 - 권고):** P4 및 축 4항을 엄격히 고수하여 브랜치 비활성화 시 원본은 절대 건드리지 않고 방출(`emit`)에서만 제외합니다. 값 삭제는 노드 자신의 `&clearValue` 표현식으로만 제어하며, `&children`에는 상태 키(`active`, `visible`, `readOnly`, `disabled`)만 허용합니다.<br>**선택지 2:** `&children`과 별개로 분기 조각 전용의 명시적 비활성화 라이프사이클 정책(예: `&onInactive: 'clear' \| 'keep'`)을 신설하되, 재활성화 시 채움 규칙의 예외를 설계 원장에 새로 규정합니다. |
| **발견 2** | **높음 (High)** | B-12 ↔ E-13 | B-12: *"이건 '읽기전용 form 인 경우' 등을 구현하기 위한 구조라서 props 로 전달되는 글로벌 값이 개별 값을 덮도록 하는게 맞습니다. rootJSONSchema 도 props 와 동치이므로 그렇게 읽히지 않을까 싶군요"*<br>↔ E-13: *"form 세부 정보와 무관하게 모두 잠궈버려야 하는 조건이 있어서 그렇습니다. (그게 아니면 제어가 어렵습니다. 예컨대 readOnly 상태로 값을 보기만 하는 뷰일때) 그래서 저는 글로벌과 로컬만 보고, 중간단계 상태 상속은 구현을 하지 않으려고 합니다"* [확인] | 소유자께서는 '읽기 전용 뷰에서 전역으로 잠그는(`true`) 상황'을 염두에 두고 답변하셨으나, "루트가 덮는다"를 액면 그대로 적용하면 루트 스키마에 기본 명시된 `readOnly: false`가 하위 개별 노드의 `&readOnly` 식을 덮어써서 하위 노드의 잠금이 강제로 풀리는 치명적 결함(B-12 질문 배경)이 그대로 부활합니다. [확인] | **선택지 1 (원리 도출 - 권고):** 결합 규칙을 논리합(OR)으로 확정하여 `readOnly = Form.readOnly \|\| root.readOnly \|\| node.readOnly`로 처리합니다. 전역 잠금은 완벽히 보장되며, 루트가 `false`여도 하위의 잠금을 무력화하지 않습니다.<br>**선택지 2:** 글로벌 덮어쓰기를 비대칭으로 정의하여, 글로벌 값이 `true`일 때만 하위를 강제 잠금하고 `false`일 때는 하위의 잠금 평가를 존중합니다.<br>**선택지 3:** 소유자 원문대로 루트 `readOnly: false`가 하위 잠금을 무조건 해제하도록 유지하고, 하위 제어 불능은 스키마 작성자의 책임으로 귀속시킵니다. |
| **발견 3** | **높음 (High)** | E-21 (되물음) ↔ A-1 ↔ P2 | E-21: *"최초 로드시에입니다. 런타임에는 false->true, true->false 경계에서만 동작하는걸 생각합니다."*<br>↔ A-1: *"예상이 맞다."* (다시 채우지 않음)<br>↔ P2: *"원본은 호출자와 작성자만 쓴다"* [확인] | `&clearValue`는 단방향 삭제 연산자입니다. `false -> true` 전이 시 값을 지우는 것은 명확하나, `true -> false` 경계에서 무엇이 동작한다는 것인지 불명확합니다. 만약 조건이 거짓이 될 때 값을 되살리거나 초기화하는 동작을 의도하신 것이라면, 이는 "한 번 지운 값은 다시 채우지 않는다(A-1)" 및 "코어의 임의 값 조작 금지(P2)"와 정면 충돌합니다. [추정] | **선택지 1 (원리 도출 - 권고):** 소유자의 원문을 "상태가 변화하는 에지 시점에만 평가한다"는 취지로 해석하여, `false -> true` 시에만 값을 지우고 `true -> false` 전이 시에는 아무 작업도 하지 않는 것(no-op)으로 명문화합니다.<br>**선택지 2:** 조건이 거짓으로 돌아갈 때 이전 값 복원이나 기본값 재채움이 필요하다면, 이는 `&clearValue`의 소관이 아니므로 양방향 제어를 위한 별도의 예약 키를 설계해야 함을 인정합니다. |
| **발견 4** | **높음 (High)** | B-1 ↔ A-4 | B-1: *"권고를 따릅니다만, 기본적으론 form 을 터트려서(error 를 throw 해서) 알려주는게 좋지 않을까 싶네요... 이 루프가 만들어지는건 기본적으로 schema를 잘못 짰기 때문이니까..."*<br>↔ A-4: *"루프의 가능성을 제한하지는 않는다. 이때문에 순환된 값이 진동하거나 발산할 수 있으며, 이를 막기 위한 상한값이 있고, 그 상한값을 초과하면 적절한 error 를 표시한다. 이는 react 의 hook 과 동일한 설계를 갖는다."* [확인] | B-1 질문의 핵심은 '프로덕션에서 검증기를 통과한 값의 제출을 차단할 것인가'였고 권고는 '신호만 두고 제출은 허용'이었습니다. 소유자께서 "권고를 따른다"고 하면서 "기본적으론 throw해서 터트리는 게 좋겠다"고 덧붙이신 것은, 런타임 크래시를 통한 제출 차단과 권고 수용(제출 허용) 사이에서 상반된 지시를 내리신 것입니다. [확인] | **선택지 1 (편집자 절충안 - 권고):** 개발 모드에서는 상한 초과 시 최외곽 진입 끝에서 throw하여 스키마 결함을 즉시 알리고, 프로덕션에서는 원본 B를 커밋하며 `diagnostics` 신호만 발생시키고 제출은 차단하지 않습니다.<br>**선택지 2:** 개발 및 프로덕션 환경을 가리지 않고 루프 상한 초과 시 예외를 던져 폼 동작 및 제출을 즉시 중단시킵니다.<br>**선택지 3:** 폼 코어는 절대 예외를 던지지 않고 오직 `diagnostics` 상태만 갱신하며, 중단 및 제출 차단 여부는 호스트 애플리케이션의 에러 바운더리와 이벤트 리스너에 위임합니다. |
| **발견 5** | **중간 (Medium)** | B-22 ↔ P1′ · 축 1항 | B-22: *"동의합니다. 이 경우에 대한 예외적 허용을 하죠. 이를 통해 스키마를 통해서 form을 제어하는 방법은 if-then-else 와 oneOf / anyOf 의 const 와 enum 이 됩니다. 그럼 현행과 같은거같긴 하군요. 다만... 우리는 '&active': 을 더하는거지 스키마를 수정하는건 아니니까"*<br>↔ P1′: *"폼은 oneOf·anyOf로 분기를 고르지 않는다."*<br>↔ 축 1항: *"enum·const를 판별식으로 쓰는 특수 문법은 쓰지 않는다."* [확인] | B-22 질문의 제안은 작성자가 union 호스트에 `&discriminator: 'kind'`를 **명시했을 때만** 청사진 단계에서 `&active`로 변환해 주는 설탕 문법이었습니다. 소유자께서 "그럼 현행과 같은 거 같긴 하군요"라고 하신 것은, 현행처럼 스키마만 보고 판별식을 자동 감지하던 과거 방식으로 회귀하는 것으로 오인하셨을 위험이 있습니다. [추정] | **선택지 1 (원리 도출 - 권고):** 현행과 달리 `&discriminator`가 명시되지 않은 순수 `oneOf/anyOf`는 모든 분기가 동시에 켜진다는 점을 재확인하고, 명시 선언 기반의 청사진 설탕 문법으로 확정합니다.<br>**선택지 2:** 소유자의 인지대로 OpenAPI나 생성기 스키마 호환을 위해, 각 분기에 `const/enum` 태그가 완벽히 존재할 경우 `&discriminator` 선언 없이도 코어가 판별식을 자동 인식하도록 허용합니다 (단, P1′과 축 1항 수정 필요). |
| **발견 6** | **중간 (Medium)** | A-2 ↔ E-19 (되물음) ↔ E-23 (되물음) | A-2: *"...잘못된 스키마에 대한 책임은 사용자에게 있고, form은 고지 의무만 진다."*<br>↔ E-23: *"이건 우리가 참견할 문제는 아닙니다. 평가하지 않습니다."*<br>↔ E-19: *"23에서 말한대로 굳이 안봐도 되지않나 싶습니다... 우리는 그걸 관여하기로 하면 끝이 없을거에요"* [확인] | A-2에서는 잘못된 스키마에 대해 폼이 '고지 의무(경고)'를 진다고 선언하셨으나, E-19와 E-23에서는 `if` 내부 스키마 평가를 일체 거부하셨습니다. 이로 인해 편집자가 도출로 유지하려던 `else: false` 누락 경고와 소유자의 '불간섭 원칙' 사이에 충돌이 발생합니다. [확인] | **선택지 1 (소유자 원칙 고수 - 권고):** 폼 코어는 `if` 내부를 일절 들여다보지 않으며, `else: false` 누락 검사도 폼에서 완전히 제외하고 순수하게 ADR 0010 컨벤션 문서로만 안내합니다.<br>**선택지 2:** `else: false` 누락은 분기 평가 실패를 초래하는 치명적 패턴이므로, 폼의 고지 의무(A-2) 차원에서 개발 모드 경고를 예외적으로 유지합니다. |
| **발견 7** | **중간 (Medium)** | E-18 ↔ D-7 · E-16 | E-18: *"깊은 병합을 했으면 합니다. 그 이유는 커스텀 props는 (FormTypeInputProps 같은) 내부에 객체타입을 가질 수도 있고 함수나 핸들러를 가질 수도 있어서... 이때는 common-utils 의 merge 를 쓰죠"*<br>↔ D-7: *"네, 세부적인 규칙이 포괄적인 규칙을 덮는 기존 관례를 따릅니다"*<br>↔ E-16: *"무조건 조각 나중-승 통일: 예"* [확인] | 다른 모든 메타데이터와 주석 키워드는 전순서상 '나중-승(덮어쓰기)'을 따르는데, `options`만 유일하게 깊은 병합(deep merge)을 수행하면 상위 조각에서 정의한 옵션을 하위 조각에서 제거하거나 초기화하기 어려워집니다. 또한 순수 JSON Schema 정의에 함수/핸들러가 들어간다는 설명은 폼 스키마의 직렬화 가능성과 긴장합니다. [추정] | **선택지 1 (원리 도출 - 권고):** `options`도 다른 폼 전용 키처럼 키 단위 얕은 병합(shallow merge)으로 통일하여 예측 가능성을 확보하고, 필요 시 `undefined`를 주어 덮어쓸 수 있도록 합니다.<br>**선택지 2:** `@winglet/common-utils`의 `merge`를 사용하되, 객체 컨테이너만 재귀 병합하고 함수, 핸들러, 원시값은 나중 조각의 값으로 덮어쓰도록 정밀 규정합니다. |
| **발견 8** | **낮음 (Low)** | E-4 ↔ 소유자 축 "UI 제어는 `&`로" | E-4: *"네, virtual 은 스키마로 선언되는건 아니니까 그냥 둡시다. 유효성검증도 영향 없고."*<br>↔ 축 요약: *"UI 제어는 최대한 `&`로 하고... 값 변경도 전량 `&`와 `injectTo`로"*<br>↔ 원리 G4: *"하나의 개념에는 하나의 장치, 특수 경로 없음"* [확인] | 모든 폼 전용 제어 키워드를 `&` 네임스페이스로 통합하기로 한 축의 대원칙에서 `virtual`만 유일하게 접두사 없이 표준 키워드 자리에 평문으로 잔존하며, 검증기 전달 전 수동 제거라는 특수 경로(G4 위반)를 유지합니다. [확인] | **선택지 1 (원칙적 통일 - 권고):** `virtual`도 `&virtual: true` 또는 `control.virtual`로 전환하고, 기존 키워드는 이주 안내를 거쳐 폐기합니다.<br>**선택지 2 (현행 수용):** 소유자의 결정대로 `virtual`은 코드 변경 없이 레거시 예외 장치로 영구 유지합니다. |
| **발견 9** | **중간 (Medium)** | E-13 ↔ D-14 | E-13: *"기본적으로 본인의 readOnly 조건을 글로벌 조건이 이기는 설계입니다. ... 그래서 저는 글로벌과 로컬만 보고, 중간단계 상태 상속은 구현을 하지 않으려고 합니다"*<br>↔ D-14: *"readOnly, visible 같은거에 &를 붙이는건 좀 이상하긴 하니까 control(or controls 적절한거 선택) 블록으로 묶으면 어때요? target 과 control 로 분리하는거죠."* (`&children: [{ targets: [...], control: {...} }]`) [확인] | **[판정: 중간 단계 제어의 우회적 부활]**<br>소유자께서 E-13에서 정의한 "로컬"은 명백히 *"본인의 readOnly 조건(노드 자신의 스키마)"*을 의미합니다. [확인] 반면 D-14에서 승인하신 부모 객체의 `&children`은 자식 노드 입장에서 자신의 선언이 아닌 '중간 부모 노드가 주입하는 외부 제약'입니다. 편집자는 이를 스키마 트리 상속(암묵 전파)이 아닌 명시적 타겟팅이라는 이유로 "로컬 층 안의 결합"에 편입시켰으나, 계층 구조 관점에서는 E-13이 명시적으로 배제하려 했던 **중간 단계 상속·제어가 별도 문법으로 부활한 것**입니다. [추정] 이로 인해 부모 `&children`의 잠금과 자식 로컬의 잠금, 루트 글로벌 간의 위계가 "글로벌 > 로컬"이라는 2층 단순 체계와 논리적 긴장을 유발합니다. [확인] | **선택지 1 (원리 도출 - 권고):** `&children`을 '중간 상속'이 아닌 '자식 노드에 대한 컴파일 타임 로컬 식 주입(설탕 문법)'으로 정의하여, 결합 단계에서는 자식 자신의 로컬 선언과 대등한 로컬 층 제약으로 흡수합니다. 로컬 층 내부 결합은 논리합(OR: 하나라도 잠그면 잠김)을 따르고 글로벌이 이를 덮습니다.<br>**선택지 2:** E-13의 원칙을 절대적으로 적용하여 부모 객체의 `&children` 문법 자체를 폐기하고, 오직 루트 글로벌 설정과 개별 노드 자신의 `&readOnly` 선언만 남겨 완벽한 2층 구조를 만듭니다.<br>**선택지 3:** 중간 단계 제어의 존재를 공식 인정하고, 위계를 "글로벌 > 부모 `&children` > 로컬"의 3층 구조로 명문화합니다. |
| **발견 10** | **높음 (High)** | A-3 ↔ B-22 ↔ P1′ · 소유자 축 1항 | A-3: *"분기를 고르지 않으나, if-then-else 문법은 예외적으로 분기를 결정하는 것에 쓰인다. 내 이해가 맞나? ... 이 조건식에 따라, 'then': {...}의 스키마가 자식노드에게 전달되고, 또한 이들 필드가 노출된다. 즉, &를 제외하고 schema 만으로 form 을 제어하는 방법이 if-then-else 인 것이다."*<br>↔ B-22: *"동의합니다. 이 경우에 대한 예외적 허용을 하죠. 이를 통해 스키마를 통해서 form을 제어하는 방법은 if-then-else 와 oneOf / anyOf 의 const 와 enum 이 됩니다. 그럼 현행과 같은거같긴 하군요. 다만... 우리는 '&active': 을 더하는거지 스키마를 수정하는건 아니니까"*<br>↔ P1′: *"폼은 oneOf·anyOf로 분기를 고르지 않는다."*<br>↔ 축 1항: *"enum·const를 판별식으로 쓰는 특수 문법은 쓰지 않는다."* [확인] | **[판정: 서로 다른 예외 사유이며, P1′의 심각한 형해화 초래]**<br>1. **예외 근거의 이질성:** A-3의 `if/then/else`는 표준 JSON Schema 조각 활성화 조건(가드)을 검증기 플러그인에 위임하여 참인 조각만 켜는 **형상 선언의 조건부 노출** 메커니즘이며, 코어가 분기 선택 상태(`selection`)를 갖지 않습니다. 반면 B-22의 `&discriminator`는 pydantic/zod 등 외부 생성기와의 현실적 호환을 위해 작성자의 명시 하에 판별 프로퍼티의 값을 읽어 분기 배타성을 인위적으로 부여하는 **선택 장치의 설탕 문법**이므로, 두 예외의 도입 사유와 동작 원리는 완전히 다릅니다. [확인]<br>2. **P1′ 잔여 평가:** A-3(표준 분기 제어)과 B-22(oneOf 분기 제어)가 모두 예외로 수용되면서, 소유자의 표현대로 *"그럼 현행과 같은 거 같긴 하군요"*라는 결과에 도달합니다. [확인] 실무에서 쓰이는 거의 모든 분기 스키마가 두 예외로 포섭되므로, P1′("폼은 분기를 고르지 않는다")에서 실질적으로 남는 원칙적 방어선은 **'작성자가 `&discriminator`를 선언하지 않은 순수 `oneOf/anyOf`에 한해 폼 코어가 판별식을 암묵적으로 자동 감지하지 않고 모든 분기를 동시에 켠다'**는 소극적 규칙 하나뿐입니다. [추정] | **선택지 1 (원리 도출 - 권고):** P1′의 본질이 '상태 칸(`selection`)의 완전 제거'와 '암묵적 자동 감지 금지'에 있음을 명확히 규정합니다. `if/then/else`는 조각 게이트, `&discriminator`는 정적 청사진 변환(`&active` 합성)으로 한정하여 코어 런타임 루프가 분기를 고르지 않는다는 불변식을 사수합니다.<br>**선택지 2:** P1′ 원칙을 현실에 맞추어 "폼은 명시적 지시가 없는 한 분기를 고르지 않는다"로 완화 개정하고, 외부 생성기 호환성을 공식적인 설계 축으로 승격시킵니다. |
| **발견 11** | **중간 (Medium)** | C-20 ↔ A-2 ↔ E-19 (되물음) · E-23 (되물음) | C-20: *"예 이건 jsonSchema 스팩을 존중합니다"* (`oneOf` 다중 활성 경고 승인)<br>↔ A-2: *"...잘못된 스키마에 대한 책임은 사용자에게 있고, form은 고지 의무만 진다."*<br>↔ E-23: *"이건 우리가 참견할 문제는 아닙니다. 평가하지 않습니다."* (`if`의 `required` 누락 검사 거부)<br>↔ E-19: *"23에서 말한대로 굳이 안봐도 되지않나 싶습니다... 우리는 그걸 관여하기로 하면 끝이 없을거에요"* (2항 개발 모드 검사 거부) [확인] | **[판정: C-20은 정합하나, 편집자의 `else: false` 경고 유지가 불간섭 원칙을 위반]**<br>1. **C-20의 정합성:** C-20은 `if` 스키마 내부를 분석·파싱하는 것이 아니라, 검증기 플러그인 평가가 끝난 뒤 **'켜진 분기의 개수(카운트)가 2개 이상인가'**라는 최종 런타임 결과만 확인합니다. 이는 소유자 축 1항(*"`if` 블록은 그 블록이 지금 켜지는지만 본다"*) 내에 완벽히 머물며, A-2의 *"폼은 고지 의무만 진다"*를 스키마 침해 없이 정확히 이행하므로 E-19·E-23의 불간섭 원칙과 완전히 정합합니다. [확인]<br>2. **영역 침범 지점:** 정작 불간섭 원칙을 위반한 것은 편집자가 도출로 유지한 **'`oneOf/anyOf` 분기 내 `if`는 있고 `else: false`가 없음'에 대한 경고**입니다. 스키마 키워드의 존재 여부를 폼 코어가 들여다보는 행위는 E-19·E-23에서 소유자께서 천명하신 *"관여하기로 하면 끝이 없다, 참견할 문제가 아니다"*라는 원칙을 정면으로 벗어납니다. [추정] | **선택지 1 (원리 도출 - 권고):** C-20(다중 켜짐) 경고는 런타임 게이트 카운트 기반의 정당한 고지 의무로 유지하고, `else: false` 누락 검사는 E-19·E-23의 불간섭 원칙에 따라 폼 코어 경고에서 완전히 제거하여 ADR 0010 컨벤션 문서 안내로만 일원화합니다.<br>**선택지 2:** `else: false` 누락 역시 스키마 작성자의 치명적 실수를 방지하는 A-2의 '고지 의무'의 일환으로 예외적 포함을 인정하되, 이는 '`if` 내용 해석'이 아닌 '최상위 키워드 존재 유무 검사'에 국한된다고 한계를 엄격히 긋습니다. |

---

### 3. 검토했으나 정합인 쌍 목록

1. **C-2(`Merge` + `undefined`) ↔ C-11(`onChange`에 `undefined`)**: 호출자의 머지 쓰기이든 렌더러의 입력 변경이든 `undefined`를 전달하면 해당 키를 '없음'으로 처리하여 제거한다는 리프 규칙(ADR 0013)과 정확히 정합합니다. [확인]
2. **D-6(로드 시 `&injectTo`의 `fire`) ↔ E-21(최초 로드 시 `&clearValue` 평가)**: 로드 시점에는 직전 커밋이 없으므로 새로운 수명으로 취급하여, 예약 층의 자동 쓰기 규칙들이 현재 로드된 데이터를 기준으로 즉시 발화한다는 원칙에서 상호 정합합니다. [확인]
3. **D-7(조각 주석 본체 덮음) ↔ E-16(무조건 조각 나중-승 통일) ↔ D-17(켜진 조각 전순서 나중-승)**: 세부 규칙이 포괄 규칙을 덮고 전순서상 나중에 선언된 조각이 앞선 조각의 메타데이터를 덮는다는 '나중-승' 일관성을 완벽히 공유합니다. [확인]
4. **E-8(`&clearValue` 승리) ↔ E-9(`&derived` 승리)**: 파생 단계의 같은 대상 규칙에서 명시적 지움(`&clearValue`)이 파생 계산(`&derived`)을 이기고, 자기 필드의 파생(`&derived`)이 타인의 주입(`&injectTo`)을 이긴다는 동작 위계(`clearValue > derived > injectTo`)를 명확하게 정립했습니다. [확인]
5. **A-2(노드 게이트 통합) ↔ C-10(노드 게이트 통합)**: 조각 게이트와 노드 게이트를 단일 장치(`&active`)의 두 가지 적용 범위로 일원화하여 단일 개념 단일 장치 원칙(G4)을 일관되게 만족합니다. [확인]
6. **E-5(`&pristine` 명칭 변경) ↔ 6.1(`&resetInteraction` 채택)**: `dirty`와 `touched`를 동시에 초기화하는 본래의 역할을 직관적인 동작 명칭으로 개정하여 용어 혼선을 해소했습니다. [확인]

---

### 4. 편집자 반영 오류

1. **B-1 임의 절충 변형**: 소유자께서는 환경 구분 없이 *"기본적으론 form 을 터트려서(error 를 throw 해서) 알려주는게 좋지 않을까 싶네요"*라고 답변하셨으나, 편집자가 `07-conclusions.md` §5.0에서 이를 임의로 나누어 *"확정(신호만 + 개발 모드 throw): 프로덕션은 신호만"*으로 자의적으로 절충하여 반영했습니다. [확인]
2. **C-15 영역 혼선 및 본질 은폐**: 소유자께서 요구하신 *"A 브랜치가 비활성화 될때 값 제거·유지·변경·초기화"*라는 분기 라이프사이클 제어 요구를, 편집자는 단순히 `&children` 컨테이너에 `clearValue, default, resetInteraction` 키워드를 추가 허용하는 문법 문제로 축소하고 미결(11.2)로 넘겼습니다. [확인]
3. **E-21 모호한 원문 임의 단정**: 소유자의 *"런타임에는 false->true, true->false 경계에서만 동작"*이라는 불가해한 표현에 대해, 편집자는 `07-conclusions.md`에는 원문을 그대로 옮겨두었다가 `03-mental-model.md` 원장 89행에서는 소유자 확인 없이 *"참→거짓에서는 아무 일도 하지 않는다(값을 되살리지 않는다)"*로 임의 축소하여 단정했습니다. [확인]
4. **A-4 에지 소비 규칙 자의적 확정**: A-4의 질문 취지(정착 안 에지 1회 소비, 4.30)에 대해 소유자께서 루프 상한 오류(Hook 비유)로 다소 빗나가게 답변하셨음에도, 편집자는 소유자가 4.30을 승인한 것으로 간주하고 문장만 보강하여 통과시켰습니다. [확인]
5. **E-19 · E-23 고지 의무 범위 왜곡**: 소유자께서 `if` 내부 스키마 검사를 일체 거부(*"우리가 참견할 문제 아니다, 관여하면 끝이 없다"*)하셨음에도, 편집자는 `07-conclusions.md` 148행 및 `03-mental-model.md` 165행에 `else: false` 누락 검사를 개발 모드 경고로 여전히 유지했습니다. [확인]
6. **B-12의 false 전파 왜곡**: 소유자께서 읽기 전용 뷰(`true`) 구현을 위해 언급하신 글로벌 우선 원칙을, 편집자가 `07-conclusions.md` 153행에서 *"값이 false여도 덮는다"*라고 극단화하여 루트의 `readOnly: false`가 하위의 모든 명시적 잠금을 해제하는 치명적 부작용을 공식화했습니다. [확인]

---

### 5. 판정

**조건부 통과 (소유자 결정 필요 번호: 발견 1, 발견 2, 발견 3, 발견 4, 발견 5, 발견 9, 발견 10, 발견 11)**

소유자의 26개 답변은 전체 아키텍처의 큰 틀을 성공적으로 정립하였으나, 위 발견 항목들은 원리 원장(03) 및 코어 루프 구현과 정면으로 부딪히는 결함을 내포하고 있습니다. 특히 **발견 1(브랜치 비활성화 시 값 제어)**, **발견 2(루트 스키마의 false 전파 결함)**, **발견 10(A-3·B-22 이중 예외로 인한 P1′ 형해화)**는 구현 착수 전 반드시 소유자의 명확한 양자택일 결정이 선행되어야 합니다. [추정]
