# 18라운드 — 소유자 답

2026-09-25. 18라운드 안건(`reviews/round-18-agenda.md`)에 대한 소유자의 답을 원문 그대로 옮긴다. 답 칸은 소유자의 말이고, 반영 칸은 편집자의 반영 방향이다.

| 번호 | 물음 | 답(원문) | 반영 |
| --- | --- | --- | --- |
| S1 | 다른 종류의 값을 보존할 때 잎 노드의 공개 값 형. ADR 0013 결정 1(받은 값을 고치지 않는다)과 그 표(타입별 파서의 강제 변환은 입력 구성 요소로)를 따르면 로드·`setValue`·자동 쓰기로 수 노드에 `"12"`가 올 때 core가 그대로 두어 공개 형이 거짓이 된다. 수 노드는 수만 받는가, 형을 넓히는가(외부 점검 codex, 안건 §6) | 첫 답: "S1? 이건 무슨소리지? isNumberNode 가 참인거랑 value가 숫자인건 다른 이야기긴 한데. undefined 일 수도, null일 수도(nullable 이면), NaN일 수도 있잖아. 그런 차원의 문제가 아닌건가? 기존에 parse number 처럼 Number Node 에는 기본적으로 숫자만 값으로 받아져야 하긴 하는데?" 둘째 답: "S1. node 는 각자의 타입에 맞는 parse 함수를 가져야 합니다. 지금처럼요. 이건 값의 조작이라기보단, json or JS 의 자동형변환을 통제 가능하게 구현하는 방향이라고 보아도 좋습니다." | 확정. 노드마다 타입에 맞는 parse 함수를 둔다. 오늘의 `src/core/parsers/`(`parseString`, `parseNumber`, `parseBoolean`, `parseArray`, `parseObject`. 파싱할 수 없는 입력에서 throw하지 않는다)를 그대로 쓴다. 파서가 실제로 무엇으로 바꾸는지와 어디까지가 JS·ajv의 형변환과 같은지는 `reviews/raw-round18-s1-parse-check.md`의 실행 검증이 적는다. 그 검증에 따라 반영 범위(대체되는 자리와 오늘 파서 가운데 남길 동작)는 소유자가 다시 정한다. 이것은 값의 교정이 아니라 JSON·JS 자동 형변환을 통제할 수 있게 구현한 것이므로 ADR 0013 결정 1의 대상이 아니다. 공개 값 형은 오늘처럼 그 타입의 값이다(수 노드는 수, 파싱 실패는 `NaN`). 이 답이 대체하는 자리: `adr/0013-core-does-not-rewrite-values.md:88`(타입별 파서의 강제 변환은 입력 컴포넌트로)과 `:89`(파서 강제 변환의 폐기는 유지), `03-mental-model.md:152`(파서 강제 변환은 입력 컴포넌트로), `09-landing-and-test-strategy.md:9`(파서 변환은 교체 대상), `:33`(PR-2의 교체 대상 칸의 `core/parsers/*`는 그대로 쓰는 것으로), `:160`(파서 변환을 단언하는 시험을 버림). 편집자가 닫을 세부: parse는 각 동작 행의 `interpret`(입력 해석) 칸이 부르고, 적용 범위는 '지금처럼'을 따라 노드에 드는 모든 쓰기다. `parsers`의 새 자리는 18라운드의 노드 구조 항목에서 정한다 |
| S1 이어서 | 오늘 파서를 두고 할 일(`reviews/raw-round18-s1-parse-check.md`의 실행 검증 뒤). 가: 오늘 파서를 그대로 둔다(5라운드 원칙의 뒤집힘으로 기록). 나: 뜻이 그대로인 변환만 parse에 남기고(ajv 규칙 수준) 문자 제거·자르기·비우기는 버리거나 명시적 옵션으로 돌린다. 다: 모든 형변환을 값의 수정으로 보고 결정 1을 형이 아닌 제약에만 적용한다 | ""나"로 갑시다. 이게 정확히 내가 원하는 기능이에요. 그 이상을 하는건 나도 원치 않습니다. 다만 흠... 변경하지 못하는 값을 입력했을때, 이전에는 NaN 같은 값을 넣었는데요, 어떻게 하면 좋을까요? 문득 든 생각으론 변환 가능한 값만 변환하고, 안되면 입력값을 그냥 전달해서 need to number 같은 에러를 보여주면 어떨까 했는데, 그럼 최초 문제인 Node 가 자신의 타입에 맞는 값을 소유한다는 보장을 못하게 되니... 파싱 실패시 지금처럼 NaN으로 파싱하게 하는게 좋을까요?" | 확정(나). parse는 뜻이 그대로인 변환만 한다(ajv 규칙 수준). 오늘 파서의 문자 제거, 정수 자르기, 빈 값 치환(`""`, `[]`, `{}`), 불리언의 진릿값 변환은 parse에서 뺀다. ADR 0013 결정 1에는 이름 붙은 예외(형 정규화)로 적는다. 변환하지 못한 입력의 처리는 소유자가 물었고 편집자 권고를 드렸다(답 대기) |
| S1 셋째 | 변환하지 못한 입력의 처리. 세 갈래 독립 검토(Claude 스웜 일곱, codex, antigravity)가 모두 '나′'(받은 그대로 든다)를 권했다(`reviews/raw-round18-s1-unconvertible-review.md`). 물은 것: 나′ 확정, 노드의 정합 상태(경고등)를 두고 공개 형의 판별자로 쓰는가, 변환 불가를 `onError`로 보내는가 | 첫 물음: "흠 그럼, 이건 사실상 형변환 시도를 해보고, 안되면 그대로 둔다. 되면 한다. 이정도인거고, 개별 노드가 자신의 타입에 맞는 Value를 가질 것이라는 것은 보장할 수 없는건가" 둘째: "근데 이게 그럼 좀 복잡해지는게, node.value 는 제어컴포넌트에서 상태로 그대로 쓰인단말이지. 그래서 이걸 바꿔버리면, 제어컴포넌트 동작을 예측하기 어려워질걸?" 셋째: "이거 에이전트 스웜 교차검증을 한번 거쳐봐. 판단이 쉽지 않네. 그러면서도 사용성에 지대한 변화가 일어날 수 있고. 그리고 자칫 네가 말한대로 제어컴포넌트 환경에서 node의 value 가 어긋나는 문제가 있을 수도 있을거같고 말이지. 값을 가지고 있는데 빈 값을 표현하는것도 좀 위험할거같아. 만약 validator 가 연결되지 않은 경우에는? 그 경우에는 진짜 form 에러를 받고(onError로), 값이 비워져야 하는건가? 아무튼 이건 독립검토가 좀 필요하다" 넷째: "흠... 클로드까지 보고 결정하자. 근데 데이터 유지로 기울긴 하는구나. 참고로 plugin 은 그냥 샘플이고, 보통 용법은 사용자가 formTypeInput 을 구현해서 붙이는거긴 해. 만들기도 적용하기도 비교적 쉬우니까. 그럼 결국 올바르지 않은 타입의 표현은 FormTypeInput 구현에 위임되는거구나. 그럼 이건 어때? Node 가 자신 타입에 맞는 값을 제공한다는걸 보장하진 못하지만, 현재 Node 의 값이 "정합한지" 여부는 상태로 둘 수 있을거같아. error 와 별개로, 이 상태를 보고 현재 값의 건전성을 판단하도록 하는건 어떤가? "나"를 수용하면서 경고등을 추가하는 방향인데. 마지막 응답까지 "나"면 이 방향으로 마음이 기울긴 하네" 답: "나 로 확정합니다. 경고등 추가도 승인합니다. 변환에러는 onError 로 전달하죠. 다만, 그럼 한가지, 자동전환시도는 어떻게 되는거죠? "123" -> 123 같이 변환하는거요. 이건 편의기능에 가까울거같은데, 지원하나요?" | 확정(나′, 경고등, `onError` 전달). 변환 목록(ajv `coerceTypes`의 부분집합): 수 노드는 앞뒤 공백을 뺀 전체가 수 표기인 문자열을 유한한 수로 바꾸고(정수 노드는 결과가 안전한 정수일 때만, 이미 수인 값은 자르지 않는다), 문자열 노드는 유한한 수와 불리언을, 불리언 노드는 정확히 `"true"`·`"false"`와 수 1·0을 바꾼다. `null`은 어느 노드에서도 바꾸지 않는다. 이미 자기 타입인 값은 그대로 둔다. 바꾸지 못한 값은 받은 그대로 들고 `value`·방출·제출이 모두 그 값이다(비우기·대체·거부 없음). 노드는 정합 상태(경고등)를 들고, 이것이 공개 형의 판별자다(이름과 모양은 18라운드 §7). 자동 변환(`"123"`을 123으로)은 S1의 parse 자체이며 지금처럼 늘 켜져 있다(끄는 옵션을 두지 않는다). 편집자 결정: `onError` 기록의 level은 `warning`이고(값을 보존하므로 폼의 약속은 지켜진다. error 층은 throw·거부·싱크로 드러나야 해 통보 3과 부딪힌다), 가칭 `SCHEMA_FORM_WARNING.VALUE_TYPE_MISMATCH`를 노드의 정합 상태가 켜질 때마다 한 번 보낸다(path, 기대 형, 받은 값의 종류, 쓰기 출처). 검증기가 있든 없든 보낸다(core의 parse가 찾는 것이라 검증 결과가 아니다). 제출은 검증기가 있으면 검증이 막고 없으면 막지 않는다(통보 3). 후속 세부는 `reviews/round-18-agenda.md` §6의 'S1 결정의 후속 세부' 행 |
| 원장 | 단일 원장 파일럿(오류 영역, 69항목)의 결과와 전체 증류의 형식 확정안(항목 칸, 문장 경계 분할과 `path:line#n` 출처, 번호 재사용 없음, 요약·대조표는 보기, 조각마다 닫은 사람, 검사 여섯에 영역별 검증자와 codex·antigravity 누락 찾기)을 보이고 둘을 물었다. (1) 이 형식으로 확정하는가. (2) 전체 증류를 18라운드 전에 하고, 18라운드부터 결정은 원장에 적는 순서로 가는가(`HANDOFF.md:153`의 제안: 옛 문서는 더 고치지 않는다). | "커밋을 한번 했습니다. 제안한 방법대로 진행하죠. 형식도 수용합니다. 마지막 라운드로 온전한 결과물을 얻었으면 좋겠네요." (2026-09-26) | 형식은 `ledger/README.md`로 고정했다. 순서는 전체 증류 → 18라운드(원장 위에서) → 총검증 → 설계 완료 → ADR·설계문서 → 옛 문서 백업이다. 옛 설계 문서(`00`–`09`, `adr/`, `open-questions.md`)는 이 답부터 고치지 않고, 어긋남은 원장의 충돌 칸에 적는다. 18라운드는 마지막 라운드로 삼아 원장이 온전해질 때까지 닫지 않는다. `HANDOFF.md:155`의 절 단위 통과 제안(08·09 대신 새 설계문서에서)은 이 답이 명시하지 않았으므로 18라운드 소유자 물음으로 남긴다. |
| 12-1 | 자동 변환(`"123"` → `123`)을 끄는 옵션을 두는가(`reviews/round-18-agenda.md:159`). | "자동변환을 따로 끄는 옵션은 생각하지 않습니다만, 필요하다고 한다면 form 단위에서 props 로 추가하도록 하시죠" (2026-09-26) | 끄는 옵션은 두지 않는다. 필요가 생기면 노드나 스키마 단위가 아니라 `<Form>`의 prop 하나로 더한다(설계 예약: 폼 단위, 이름은 그때 정함). WRITE-076이 이 답으로 닫힌다. |
| 12-2 | 포커스 아웃 `trim`이 자른 값의 쓰기가 자동 쓰기인가. 함께, 자른 값이 현재 값과 같으면 쓰지 않는 규칙을 받는가(`reviews/round-18-agenda.md:160`). | "자동 쓰기 아닙니까? 그리고 trim 전후 값이 같으면 쓰지 않아도 됩니다. 효율적이게." (2026-09-26) | 자른 값의 쓰기는 core의 **자동 쓰기**다(R17-3 선택지 "다"의 원문 "자동 쓰기가 여섯이 됨"이 맞고, 17라운드 반영 칸의 "자동 쓰기가 아니다"는 편집자의 오독이었다). 따라서 자동 쓰기는 여섯이고 억제 비트(`DisableAutomaticWrites`)의 대상이며, 억제를 켠 폼에서는 포커스 아웃 때 자르지 않는다. 자른 값이 현재 값과 같으면 쓰지 않는다(소유자 확정). ADR 0013:49·:89, `08-design-a-to-z.md:130`, ADR 0003:32의 "자동 쓰기가 아니다"는 이 답으로 대체된다. WRITE-009·WRITE-007·CONTROLS-007이 이 답으로 닫힌다. |
| 12-3 | 인터프리터형 검증기의 지원 수준과 컴파일 예산(`reviews/round-18-agenda.md:161`, §5 :67). 가: AJV 기준 예산만 보장. 나: 역색인으로 격차를 줄임. | "검증기의 성능은 우리가 관여할 문제가 아닙니다만. 뭘 말하는건지요?" (2026-09-26, 미리보기 댓글) | 가로 읽는다. 검증기의 성능은 폼이 관여할 문제가 아니므로 폼은 아무 장치도 더하지 않는다(역색인 없음, `if` 내용 불관여 유지). 예산은 AJV 기준으로 적고, 인터프리터형은 "동작하되 성능은 플러그인의 몫"으로 문서화한다. VALIDATE-027이 이 답으로 닫히고 VALIDATE-032·FRAGMENT-023은 그대로다. |
| 12-4 | 방언 선언과 개발 모드 경고 제안을 받는가(`reviews/round-18-agenda.md:162`). | "경고정도는 주도록 합시다" (2026-09-26) | 받는다. 플러그인이 자기 방언을 선택적으로 선언하고, 스키마의 `$schema`와 어긋나면 개발 모드에서 경고만 낸다(프로덕션 출력 없음, `onError` 핸들러가 있으면 경고 기록). VALIDATE-026이 현행이 된다. 경고 코드는 ADR 0014 §7.2 목록에 더한다(가칭, 편집자). |
| 12-5 | `UpdateValue` 통지에 출처(`source`) 칸을 두지 않는 것으로 확정하는가(`reviews/round-18-agenda.md:163`). | "source 를 저장하는건 문제 없습니다. 다만, 사용자가 이걸 쓸 일이 있을지는 모르겠군요. 오히려 dirved 가 발화된 경우, input에 다른 표시를 하거나 하는 등에는 쓸 수 있겠습니다만. 아무튼 이 통지에 옵션을 추가하는건 문제 없습니다" (2026-09-26) | 나. 출처 칸을 둔다. 이름과 값 목록은 편집자가 정한다(통지는 사실만 싣는다: 쓰기 종류의 출처 — 입력·자동 쓰기(derived·injectTo·default 채움·trim)·로드). 소유자가 든 쓰임은 derived가 발화한 입력을 다르게 표시하는 것이다. EVENT-025의 부정 결정은 대체된다. |
| 12-6 | 절 단위 통과를 08·09 대신 새 설계문서에서 하는가(`reviews/round-18-agenda.md:164`, `HANDOFF.md:155`). | "두번 읽지 않습니다" (2026-09-26) | 가. 절 단위 통과는 새 설계문서에서만 한다. 08·09는 통과 절차 없이 백업으로 간다. PROCESS-062가 현행, PROCESS-053은 대체됨이 된다. |
| 12-7 | 변환하지 못한 입력의 `onError` 기록 level(`reviews/round-18-agenda.md:165`). | "형변환 실패로 문제가 생기는 경우에 대한 대응은 FormType 이 하기로 했잖아. 그래서 개발단계에서는 중요한데, 리얼부터는 어쩔 수 없다고 생각하긴 해. warning 이면 되지않을까?" (2026-09-26) | 가. level은 `warning`이다(개발 모드 콘솔, 프로덕션은 핸들러가 있을 때만). 까닭은 소유자의 말대로 틀린 형의 대응이 입력 구성 요소(FormTypeInput)의 몫이라 개발 단계에서 중요하고 프로덕션에서는 어쩔 수 없다는 것이다. ERROR-186·LANDING-126이 현행이 된다. |
| 12-8 | 값 읽기의 공개 이름(`node.value` 합성 값·투영 전, `node.outputValue` 방출 값, `getInactiveValues(path)`)을 확정하는가(`reviews/round-18-agenda.md:166`). | "이대로 가도 되는데요, value 랑 outputValue 가 다르면, 투영할때만 바뀌는 경우(빠지는 값?)은 어떤게 있죠? 이거 물어보는 이유가, 어떤 object node 에 조건부 브랜치가 있을때, 이게 브랜치가 하나 꺼진 경우, 그 값은 제거되기로 했잖아요? 근데 node 본인은 꺼진 브랜치의 값을 볼 수 있는건가요? 아니, value 로 전달되는거면 FormTypeInput 에서는 value가 활성브랜치 여부와 무관하게 보이는건가?" (2026-09-26) | 가. 이름은 표대로 확정한다(VALUE-027·SURFACE-006 현행. VALUE-024의 옛 문장 "value는 원본"은 VALUE-027의 "value는 합성 값"과 어긋나므로 대체됨(→ VALUE-027)). 물음의 답은 대화로 드렸고 요지는 이렇다. `value`(local)는 **활성** 자식의 방출 값만 합성한 것이라 꺼진 분기의 값은 `value`에도 없다. 꺼진 분기의 노드는 형상에 없어 그 입력 구성 요소는 그려지지 않으며, 그 원본은 잠복 원본으로만 남아 `getInactiveValues(path)`로 열거한다. `value`와 `outputValue`의 차이는 투영뿐이다. 투영은 `omitEmpty`·`omitTrailing`처럼 원본을 건드리지 않고 방출에서 빈 값·꼬리 값을 빼는 규칙이다(ADR 0006 §6·§7). 꺼진 분기와 게이트가 거짓인 노드는 투영이 아니라 형상에서 빠지는 것이라 `value`에도 없다. 그래서 입력 구성 요소가 받는 `value`는 활성 분기와 무관하게 보이지 않는다. |
| 12-9 | 노드 공유의 "같은 종류" 정의를 받는가(`reviews/round-18-agenda.md:167`, §1 :20). | "같은 종류 맞습니다." (2026-09-26) | 가. `number`와 `integer`, `['string','null']`과 `'string'`은 같은 종류다. BLUEPRINT-009가 현행이 된다. §1 :20도 닫힌다. |
| 12-10 | D-15: 컨벤션을 어긴 양의 순환 스키마에서 로드한 값이 알림 없이 빠지고 상태가 `stable`로 남는 것을 받아들이는가(`reviews/round-18-agenda.md:168`). | "받아들입니다" (2026-09-26) | 가. 받아들인다. 컨벤션 문서에만 적고 경고 코드는 두지 않는다. TEST-061은 현행(부정 결정)이 되고, A-2의 고지 의무는 이 경우에 적용하지 않는다(답 19가 이긴다). |
| 12-8 이어서 | 값 읽기 이름의 세부. 방출 값의 낱말(`output`/`submit`/다른 것)과 `FormHandle.getValue()`의 이름을 그 낱말에 맞출지. | "일단 첫번째로 form 외부 입장에서는 이게 outputValue 인거랑 value 를 구분할 필요가 없을거같아. node 의 공개표면 이름은 outputValue 로 확정하자. 다만, form handle 에서 getOutputValue 로 할 필욘 또 없을거같다. submit 은 따로 쓰는 명칭이 있어서(form submit 제어용) 사용하기 어렵고. 개별 노드는 내가 getter 를 쓰지 get~~() 함수를 제공하지 않는 방향을 썼어서 getInactiveValues 는 어떻게 구성될지 모르겠군. path 를 받는걸 보면 node 인터페이스는 아닌건가?" (2026-09-26) | 노드의 공개 이름은 `value`(합성 값)와 `outputValue`(방출 값)로 확정. `FormHandle.getValue()`는 이름 그대로 두고 루트의 `outputValue`를 돌려준다(폼 밖에서는 둘을 구분할 필요가 없다). `submit`은 폼 제출 제어에 쓰는 낱말이라 값 이름에 쓰지 않는다. 노드 인터페이스의 관례는 getter이고 `get~()` 함수는 두지 않는다. 잠복 원본 열거의 자리는 다음 행. |
| 12-8 셋째 | 잠복 원본 열거(`getInactiveValues(path)`, F26)의 자리. 권고는 `FormHandle.getInactiveValues(path?)`였다. | "폼 핸들이 오히려 쓸대가 없을거같은데. root node 에 핸들로 추가하고, 개별 노드는 rootNode 의 기능을 경유해서 node.inactiveValues 를 구현하면 어떨까 싶다." (2026-09-26) | 잠복 원본 열거는 **루트 노드의 함수**다(루트가 형상에 없는 노드의 원본을 들고 있으므로 저장 자리와 같다). 모든 노드는 getter `node.inactiveValues`를 두고, 자기 경로로 루트의 함수를 불러 그 아래의 잠복 원본을 돌려준다. `FormHandle`에는 더하지 않는다. `getInactiveValues(path)`라는 옛 표기(06 §6 N6, 02 §6, ADR 0006 §8, ADR 0013 미결)는 이 답으로 대체된다. 반환 모양(WRITE-020, 안건 11-14)은 그대로 열림이다. |
