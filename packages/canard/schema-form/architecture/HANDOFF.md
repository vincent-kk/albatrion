# 핸드오프 — 이어받는 세션이 먼저 읽는 것

갱신: 2026-09-25 (17라운드 최종 반영: 소유자 답, 4번 오류 채널과 노드 구조의 스웜 정련, 커밋하지 않음). 이전 갱신: 2026-09-24 (15라운드 반영, 원리 원장 6차, ADR 0003 6차). 브랜치 `refactor/schema-form-internal-architecture`(원격에 있음). 이 디렉터리는 npm 배포물에 들어가지 않는다.

## 1. 지금 어디에 있는가

`@canard/schema-form`의 전면 재설계를 **설계 단계**에서 진행 중이다. 코드는 한 줄도 바꾸지 않았다. 설계는 열일곱 라운드의 검토를 거쳤고 ADR 0014는 4판(채택, 17라운드), ADR 0003은 6차, 0002·0005·0006·0007·0008·0013은 5차, 나머지는 4차(5차 주 있음)다. 아직 끝나지 않았다.

| 라운드 | 대상 | 결과 |
| ------ | ---- | ---- |
| 1 (`reviews/round-1.md`) | ADR 0001–0011 초안 | R1–R19. ADR 대부분을 고쳐 씀 |
| 2 (`reviews/round-2.md`) | 고쳐 쓴 ADR | S1–S15 확인. 작업 루프 2차안이 방출 값을 상태의 함수로 만들지 못함(S3)이 핵심 |
| 3 (`reviews/round-3.md`) | 3차안 명세(`round-3-spec.md`) | T1–T14·B1–B6·C1–C4. 3차안은 구현 가능하고 비용도 맞으나 열아홉 군데를 고쳐야 함(E1–E19). 소유자 결정 여섯(D-1–D-6) |
| 4 (`reviews/round-4.md`) | 3.1판 명세(`round-4-spec.md`) + 이벤트 시스템 + 계승 제약 | U1–U19·V1–V12. 3라운드 결함 전부 소멸, 동기 통지·배치·수렴은 실행으로 통과. 파동 상한·원장·리스너 목록·배치 경계를 F15–F19로 채움. 소유자 결정 넷 추가(D-7–D-10) |
| 5 (`reviews/round-5-decisions.md`, `round-5-derivations.md`) | 결정 D-1–D-10 자체 | 네 시선 검토, 결정 간 모순 C-1–C-11. 소유자의 원칙 P1′로 D-3이 도출되고 나머지는 프로토타입(`loop-v4b`·`loop-v4c`·`rejected-keys`)과 React 테스트로 확인. **ADR 0002·0005·0006·0007·0008·0011·0013을 4차 본문으로 고쳐 씀** |
| 6 (`reviews/round-6-coherence.md`) | 4차 본문 전체 조망 — "모든 동작과 인터페이스가 예측 가능하고 합리적이며 표준적이고 일관된 원리에 기반하는가" | 리뷰어 다섯(로컬 verifier 2, cennad codex·antigravity·claude), 대조 검증 62묶음(확정 52·부분 6·기각 2·추론 2). 형상·읽기·방출·통지는 원리 위에 서 있으나 **원본을 쓰는 자동 쓰기**(전이 주입·injectTo·&derived)는 라운드 도중 형상·직전 커밋·배치 여부·선언 순서를 읽어 같은 상태가 경로에 따라 다른 값을 커밋한다(E1·E2·E8·E9, 실행 확인). 새 원리 제안 P6–P9, 소유자 결정 D-11–D-23, 단조 수정 27건 |
| 7 (`reviews/round-7-convergence.md`) | 소유자 지시 "정책 판단이 아니면 축에서 수렴시켜라" — D-11–D-23·P6–P9·단조 수정 27건 | 소스 여섯(도출 셋: 로컬 opus·cennad antigravity·cennad 격리 claude, 실행 둘: 프로토타입 `loop-v4d`·codex 독립 모델, 조사 하나), 대조 검증이 실행을 재현하고 인용 28건 점검. **일곱이 축에서 닫힘**(D-12·13·14·16·17·19·21), 셋은 핵심만(D-11·22·23), 둘은 정책(D-18·20), 하나는 실험(D-15). **핵심 발견: 배치 ≠ 순차는 결함이 아니라 축의 귀결** — 6라운드 #5·#16의 G5·P3 위반 격상과 D-13 권고 (a)는 과장. P6 (i)는 채택되나 자동 쓰기의 고정점 문제(없음·여럿·미도달)를 드러내 새 결정 후보 D-24–D-36(도출 7·소유자 5·분할 1) |
| 8 (`06-conclusions.md`, 원문 `reviews/raw-round8-*.md`, `spikes/round8/`) | 소유자 지시 "정책도 원리로 추론 가능한 영역이 있다 — 진짜 판단은 분리하고 나머지는 스웜으로" | 7라운드 정책 열둘을 (가) 이름·표면, (나) 원리로 더 밀 것 여섯, (다) 가치 판단으로 재분류. (나)에 도출 셋 + 프로토타입 `loop-v4e` + 대조 검증. 결과: D-31 원본 B 커밋·D-32 받은 순서는 도출, D-11′·D-27·D-29·D-22′는 한 문장씩으로 좁힘. 이름 제안 N1–N7(쓰기 옵션은 소유자 지시로 비트마스크). 정정: D-21의 현행은 별칭(행동 변화), 편집자 가설 둘의 근거 오류. **모든 결과를 `06-conclusions.md`에 약어·생략 없이 모아 소유자 통과/반려 대기** |
| 9 (`07-conclusions.md`, 명세 `reviews/round-9-spec.md`, 종합 `reviews/round-9-derivation.md`, 원문 `reviews/raw-round9-*.md`, `spikes/round9/`) | 소유자가 06 검토 전에 축 열 항목을 더함(폼은 `if`·`oneOf`를 해석하지 않음, 조건 프로퍼티는 `properties`에, JSON Schema 층은 값을 조작하지 않음, 노드별 유효 스키마 병합 의무, `&` 층은 값을 제어, `computed`와 `&` 병행, 글로벌·로컬 경합 유지) + 읽기 셋의 답(분기는 검증 조건이며 `if/then/else`나 `&`로 제어, 채움은 노드가 생길 때 `&default` > `default`, 루트 `readOnly` 등의 위계) | ajv 실측(`oneOf`/`anyOf` 안의 `if`는 `else: false`와 `required`가 함께 필수), 이름 추천 셋(`control`, `&unsetValue`, `DisableAutomaticWrites`, `&if`→`&active` 통합, `&default`, `&children`), 도출 셋 + 프로토타입 `loop-v5` + 대조 검증. 결과: 판별식·`selection`·분기 선택 API·동점이 모두 지워짐(상태 칸은 원본과 `extras`뿐), 채움은 노드 단위 한 번, 예약 층의 단계 표, 같은 대상 규칙(대상별 하나), 결합 규칙 AND/OR(확신 중), 병합표. 5.1 `fire`·5.2 에지·5.3 C 기울기·5.5 소멸·5.6 첫째 유지. **`07-conclusions.md`가 06을 대체하며 소유자 통과/반려 대기** |
| 10 (`07-conclusions.md` 5절·12절, 명세 `reviews/round-10-spec.md`, 종합 `reviews/round-10-derivation.md`, 원문 `reviews/raw-round10-*.md`, `spikes/round10/`) | 소유자 지시 "원칙에서 추정 가능한 것과 정말 내가 결정해야 하는 것을 나누고, 추정 가능한 것은 추정 라운드와 검증을 돌려라" | 스물셋을 소유자만 정할 것 셋(22·12·1), 확인만(10), 추정 대상 열아홉으로 가르고 독립 추정 셋 + 실험(v6, 스위치 5·48조합·2,904건) + 대조 검증. 채택 규칙(셋 모두 "추정" + 인용 확인) 통과는 3(`findNodes`) 하나. 검증자 판정 추정 넷(11·15·20·2)은 확인 후보, 셋이 같은 방향 넷(6 `fire`·7 C·14 (나)·17), 미수렴 열. 실험: 8·9·21의 후보는 예산·손실·순서 의존에서 갈리지 않고 "선언 순서"만 선언 의존을 새로 만듦. 새 발견: 정착 안의 에지 소비 규칙(07 4.30). **소유자가 07 5절의 스물둘에 답하면 된다** |
| 11 | 2026-09-23 | 소유자 답 26개의 정합성 검증(검증자 claude + antigravity 교차, 둘 다 조건부 통과), ADR 일곱 5차의 원장 대조 검증과 수정 25건 적용, `02-target-overview.md` 5차와 그 검증·수정 12건, 생성기 스키마 14종 실측(`spikes/round11-corpus`). 파일: `reviews/round-11-owner-answers-check.md`, `reviews/raw-round11-owner-answers-{claude,antigravity}.md`, `spikes/round11-corpus/REPORT.txt` |
| 12 | 2026-09-24 | 소유자 답 정합성 검증의 확인 항목 여덟을 검토 문서로 물어 답 22개를 받음(7건 확정). 레드팀 공격 둘(루트 스키마 글로벌 잠금 제거, 나감 비움 기본)을 검증자(오늘 코드 탐침)와 antigravity에게 독립으로. 오류·경고 분류를 오늘 코드 체계로 다시 구성. `&clearValue`→`&unsetValue`. 파일: `reviews/round-12-owner-review.md`, `round-12-owner-answers.md`, `round-12-derivation.md`, `raw-round12-*.md` |
| 13 | 2026-09-24 | 레드팀 뒤 남은 판단 넷을 물어 답을 받음: 코어에 글로벌 잠금 없음(루트 키 특수 처리 삭제, Form 속성은 렌더 계층의 전체 잠금), 나감 비움은 선택이고 기본은 유지, `&`는 제어 키에만(표현 키는 접두 없음), 규칙 충돌은 종류 순위 + 같은 종류는 문서 순서. 파일: `reviews/round-13-owner-review.md`, `round-13-owner-answers.md` |
| 14 | 2026-09-24 | 다섯 가치(일관성·투명성·예측가능성·고속성·표현자유도)에 비춘 스웜 검증 여덟(가치 축 다섯·현행 코드 대조·코드 사실·antigravity). 원리는 그대로, 편집자 문장의 어긋남·빈틈 스물넷 묶음을 원칙으로 닫아 반영, 소유자 판단 열하나는 `reviews/round-14-owner-review.md`. a-z 설계서 `08-design-a-to-z.md`와 독립 PR 단계 계획. 파일: `reviews/round-14-values-check.md`, `raw-round14-*.md`, `reviews/round-14-owner-review.md`, `round-14-owner-answers.md`, `adr/0014-error-policy.md`(제안 3판) |
| 15 | 2026-09-24 | a-z 검토 논의. `&` 축약 제거, 그룹 셋, 기준점 호스트, 렌더 계층 이름 규칙. `reviews/round-15-decisions.md` |
| 16 | 2026-09-24 | 최종 검증 단계. 정착 검토(조건부 통과, 조건 여덟), 테스트·스토리북·벤치 전략, 문서 게이트 여덟 번, 스웜 수렴(도출 셋·반박 셋·판정)과 그 뒤 게이트 여섯 번, 소유자 답 열(여덟 확정, `reset`·릴리스는 조사 뒤 16라운드 스웜 수렴(편집자 결정)으로 닫힘, PR-8 판 번호는 1.0.0-beta 뒤 1.0.0으로 확정). 파일: `09-landing-and-test-strategy.md`, `reviews/round-16-*.md`, `reviews/raw-round16-*.md` |
| 17 | 2026-09-25 | 소유자 지시 "열여덟도 이번처럼 스웜으로 먼저 수렴시키세요". 16라운드가 넘긴 소유자 확인 열여덟(15라운드 게이트 뒤 편집자 결정 일곱, 14라운드 O-8, ADR 0014 미결 아홉)을 스웜으로 수렴(도출 넷·반박 넷·판정 하나, 규칙 1·2)하고 게이트 둘(A 실패 결함 열, B 조건부 통과 결함 스물넷)과 적용 뒤 게이트 넷을 반영. 편집자 결정 스물다섯은 17라운드 스웜 수렴(편집자 결정). 소유자 질문 셋과 통보 넷은 모두 답을 받음: R17-1 나(정착 오류는 모든 환경에서 사슬 끝 throw, `degraded` 동안 제출 거부, 끄는 스위치 없음), R17-2 ㄴ(나가는 객체·분기의 `unsetOnInactive`가 함께 나가는 하위 트리로 내려감), R17-3(`options.trim`, 포커스 아웃 때 문자열 행의 `finishInput` 칸이 자름, 어댑터는 입력 마침 신호만), 통보 1·2 허용, 통보 3 고침(검증기 없음은 거부 없이 경고). 통보 4의 답으로 4번 오류 채널 스웜(도출 셋·반박 셋·판정·게이트, 안 B: Form 속성 `onError`는 검증 결과를 뺀 폼 내부의 오류와 경고를 같은 모양의 기록으로 받는 관찰자, 게이트 조건부 통과 R17G-1–R17G-11, 소유자 확인 둘)과 노드 구조 스웜(도출 셋·반박 셋·판정·게이트, 단일 클래스 `SchemaNode`와 `BEHAVIORS[type][strategy]`, 책임별 fractal과 겉면 규칙, 이름 칸 열다섯, 게이트 조건부 통과 N1–N17)을 병렬로 돌림. 이름 규칙과 전환 방식의 답을 받아 17라운드 정리 명세 한 판을 편집자 넷이 파일을 나눠 반영. ADR 0014는 4판(채택). 결정하지 않은 것은 18라운드 안건. 파일: `reviews/raw-round17-convergence.md`, `reviews/round-17-owner-answers.md`, `reviews/raw-round17-onerror.md`, `reviews/raw-round17-node-structure.md`, `reviews/round-18-agenda.md`, `reviews/round-15-decisions.md`의 "17라운드 결정" 칸 |

수락된 큰 결정(소유자 발언 인용은 각 ADR의 "합의 근거"):

- 검증기 입력 불변, 판정 동치 G1 (ADR 0001)
- 조건부 장치는 가드 → 조각 하나의 모델 (ADR 0002)
- FE 키워드는 키워드 위치의 그룹 객체 셋(`controls`·`options`·`presentation`), 키워드 위치에서만 제거 (ADR 0003)
- 검증기 플러그인 + 동기 `compileGuard` (ADR 0004)
- 노드 트리가 곧 상태, 별도 데이터 모델 없음, Fiber 방식 (ADR 0006)
- Form은 단일 스키마, 오버레이 입구 없음 (ADR 0012)
- core는 값을 고치지 않는다, 에러를 보여준다, min/max는 입력 컴포넌트에 (ADR 0013)
- core는 React를 모른다, 터미널 전략은 유지 (`00-goals.md` C3, ADR 0011 §3. core는 React를 모른다는 목표이며, 오늘 core가 `ValidationManager` → `PluginManager`로 React 구성 요소 모듈을 가져오는 import의 분리는 PR-4 전 설계 항목이다. 인라인 `FormTypeInput`의 터미널 판정은 렌더 계층이 넘기는 판정 함수로 옮긴다, 17라운드 스웜 수렴(편집자 결정))
- 목표 후보 C2·C4·C6·C7·C8 채택 (`00-goals.md`)
- 오류는 삼키지 않는다: 오류·경고·검증 결과의 세 층, 정착 오류는 모든 환경에서 사슬 끝 throw와 `degraded` 동안 제출 거부, 관찰자 `onError` (ADR 0014 4판, 17라운드)
- 노드 구조: 상속 없는 단일 클래스 `SchemaNode`와 종류별 동작 행 `BEHAVIORS[type][strategy]`, 공개 `node.group`은 `node.strategy`로 (`09-landing-and-test-strategy.md` §3, 17라운드 소유자 답)

## 2. 소유자의 결정을 기다리는 것 — 교차검증을 거친 권고와 함께

`reviews/round-3.md` §4(선택지)·§8(cennad 교차검증 + 수렴 1회, 3자 일치 권고). **소유자의 지시(2026-09-22)**: 대략 수렴했으므로 결정 전에 먼저 `03-mental-model.md`로 이해를 맞추고, 그 뒤에 단조 작업(ADR 본문 개정)을 한다. 2026-09-23에 소유자가 P1을 원칙으로 답했고(P1′), 그 원칙에서 D-1–D-6이 도출됐다(`reviews/round-5-derivations.md` §3). ADR 4차 본문은 이 도출을 전제로 쓰였으며 각 ADR의 상태 줄이 "원리에서 도출, 소유자 확정 대기"로 표기한다. 이해 맞추기의 남은 질문은 `03-mental-model.md` §7.

| # | 결정 | 3자 일치 권고 | 소유자 |
| - | ---- | ------------- | ------ |
| D-1 | #338 null 계약 | **유지.** null은 키 없는 전체 교체, 빈 상태는 로드 계약의 default. E9·E18 삭제, ADR 0006 결정 5 개정 | 원리(P2)에서 도출, 확정 대기 — ADR 0006·0013 반영 |
| D-2 | 조각 활성 계산 | **비단조 + 상한**, 정착 상태 칸과 루트 통지로 프로덕션 관측 | 원리(P3)에서 도출, 확정 대기 — ADR 0002·0007 반영 |
| D-3 | 금지 조각 | **읽지 않는다(검증기에만).** 잔여 키는 플러그인 계약 `rejectedKey` | 소유자 원칙 P1′(2026-09-23)에서 도출 — 2라운드 발언은 `&active`만 뜻했음. ADR 0002 반영, 확정 대기 |
| D-4 | 쓰기 종류 | 옵션 객체 `{ mode?, disableDefaultInjection? }`, `Refresh`는 core 판단 | 원리(P2)에서 도출, 확정 대기 — ADR 0013·0007 반영 (4차. 5차에서 대체됨 — 원장 §3·§1.4·§6) |
| D-5 | 로드 시 default 스위치 | 기본 계약 유지 + Form 속성 + 호출 옵션(양방향), `reset`·마운트 포함 | **방향 확정: 뜻은 "초기값 조작 비활성화", 이름은 `disable…` 형태.** 후보 `disableDefaultInjection`, 이름 확정 대기 |
| D-6 | `virtual` | (a) `&` 계열 + 참조 그룹 노드, 튜플 value는 읽기 전용 파생 | 원리(P5)에서 (a)·(b) 모두 양립, 차이는 바인딩 두께의 비용 — ADR 0011이 (a)를 전제로 재작성, 확정 대기 (4차. 5차에서 대체됨 — 원장 §3·§1.4·§6) |
| D-7 | `setValue(getValue())` 멱등성 | (a) 그대로 (b) 직전 커밋에도 없던 키에만 주입 | **(a) + `setValue` 호출 단위 옵션** (이름 미정) |
| D-8 | 원본 없는 중첩 union의 판별 default | (a) 첫 분기 값 (b) 호스트에 원본이 있을 때만 | **전제 철회 — 암묵 default 없음, 빈 값은 분기 없음(현재와 같다).** 3.1판 A7·3라운드 E8 수정 대상 (4차. 5차에서 대체됨 — 원장 §3·§1.4·§6) |
| D-9 | `RequestRemount` | (a) 제거 (b) 탈출구로 유지 | **(b) 유지** — 사용자 도구 |
| D-10 | 루트 `onChange` 디바운스 기본값 | (a) 파동마다 동기 (b) 현재처럼 매크로태스크 디바운스 | **(c) 최외곽 동기 진입당 1회**, 마이크로태스크 없음 |

**ADR 4차 본문(2026-09-23)**: 위 도출을 전제로 ADR 0002·0005·0006·0007·0008·0011·0013의 본문을 고쳐 썼다. "확인된 결함" 절은 모두 본문에 흡수해 삭제했고, 남은 것은 각 ADR의 "미결"에 있다. 소유자가 D-1–D-6 가운데 하나라도 도출과 다르게 정하면 해당 ADR 본문과 `03-mental-model.md` §5를 고치고 "변경 이력"에 남긴다.

**5라운드 후속(2026-09-23)**: 소유자가 원칙으로 답했다 — "form은 값을 고치지 않고 에러만 보인다. 빼거나 지우는 것은 `&` 명령뿐이다. form이 스키마에서 읽는 것은 노드의 모양을 정하는 문법(타입·자식의 존재·if-then-else 같은 가변 구성)뿐이다." 이것을 P1′로 적었고, 모순 11건 가운데 정책 판단이던 D-3이 (iii)로 도출됐으며 나머지는 프로토타입(`loop-v4b`, `loop-v4c`, `rejected-keys`)과 React 테스트로 확인됐다 — `reviews/round-5-derivations.md`. **D-1–D-6의 확정은 소유자가 ADR 4차 본문을 읽고 한다.** 새로 문서화할 것: React 이펙트로 파생 값을 쓰면 키 입력당 onChange가 둘(C-10, 사실) — 파생은 `derived`/`injectTo`/리스너로.

**10라운드 뒤 소유자의 답(2026-09-23)**: 소유자가 deilen 뷰어에서 스물셋에 모두 답했다(`reviews/round-10-owner-answers.md`, 07 §5.0). 그 답으로 원리 원장 `03-mental-model.md`를 5차로 다시 썼고, ADR 0002·0003·0005·0006·0007·0008·0013의 5차 본문 작업이 진행 중이다(슬라이스 0). 다음 세션은 ADR 5차의 검증 결과와 `02-target-overview.md` 다시 그리기, corpus 14종 재실행, 성능 기준선 측정부터 한다.

**10라운드(2026-09-23)**: 07의 5절을 소유자가 답하기 쉬운 순서로 다시 배치했다. 5.1 소유자만 정할 것 셋, 5.2 확인만 받을 것 다섯, 5.3 셋이 같은 방향으로 기운 것 넷(권고 있음), 5.4 수렴하지 않은 것 열(권고 있음). 3번은 4.29로 옮겨 닫혔다. 4.30(정착 안 에지 소비)은 편집자 판정이며 소유자 확인 대상이다. 다음 세션은 소유자의 답을 받는 것부터 한다.

**9라운드와 최종 결론 2판(2026-09-23)**: **다음 세션은 `07-conclusions.md`의 소유자 판정(절 단위 통과/반려)을 받는 것부터 한다.** 06은 07이 대체했고 그대로인 항목의 전문만 06에 남아 있다. 07의 5절에 소유자가 답할 문장 스물두 개가 있다(10라운드 뒤 재배치: 소유자만 셋, 확인만 다섯, 권고 있는 것 열넷). 11절이 개발 진입 평가(단계적 진입 가능, 코드 전 할 일 넷, 슬라이스 0–8), 12절이 10라운드 결과다. 통과 뒤 순서는 07 §9(원리 원장 `03-mental-model.md` 다시 쓰기가 먼저, 그 다음 ADR 5차 본문).

**8라운드와 최종 결론(2026-09-23)**: (07로 대체됨) 다음 세션은 `06-conclusions.md`의 소유자 판정(절 단위 통과/반려)을 받는 것부터 한다. 소유자는 "이해하지 못하면 반려한다"고 했다 — 반려된 절은 더 풀어 쓰거나 근거를 보강해 다시 낸다. 통과된 절부터 §9의 순서(ADR 5차 본문, 단조 수정, `02-target-overview.md`, ADR 0010, 이주 안내)를 진행한다. 실험 셋(§7)은 판정과 무관하게 시작할 수 있다.

**7라운드(2026-09-23)**: 6라운드의 D-11–D-23은 `reviews/round-7-convergence.md`로 정리됐다. §2가 수렴된 것(소유자는 확인만), §3이 소유자 정책으로 남은 것(D-11′·D-14′·D-18·D-20·D-22′·D-23′·D-27·D-29·D-31·D-32·D-35·D-36, 살아남은 선택지와 지워진 선택지를 함께), §4가 원리 읽기의 확인 여섯(G5의 "같은 쓰기 순서"는 정착 단위, P2 문구, G4 좁히기, P1′ 괄호, D-15 S2 비대칭, 도출 일곱 D-24·25·26·28·30·33·34의 수락), §7이 실험이 먼저인 것(D-15 출발점 S1/S2, D-33 deepEqual 비용, D-17 React 이펙트 순환)이다. **6라운드의 정정은 §5** — D-13은 (b) 직전 커밋이며 배치와 순차의 차이는 문서화 대상, D-11의 전제 "`readOnly`로 막힌다"는 현재 코드에서 거짓, F13 힌트는 단서 복원이 아니라 제거.

**5라운드(2026-09-22)**: 소유자 지시로 결정 D-1–D-10을 네 시선(배경지식·소비자·구현 비용·반대 변론)으로 다시 검토했다 — `reviews/round-5-decisions.md`. 결정은 내리지 않았다. 항목별 이해·장단점·제안·열린 질문과 함께 **결정 간 모순 11건(C-1–C-11)**을 적었으며, 특히 C-2(null 복귀와 `setValue(getValue())`의 재주입이 같은데 D-7만 구별), C-7(빈 값에서 판별식 union은 분기 없음·선택 가드 union은 첫 분기), C-9(D-10의 "진입당 1회"는 프로토타입에 없음)는 결정 전에 볼 것.

결정이 나오면: `open-questions.md`에서 지우고 해당 ADR로 옮긴다(README "기록을 고칠 때").

## 3. 의제 — 남은 것

아래 1–6·8은 2026-09-23 4차 본문으로 **완료**됐다(각 ADR의 "변경 이력"). 남은 것은 7·9·10과 각 ADR의 "미결" 절, 그리고 7라운드의 결과다. **다음 세션의 순서**: (1) 소유자가 `reviews/round-7-convergence.md` §2의 수렴과 §4의 읽기 확인 여섯을 받거나 되돌린다 — 하나라도 다르면 해당 행이 다시 열린다. (2) §3의 정책 열둘을 한 번에 하나씩 묻는다. 무게 순으로 D-11′(파생 필드 덮어쓰기)과 D-27(마운트·`reset`의 `injectTo`)을 한 규칙으로, 그 뒤 D-31·D-29·D-32, 이름은 D-18·D-20·D-22′·D-23′·D-35·D-36. (3) §7의 실험 셋 가운데 D-15 출발점 실험은 결정과 무관하게 지금 돌릴 수 있다. (4) §8의 free 8행과 free\* 4행은 소유자가 §2를 받으면 바로 적용하고, 나머지는 결정과 함께 **ADR 5차 본문**에 반영한다. 원래의 개정 순서는 기록으로 남긴다:

1. **ADR 0007 4차 본문** — E1(계산은 raw를 쓰지 않는다, default는 전이 사건), E4(모든 조각 매 바퀴), E5(가드는 투영 후 값, 호스트만 `{}`), E6(옵션 B), E7(상한 라운드 미실행), E12(상속 overlay). 스파이크 `spikes/work-loop/proto/loop-v3.mjs`를 E1·E5·E6·E12로 고쳐 `selfcheck-v3.mjs`의 h와 레드팀 `redteam3/attacks.mjs`의 반례가 사라지는지 본다. **특히 E5로 바뀐 가드 입력이 S8을 정말 막는지, E12의 재계산이 바퀴 상한 안에 끝나는지.**
2. **ADR 0002 개정** — 조각 트리(E3), 금지 조각(D-3), 초기 선택 가중(E17), 상속 overlay(E12).
3. **ADR 0005 개정** — 판별식 식별을 E14로 대체. 계약 레드팀의 코퍼스(`spikes/guard-cost/redteam3/corpus.mjs`, 14종)를 회귀 표본으로 삼는다.
4. **ADR 0006 개정** — 칸 목록(raw·local·emit·active·selection·extras·dirty), emit의 불변식(E9), 역색인 행 삭제(E13).
5. **ADR 0013 개정** — E15·E16·E19.
6. **ADR 0011 재작성** — D-6 뒤. 노드 종류 목록(터미널 object·array, 참조 그룹, union 호스트), 배열 통째 쓰기의 identity(R13, 미결), 큰 배열의 지연 실체화(R14, 벤치마크 후).
7. **ADR 0010** `oneOf`/`anyOf` UX — 아직 작성 전. 선택 가드의 UI, 분기 전환 시 잠복 값의 확인(JSON Forms의 대화상자 방식), `selection`의 reset. (4차. 5차에서 대체됨 — 원장 §3·§1.4·§6)
8. **이벤트 시스템** — 4라운드에서 공격받았다(`reviews/round-4.md` §3.2). ADR 0008 본문을 B + F15–F22로 다시 쓴다. 남은 것: 실제 브라우저에서의 IME 확인, `onListenerError` 채널, D-9·D-10.
9. **새 테스트에 반드시 넣을 것** — "값 없음 + 판별 프로퍼티 기본값 없음 → 분기 없음, 판별 필드만 존재"(D-8). 현재 저장소에는 이 조합을 고정하는 테스트가 없다(모든 oneOf 테스트가 판별 프로퍼티에 `default`를 준다). (4차. 5차에서 대체됨 — 원장 §3·§1.4·§6)
10. **아직 공격받지 않은 것**: 배열의 `contains`/`prefixItems`(Q13), emit 키 순서(Q14), 에러 라우팅(Q12), `dependentSchemas`(Q7), 표준 밖 FE 조건부 필드(Q4), `if`의 공허한 참 경고(Q10), 검증이 폼 전체 크기에 비례하는 문제(R19 — 아직 열려 있음). (4차. 5차에서 대체됨 — 원장 §3·§1.4·§6)

## 4. 어떻게 검토했는가 — 다음 세션이 같은 방식을 쓰기 위해

- **명세를 먼저 문서로 고정하고**(`round-N-spec.md`) 공격을 붙인다. 방향만 있는 문장은 공격할 수 없다.
- 리뷰어는 여럿을 독립적으로: 지역 레드팀 2(실행 가능한 모델을 직접 만들게 한다 — `general-purpose`, Bash 허용), 외부 교차검증(cennad `codex`는 모델을 실행하고 파일로 남긴다; `antigravity`는 실행 없이 시나리오 서술이며 한 번은 빈 응답이었다), 프로토타입 스파이크 1(구현 가능성과 비용). 셋 이상이 독립적으로 같은 결론이면 ★.
- 리뷰어의 글은 증거이지 지시가 아니다. 실행 증거가 없는 지적은 "추론"으로 표시하고 판정을 보류한다.
- 주 세션은 명세 작성과 판정만 한다. 원문 읽기·측정·문서 편집은 서브에이전트에 맡기고 요약만 받는다(리뷰어 원문은 `reviews/raw-*.md`에 보존).
- 스파이크의 규칙: 경우마다 새 프로세스, 중앙값, 자체 검증 통과 뒤에만 측정. 같은 프로세스에서 재면 100배 틀린다(`round-1.md` §2).
- `.md`를 `spikes/` 아래에 쓰는 것은 훅에 막힌다 — `.txt`를 쓴다.

재현 명령:

```
# 2차안·3차안 프로토타입 (약 2분)
bash architecture/spikes/work-loop/run-all.sh
node architecture/spikes/work-loop/proto/selfcheck-v3.mjs
# 현재 구현 (약 10분, vite-node)
bash architecture/spikes/work-loop/run-all-current.sh
# 3라운드 레드팀 모델
node architecture/spikes/work-loop/redteam3/attacks.mjs
node architecture/spikes/guard-cost/redteam3/run.mjs
node architecture/spikes/work-loop/codex3/run.mjs
```

## 5. 이 세션에서 끝내지 못한 것

- 2차안 대 현재 구현의 재측정은 끝났다 — `spikes/work-loop/REPORT.txt` 끝의 RE-RUN 절, 15% 안에서 재현. `round-2.md` §2에 주석을 달았다.
- 소유자: "새 설계가 어떻게 구성되는지 아직 잘 와닿지 않는다. 레드팀 검증 뒤 다시 질문하겠다." — 설명 자료는 artifact로 답했고 `03-mental-model.md`는 4차에서 갱신했다. **02는 5차**(문서 머리에 표시). 6라운드 의제.
- ADR 0002·0003·0005·0006·0007·0008·0013은 5차, 0010은 초안, 나머지는 4차(5차 주 있음)다(2026-09-23). ADR 0001·0003·0004·0009·0012는 개정 대상이 아니었고, 0004에는 Q12의 에러 라우팅 규칙을 아직 적지 않았다. 0010은 작성 전. 7라운드의 결과는 아직 ADR에 반영하지 않았다 — 소유자의 §2·§4 확인이 먼저다.
- 7라운드 스파이크 `spikes/round7/proto/loop-v4d.mjs`는 v4c에 P6·에지 발화·초기 선택 스위치를 더한 것이며(`make-v4d.mjs`가 차이 전부), `selfcheck-v4d.mjs`는 새 동작에서 A4b·A4c·A4-cap이 실패한다 — 셋 다 옛 규칙을 적은 검사이고 고치지 않았다. D-15 실험은 이 파일의 사본에 `START_SET` 스위치를 더해 돌린다(`round-7-convergence.md` §7).
- 커밋은 소유자 요청으로 했다(2026-09-23). 커밋 시 co-author와 세션 ID를 넣지 않는다(소유자 규칙).

## 6. 제약 (소유자 규칙, 계속 유효)

- 한국어 격식체. 소유자는 Vincent / Vincent Kelvin으로만 부른다.
- 재설계는 완전한 브레이킹 체인지이며 모든 버전을 한 번에 메이저 버전급으로 올린다(C7. 판 번호는 1.0.0-beta 뒤 1.0.0, 09 §6.2의 열넷째). 릴리스 노트와 이주 프롬프트를 낸다(C8).
- 소유자가 부분마다 이해하고 진행한다 — 결정 항목은 한 번에 하나씩, 선택지와 결과를 함께 제시한다.

## 다음 세션

16라운드는 `99765899b`로, 17라운드는 그 다음 커밋(메시지에 'design review round 17')으로 커밋되었다.

17라운드의 결과는 다음과 같다.

- **이월 열여덟의 스웜 수렴.** 16라운드가 넘긴 소유자 확인 열여덟을 스웜으로 먼저 수렴시켜 편집자 결정 스물다섯으로 닫았다(`reviews/raw-round17-convergence.md`). 남은 소유자 질문 셋과 통보 넷은 모두 답을 받았다(`reviews/round-17-owner-answers.md`).
- **R17-1 → 나.** 작성자 스키마·호출자 데이터에서 온 정착 오류(예산 초과, `controls` 식·가드 실패, `controls.injectTo` 대상 없음)는 모든 환경에서 커밋·통지 뒤 사슬 끝에서 던진다. `degraded`(원인: 예산·식·대상·공유 충돌)는 다음 로드까지 남고 그 동안 폼의 제출 경로가 `SchemaFormError`로 거부한다(`getValue()`는 막지 않음). 끄는 스위치는 없다(`throwOnBudgetExceeded` 없음). 12라운드 §4, 10라운드 B-1, 14라운드 O-4 가는 이 답으로 대체되었다.
- **R17-2 → ㄴ.** 나가는 객체·분기에 켠 `unsetOnInactive`는 함께 나가는 하위 트리로 내려간다(13라운드 답 1에 둘째 예외). 자손이 스스로 적은 선언이 가까운 순서로 이긴다.
- **R17-3(`trim`).** 설정은 `options.trim`(닫힌 목록)이다. 포커스 아웃 때 저장값을 자르며, 판단은 문자열 동작 행의 `finishInput` 칸에 두고 어댑터는 타입을 모르는 입력 마침 신호(`finishInput`)만 보낸다. 자른 값은 사용자 입력과 같은 쓰기(입력 출처)이고 코어의 자동 쓰기가 아니다.
- **통보.** 통보 1·2는 허용되었다. 통보 3은 고쳤다. 검증기가 없고 검증 모드가 `None`이 아니면 거부하지 않고 검증 없이 진행하며, 개발 모드 콘솔과 `onError`의 경고 기록으로 트리마다 한 번 알린다. 검증기는 있으나 전체 스키마 컴파일이 실패한 경우는 R17-1 나를 따라 `validate()`와 제출을 거부한다.
- **4번 → 안 B.** Form 속성 `onError(record)`는 검증 결과를 뺀 폼 내부의 오류와 경고를 같은 모양의 기록으로 받는 관찰자다. 흐름을 바꾸지 못하고(바운더리는 다시 던지지 않고 가두어 보고한다), 핸들러가 없으면 비용이 없다. 기록은 `level`, `code`, `message`, `path`, 원래 오류 등을 싣고, 코드 목록은 50행이다. 게이트 뒤 소유자 확인 둘은 (가) 검증기 없음 알림은 `warning`, (나) `@winglet/react-utils`의 선택 인자 확장 허용(PR-7에서 그 모듈의 `DETAIL.md`를 먼저 갱신)이다. ADR 0014는 4판(채택)으로 다시 썼다. 기록은 `reviews/raw-round17-onerror.md`다.
- **노드 구조.** 상속 없이 단일 클래스 `SchemaNode`와 종류별 동작 행을 둔다. 표는 `BEHAVIORS[type][strategy]`이고 노드는 생성 때 고른 행 하나를 필드 `behavior`로 들며, 공개 `type`·`strategy`는 행에서 읽는 게터다. 공개 `node.group`은 `node.strategy`로 바뀐다(값은 그대로, 가드 이름은 유지). 새 core는 `record/`, `behaviors/`(종류 모듈 `stringBehavior/`·`numberBehavior/`·`booleanBehavior/`·`nullBehavior/`·`virtualBehavior/`, `objectBehavior/`와 `arrayBehavior/` 안에 `branch/`·`terminal/`·`utils/`), `navigation/`, `settle/`, `dispatch/`, `validation/`, `SchemaNode/`로 나뉜다. 형 이름은 `SchemaNodeRecord`, `Behavior`, `SchemaNodeFactory`, `SchemaNodeRuntime`, 모듈 수준 생성 함수는 `schemaNodeFactory`, 행의 칸은 `interpret`·`assemble`·`project`·`finishInput`·`declareChildren`·`type`·`strategy`, 종류별 데이터 칸은 `structure`다. 기록은 `reviews/raw-round17-node-structure.md`다.
- **이름 규칙.** 넓은 범위의 이름(내보내는 형, 모듈 수준 함수, fractal 진입점의 이름)은 `SchemaNode` 접두를 쓴다. `Node`로 줄여 부르는 것은 한 형의 필드나 한 모듈 안의 지역 이름처럼 아주 좁은 이름공간에서만 한다(전역 `Node`와 헷갈리지 않게).
- **전환 방식.** PR마다 대상 영역의 옛 코드를 레거시 디렉토리로 옮기고 새 코드를 쓴다. 쓸 만한 코드와 함수는 가져오고 나머지는 버린다. 옛 이름과의 중복은 기준이 아니다.

반영 자리는 `08-design-a-to-z.md`(§16.3이 답과 결과를 모은다), `09-landing-and-test-strategy.md`(§2.2, §2.3, §3, §4.4), `03-mental-model.md`, `02-target-overview.md`, ADR 0003·0004·0005·0007·0008·0011·0013·0014, `open-questions.md`, `07-conclusions.md`, `01-current-structure.md`다. PR 안에서 할 문서 일(패키지 문서, 이주 자료, `docs/agents`)은 `reviews/raw-round17-convergence.md` §8에 남아 있다.

**다음 세션의 순서.** 소유자 지시(2026-09-25): "지금 설계문서가 엄청 비대해졌거든? 그래서 이걸 이제 단일 원장으로 증류하는 작업을 진행했으면 해. 지금 작업 마무리하고, 교차검증까지 모두 끝내서 설계완료를 확정하면, 모든 결정사항을 일목요연하게 정리한 ADR 과 설계문서를 만드는게 목표야. 이때, 기존 문서는 백업하고, 설계문서를 중심으로 개발을 진행하려고 해. 누락되는 값이나, 해상도가 낮아지는 설계는 없어야겠지?"

1. **외부 안건 점검(18라운드 전, 2026-09-25에 했다).** 원문과 판정은 `reviews/raw-round18-early-check.md`이고, 받은 지적은 안건에 '외부 점검'으로 더했다. codex와 antigravity에 같은 지시로 'PR-1·PR-2를 시작하기 전에 정해야 하는데 `reviews/round-18-agenda.md`에 없는 것'을 묻는다. 기준은 17라운드 커밋이고 검토자는 파일을 고치지 않는다. 지적은 검증자가 원문과 대조해 거른 뒤 안건에 더한다.
2. **단일 원장 증류(파일럿 먼저).** 파일럿은 오류 정책 영역이다(17라운드에 막 정해졌고 코드 목록 50행과 네 판의 이력이 있다). 원장 행의 칸은 번호, 영역, 결정(원문), 상태(현행, 대체됨과 대체한 번호, 열림), 출처(모든 위치), 닫은 사람(소유자 답의 위치 또는 원칙), 라운드다. 소유자가 파일럿으로 형식을 확정하면 전체로 넓힌다. 증류 중에 문서끼리의 모순이 드러나면 18라운드 안건으로 올린다.
3. **누락과 해상도를 막는 검사 세 겹.** (가) 원문 보존: 규칙, 값, 표는 요약하지 않고 옮기며 요약은 따로 칸을 둔다. (나) 기계 검사: 설계 문서(00–09, ADR, `open-questions.md`)의 코드 이름, 값, 번호, 오류 코드가 모두 원장에 있거나, 빠진 이유(대체됨, 관찰뿐)를 검증자가 확인해 적어야 한다. 2026-09-25의 거친 측정으로 서로 다른 것이 약 2,300개이고, 08 혼자 29%, 08·09·ADR을 합쳐 71%를 담는다. 소유자 답은 모두 원장 행 하나 이상에 인용되어야 한다. (다) 해상도 대조: 행마다 조건, 예외, 수량, 순서를 원문과 대조하고, codex와 antigravity가 옛 문서와 원장을 견주어 빠지거나 약해진 것을 새로 나오지 않을 때까지 찾는다.
4. **18라운드.** 안건은 `reviews/round-18-agenda.md`다. 청사진이 읽는 스키마의 범위(A), `controls` 식 언어 명세(B), 쓰기 의미론의 세부(C, `trim` 쓰기의 부수 효과 포함), 프로토타입 v7과 PR-0 실행 확인(D), ADR 0009의 성능 예산 수치와 '일정 수준'의 형태·수치, 번들 예산(E), 노드 구조의 설계 빈틈 넷(N2, N5, N6, N14), 공개 표면의 크기와 겉면의 관례, 전환 방식의 세부(레거시 디렉토리의 이름과 자리, 기존 시험·스토리북)가 있다. 결정마다 닫은 사람을 적는다. 소유자 답은 `reviews/round-18-owner-answers.md`에 원문 그대로 모은다(S1: 노드마다 타입에 맞는 parse 함수, 지금처럼). 제안(소유자 확인 대기): 옛 문서는 17라운드 커밋에서 더 고치지 않고, 18라운드부터 결정은 원장에 적는다.
5. **총검증.** 18라운드가 닫힌 뒤 codex와 antigravity로 교차검증한다(`reviews/round-18-agenda.md` §10).
6. **설계 완료 확정, ADR과 설계문서.** 원장에서 ADR과 설계문서를 쓰고 3의 검사를 한 번 더 거친다. 통과한 뒤에 옛 문서를 백업 디렉토리로 옮긴다. 소유자의 절 단위 통과는 08·09 대신 새 설계문서에서 한다(제안).
7. **PR-0**(문서 최종화, 프로토타입 v7, 시나리오 패키지 `@aileron/schema-form-scenarios`의 뼈대, vitest `test.projects`), 그다음 **PR-1**.
