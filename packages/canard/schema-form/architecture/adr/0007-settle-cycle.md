# ADR 0007 — 작업 루프: 표시 → 계산 → 파생 → 전이 → 커밋, 그 뒤에 통지와 검증

상태: 일부 수락 (5차 본문 — 2026-09-23, 10라운드 뒤). 가드가 보는 값과 순환의 처리는 수락. D-7·D-9·D-10은 수락(2026-09-22, `reviews/round-4.md` §4). D-8은 판별 프로퍼티가 사라지면서 지워졌다(`07-conclusions.md` 4.0의 4.14 행, 4.25). 예약 층의 단계와 순위, 정착 안의 에지 소비, 채움의 시점과 단위, 노드 게이트, 로드 때의 `controls.injectTo`·`controls.unsetValue`, 비수렴의 처리는 소유자의 답으로 확정되었다(`reviews/round-10-owner-answers.md`, `07-conclusions.md` 5.0). 같은 종류 규칙의 문서 순서와 나감의 비움은 13라운드 답 4·2(`reviews/round-13-owner-answers.md`)다. D-1–D-6은 원리에서 도출(`reviews/round-5-derivations.md` §3)되었고 `07-conclusions.md` 4.0의 판정을 따른다.

## 변경 이력

- 2026-09-24 — 16라운드 스웜 수렴(편집자 결정): 억제 비트의 배치 합산은 `fn`의 쓰기 묶음끼리만이다(`fn` 안의 `reset`의 로드는 묶음 밖, ADR 0008 §3, 09 §2.6의 열째).
- 2026-09-24 — 15라운드(`reviews/round-15-decisions.md`): `&` 축약을 `controls` 그룹 표기로, 조각 식의 기준점을 호스트로, 맨 폼 전용 키를 `options`·`presentation` 그룹으로 바꿨다.
- 2026-09-24 — 13라운드 소유자 답(`reviews/round-13-owner-answers.md`)을 반영했다: 코어에 글로벌 잠금 없음, 나감의 비움(선택, 기본 유지), 접두 규칙(제어 키만 `&`), 같은 종류 규칙의 문서 순서.
- 2026-09-23 — 5차 본문. `07-conclusions.md` 4.22–4.30과 9·10라운드(`reviews/round-9-derivation.md` §3–§4, `reviews/round-10-derivation.md` §4–§5, `reviews/round-10-owner-answers.md`)를 반영했다. 상태 칸 `selection`과 union의 판별 프로퍼티를 지웠다(4.25). 파생 단계에 `&unsetValue`, 같은 대상 규칙과 순위, 정착 안의 에지 소비를 넣었다(4.23·4.30). 전이 단계를 노드가 생길 때의 채움으로 다시 썼다(4.22·4.24). 호스트 바퀴 상한에 노드 게이트를 더했다(4.23). 억제 옵션을 `SetValueOption.DisableAutomaticWrites`로 바꾸고 범위를 예약 층의 자동 쓰기 전부로 넓혔다(4.0의 4.14 행, 6.1). 비수렴은 원본 B 커밋, `diagnostics.status = 'budgetExceeded'`, 개발 모드 throw, 프로덕션 신호만으로 적었다(5.0의 1). D-8을 지웠고 D-6을 현행 유지로 고쳤다(5.0의 4). 커밋 단계에 `&resetInteraction`을 두었다(5.0의 5). 답으로 닫힌 미결을 닫힘으로 표시했다.
- 2026-09-23 — 4차 본문. 3·4·5라운드 반영(E1–E19, F1–F32, D-1–D-10 도출). 이전 본문의 "확인된 결함" 절은 본문에 흡수하고 삭제.
- 2026-09-22 — 2라운드(`reviews/round-2.md`)의 S3과 상한·예외 문구의 결함을 "확인된 결함" 절에 적었다. 3차안은 `reviews/round-3-spec.md` A.
- 2026-09-22 — 상한에 걸렸을 때의 처리를 "쓰기를 버리고 throw"에서 "값은 받아들이고 형상만 고정, 개발 모드에서 에러"로 바꿨다. 이유: 2라운드 검토에서 세 리뷰어가 독립적으로 "비제어 입력의 DOM은 이미 바뀌었는데 트리만 되돌아간다"를 지적했고, 소유자가 입력을 막지 않는다는 원칙을 밝혔다.
- 2026-09-22 — 1차안("고정점에 이를 때까지 도는 Write → Resolve → Reconcile → Derive → Commit 사이클, 가지치기는 값의 삭제")을 대체했다. 이유: 적대적 검토 R6(진동), R7(형제 순서에 따른 결과), R8(입력 도중의 데이터 파괴)(`reviews/round-1.md`). 정적 순환 금지 제안에 대해 소유자는 런타임 상한을 택했다.

## 합의 근거

- 라이프사이클 — 소유자: "마이크로태스크와 매크로태스크 기반의 라이프사이클을 노드 내부에서만큼은 원칙에 맞는 직관적인 데이터 흐름을 가졌으면 한다. 지금은 서로 막 주고받는 게 많다." / "react 파이버처럼 node가 동작하도록."
- 가드가 보는 값 — 소유자: "가드는 방출 값을 보는 게 맞다. 빈 문자열을 '있다'고 보는 게 오히려 이상하다. 그렇게 처리할 거면 omitEmpty를 끄면 되고, 그럼 방출값으로 통일해도 일정하다."
- 순환 — 소유자: "injectTo의 무한루프나 derived 무한루프 방어처럼 했으면 한다. 미리 알고 처리한다기보단, 몇 회 루프를 돌면 경고하고 error를 throw하도록. react의 hook처럼. 추가적인 방어를 해도 되는데 애드훅하게 하는 것보단 돌려보고 터지는 걸 개발 단계에서 알려주는 게 낫다."
- 순환(10라운드) — 소유자: "루프의 가능성을 제한하지는 않는다. … 그 상한값을 초과하면 적절한 error를 표시한다. 이는 react의 hook과 동일한 설계를 갖는다." / "구태여 막지 않을 뿐이지 루프를 만드는 걸 권하는 설계는 절대 아닙니다."

## 맥락

현재 복잡함의 원인은 내부 로직이 이벤트를 구독해서 상태를 움직인다는 점이다(`01-current-structure.md` §5). BranchStrategy가 자기 host의 `UpdateComputedProperties`를 구독해 분기를 바꾸고, computed 의존성은 `UpdateValue`를 구독하며, `oneOf`와 `active`가 서로 다른 라이프사이클을 탄다. 이를 막으려고 불린 여섯 개가 쌓였고, 결과가 원인보다 먼저 배달되는 역전이 실측됐다.

## 결정

### 1. 정착 한 번 — 일곱 단계

쓰기(또는 쓰기의 묶음)마다 **한 곳에서, 고정된 순서로** 다음을 돈다.

| 단계 | 하는 일 | 예산 |
| ---- | ------- | ---- |
| 표시 | 쓰기를 받은 노드의 `raw`·`extras`를 갱신하고 조상 경로의 재계산 목록에 등록한다 | — |
| 계산 | 루트에서 한 번 내려간다. 재계산 목록의 자식을 먼저 완료한 뒤 자기 `local`·`emit`·유효 스키마를 만든다. 호스트는 조각과 노드를 §2의 절차로 정한다. 게이트는 둘이다 — `if`(검증기 플러그인이 컴파일)와 `controls.active`(표현식). 노드 게이트는 조각 게이트와 같은 장치다(`07-conclusions.md` 4.24). 계산의 끝, 최종 트리에서 `controls.visible`·`controls.readOnly`·`controls.disabled`·표준 `readOnly`를 한 번 결정한다. 코어에 글로벌은 없고 상태 키는 그 노드에만 걸린다(원장 §4, 13라운드 답 1). **원본을 읽기만 한다**(E1) | 호스트 바퀴 = 게이트 가진 조각 수 + 노드 게이트 수 + 1 |
| 파생 | 완성된 트리에서 `controls.derived`(자기 값 덮기), `controls.injectTo`(대상에 대한 전체 교체), `controls.unsetValue`(자기 값을 없음으로)를 평가한다. 라운드마다 후보를 모아 **대상마다 하나만** 적용한다. 순위는 `controls.unsetValue` > `controls.derived` > `controls.injectTo` > 채움이고, 같은 순위끼리는 원천(선언) 노드의 문서 순서에서 나중이 이기고, 같은 노드에 걸린 선언끼리는 층(조각의 `controls` < `controls.children` 항목 < 노드 자신)에서 세부가 이기며, 같은 층이면 조각의 전순서에서 나중이 이긴다(소유자 답 7·16·17 "뒤가 앞을 덮는다"). 진 쓰기는 버리며 그 에지도 소비한다(소비하지 않으면 다음 라운드에 진 규칙이 이겨 순위가 무의미해진다, 원장 §4). 한 규칙의 에지는 원천 값의 한 번의 변화에 대해 한 정착 안에서 한 번만 소비한다(재발화 금지, 4.30). 기준점은 그 규칙이 이 정착에서 마지막으로 소비한 원천 값이다. 원천이 다른 값으로 다시 바뀌면 새 에지이고, 진짜 순환은 그래서 예산에 잡힌다(원장 §4, 소유자 답 4). 같은 대상에 규칙이 둘 와도 경고하지 않는다(13라운드 답 4). 한 정착에서 대상에 더 높은 순위의 쓰기가 이미 적용되었으면 뒤 라운드의 낮은 순위 후보는 버리고 그 에지를 소비한다(소유자 답 9의 뜻: 순위는 라운드가 아니라 정착 단위다). 쓰기가 나오면 표시로 | 라운드 25 |
| 전이 | **생긴 노드** — 직전 커밋의 형상에 없고 이번 최종 형상에 있는 노드 — 의 **없음**인 값에 채움(`controls.default` > `default`)을 쓴다. 노드 단위이며, 이미 있던 노드는 새 조각이 켜져도 채우지 않는다. 중간 라운드의 채움은 그 노드가 최종 형상에 없으면 버린다. 생긴 노드의 `controls.unsetValue`가 참이면 채우지 않는다(순위는 단계를 가로지른다, 원장 §4). 나감 정책이 참으로 정해진 노드가 **나가면**(직전 커밋의 형상에 있었고 이번 최종 형상에 없으면, 하위 트리 포함, 선언이 하나라도 켜져 있는 공유 노드는 제외) 한 번 없음으로 만든다. 로드에는 나감이 없다(원장 §3). → 표시로 | 라운드 = 게이트 가진 조각 수 + 노드 게이트 수 + 1 (파생과 **별도**, F2. 채움이나 나감의 비움이 다음 라운드를 부르는 것은 그 쓰기가 게이트를 뒤집어 새 노드를 내거나 노드를 내보낼 때뿐이다, 원장 §4) |
| 커밋 | 계산 결과를 트리에 반영하고 정착을 확정한다. `controls.resetInteraction`(옛 `&pristine`, `dirty`·`touched` 초기화)을 최종 트리의 식 값으로 판정한다. 원본에는 쓰지 않는다. 배달 집합 전체의 `revision`을 한 번에 올리고(F16) 단조 **커밋 번호**를 매긴다(F28) | — |
| 통지 | 루트 디스패처가 문서 순서 위 → 아래로 1회 배달한다. 유효 스키마가 바뀐 노드도 배달 집합에 든다(ADR 0008) | 최외곽 진입의 되먹임 사슬당 파동 25 (ADR 0008 §2 규칙 4), `onChange` 중첩 25 (ADR 0008 §5) |
| 검증 | `validator(작성된 스키마, 방출 값)`. **커밋 번호**를 스탬프하고 비동기로 요청하며, 늦게 온 결과는 버린다. 실행은 마이크로태스크에 모아 최신 커밋 번호 하나만 돌린다(14라운드 답 O-6) | — |

- **표시부터 커밋까지는 동기·단방향이다.** 비동기는 경계(검증·React·`onChange`)에만 있고 이벤트는 출력 전용이다. 배치는 표시 N번에 계산·커밋 1번, 통지 1번이다.
- **`if/then/else`, 분기 조각의 게이트, `controls.active`(노드·조각)는 같은 계산을 탄다.** 라이프사이클이 하나다. 폼은 `oneOf`·`anyOf`로 분기를 고르지 않는다. 게이트 없는 분기는 존재만 더하고, 작성자가 명시한 `controls.discriminator`는 청사진 단계에서 분기별 `controls.active`로 바뀐다(4.25).
- 원본을 쓰는 것은 파생과 전이뿐이고 둘 다 표시로 돌아간다. 그래서 커밋된 트리는 (스키마, 트리 전체의 `raw`·`extras`)의 순수 함수다(F9, P3). 상태는 이 둘뿐이다.
- **비수렴.** 정착의 세 예산(호스트 바퀴, 파생 라운드, 전이 라운드) 가운데 하나라도 상한을 넘기면, 그 정착의 자동 쓰기 — 채움, `controls.derived`, `controls.injectTo`, `controls.unsetValue`, 나감의 비움 — 를 모두 뺀 **원본 B**를 커밋한다. `controls.unsetValue`가 지운 값도 되돌린다(4.0의 4.11 행). 결과는 `diagnostics.status = 'budgetExceeded'`로 알린다. 커밋되는 형상은 원본 B로 한 번 더 계산한 것이며, 그 바퀴도 상한에 걸리면 마지막 바퀴의 활성 집합으로 고정한다. `diagnostics.iterations`는 초과한 예산이 쓴 반복 횟수(상한값)다. 개발 모드는 최외곽 진입 끝에서 throw한다. 프로덕션은 신호만 낸다(5.0의 1)(ADR 0014 제안 3판이 대체한다. 확정 뒤 갱신). 예산 초과 상태는 다음 로드까지 남고 그 동안 `<Form>`의 제출은 거부된다(14라운드 답 O-2). 리스너 되먹임 파동과 `onChange` 중첩의 초과는 ADR 0008 §2 규칙 4와 §5를 따른다. 상한은 루프를 잇는 고리 하나만 끊는다 — 정착 예산은 자동 쓰기, 되먹임 파동은 그 파동의 되먹임 쓰기, `onChange` 중첩은 그 호출(원장 §4). 루프의 가능성을 막지는 않지만 루프를 권하는 설계도 아니다. 2라운드의 S3(begin/complete 두 패스)은 사라졌다 — 조각은 완료 단계에서 정해지고 출발점이 고정된다.
- **비용의 상한(G6, 14라운드).** 파생·전이·커밋과 원본 B의 기록은 이번 정착의 재계산 목록과 자동 쓰기 기록만 순회한다(생긴 노드의 채움과 나간 하위 트리의 순회는 제외). 트리 전체 순회는 로드에서만 허용한다. 원본 B는 스냅숏이 아니라 자동 쓰기의 되돌림 기록으로 만든다. 청사진이 `controls`의 식과 `controls.watch`의 경로에서 역의존 표(경로 → 그 경로를 읽는 식을 가진 노드)를 정적으로 만들고, 표시 단계는 쓰기 노드의 조상과 그 역의존 노드의 조상을 재계산 목록에 넣는다(ADR 0005의 역색인 기각은 검증기 `if` 게이트의 건너뛰기에만 해당한다). 한 정착의 라운드 상한은 파생 25 + 전이 상한(트리 전체의 게이트 가진 조각 수 + 노드 게이트 수 + 1)이며, 전이 라운드는 쓰기를 낸 라운드만 세고 `if/then/else`는 게이트 하나로 세며, 파생 라운드는 전이 뒤에도 이어 센다(곱하지 않는다). 자식 호스트의 재계산은 정착당 덧씌움 집합의 서로 다른 값 수만큼이며 그 합은 호스트 바퀴 예산에 함께 센다. `if` 게이트의 컴파일은 작성된 스키마의 위치당 1회이며 폼 인스턴스 사이에 공유한다(ADR 0004).

### 2. 호스트가 조각을 정하는 법

| 규칙 | 내용 | 근거 |
| ---- | ---- | ---- |
| 출발점 고정 | `A := 게이트 없는 조각 ∪ 상속 overlay`. 게이트 없는 조각은 본체 `properties`, 게이트 없는 `allOf` 항목, 게이트 없는 `oneOf`·`anyOf` 분기다. 직전 커밋의 `active`는 **읽지 않는다** — 생긴 노드의 판정(전이)과 평가 순서 힌트(F13)로만 쓴다. 노드 게이트도 게이트 가진 조각처럼 꺼진 채 출발한다 | E1·E6 |
| 조각은 트리 | 중첩 조각은 감싸는 조각이 활성일 때만 순회한다. 전순서(호스트에서 조각까지의 경로를 (키워드 순위, 배열 인덱스) 쌍의 열로 보고 사전식으로 비교한 것. 키워드 순위는 본체 `properties` < `allOf` 항목 < `if/then/else` < `oneOf`·`anyOf` 분기. JSON 키 순서에 기대지 않는다). 노드 게이트는 그 노드를 선언한 조각 바로 뒤에, 같은 조각 안에서는 선언 순서로 든다 | E3 |
| 매 바퀴 전부 | 한 바퀴에 **모든** 게이트(조각 게이트와 노드 게이트, 켜진 것과 꺼진 것 모두)를 평가한다. 한 바퀴 안의 켜짐·꺼짐은 뒤의 게이트에 즉시 반영된다(가우스-자이델) | E4, F3 |
| 게이트 입력 | 게이트는 **검증기가 볼 값**을 본다 — `A`로 합성한 local에 투영(`omitEmpty`·`omitTrailing`·null)을 적용한 것. `if`는 검증기 플러그인이 컴파일한 것으로, `controls.active`는 표현식으로 평가한다. `extras`(선언되지 않은 키의 값)도 게이트 입력에 든다(원장 §5). 폼은 `if`의 내용에 관여하지 않는다. 예외는 하나: 호스트 자신의 `raw`가 비객체이면 `G = {}` | E5, P1 |
| 비단조 재평가 | 참이면 켜고 거짓이면 끈다. `A`가 바뀌면 다시 돈다. 상한 = **게이트 가진 조각 수 + 노드 게이트 수 + 1** | E6, F3, 4.23 |
| 상한 초과 | 원본 B를 커밋하고(§1), 형상은 원본 B로 한 번 더 계산하며 그 바퀴도 상한에 걸리면 마지막 바퀴의 `A`로 고정한다. 결과는 `diagnostics.status = 'budgetExceeded'`로 알린다. 자식 호스트의 초과도 루트의 `diagnostics`에서 관측된다. 개발 모드는 최외곽 진입 끝에서 throw한다(ADR 0014 제안 3판이 대체한다. 확정 뒤 갱신) | E6, F12, 5.0의 1 |
| 금지 조각 없음 | `false` 서브스키마와 `not`은 아무것도 선언하지 않는다. 폼은 읽지 않는다 | P1′, D-3 |
| 상속 overlay | 끌어올린 조각의 `then`이 손자를 선언하면 청사진이 그 선언을 자식 호스트의 overlay로 귀속시킨다. 부모의 바퀴가 overlay 집합을 바꾸면 자식을 **바퀴 안에서** 재계산하되 (`raw`, overlay 집합)으로 메모한다 | E12, F5 |
| 합성 | `local := compose(A)`, `emit := project(local)`. `delete` 없이 조각이 선언한 키만 패치한다(F13). `emit`의 키 순서는 스키마 선언 순서, `extras`는 뒤에 받은 순서 | E10, F13, 4.12 |

**고정점이 없는 스키마는 지원 범위 밖이다.** 게이트 의존 관계에 부정을 포함한 순환이 있으면 결과는 상한에서 결정적이되 임의적이고 `budgetExceeded`로 표시된다(F3). 대표적인 진동은 `if: { not: { required: ['x'] } }, then: { properties: { x: { default: 1 } } }`다. 채움이 노드가 생길 때 한 번이 된 뒤로 이런 자기 게이트 순환은 런타임에는 사라지고 로드에만 남는다(`07-conclusions.md` 3.7). 고정점이 둘 이상이면(R7) 선언 순서에 대해 결정적인 하나로 정착하며, 어느 경우에도 원본은 기본으로 남는다. 나감 정책이 참으로 정해진 노드는 나갈 때 한 번 비운다(원장 §3).

### 3. 쓰기 — 호출자가 종류를 선언한다

| 사건 | 종류 | 자식 원본에 미치는 것 |
| ---- | ---- | -------------------- |
| 리프 입력, `setValue(V, SetValueOption.Merge)` | 부분 쓰기 | 지정한 것만 바뀐다. 입력의 `onChange(undefined)`와 `Merge`로 키에 쓴 `undefined`는 그 키를 없음으로 만든다(`extras` 포함). V가 통째로 준 배열은 통째 교체다 |
| `setValue(V)`(기본 `Overwrite`), `defaultValue`, `reset()` | **전체 교체(로드)** | V에 없는 자식의 원본은 **없음**이 된다. `null`·`17`도 V다 — 키가 없으므로 모든 자식이 없음이 된다 |
| `controls.injectTo` | 전체 교체(대상에), 작성자 선언 | 대상에 대해 위와 같다. 원천의 방출 값이 바뀔 때(에지, 아래) |
| `controls.derived` | 자기 값 덮기, 작성자 선언 | 의존 값이 바뀔 때(에지). 사용자가 쓴 값은 다음 의존 변화 또는 그 노드가 다시 생길 때까지 남는다 |
| `controls.unsetValue` | 자기 값을 없음으로, 작성자 선언 | 식이 거짓에서 참이 되는 순간. 입력은 남는다. 참에서 거짓이 되면 아무 일도 하지 않는다 |
| 나감의 비움(선택, 기본 꺼짐) | 나간 노드의 값을 없음으로, 작성자·호출자 선언(정책 키) | 노드가 나갈 때 한 번(하위 트리 포함, 공유 노드 제외). 로드에는 없다. 전이 단계, 최종 형상 기준. 정책 키(`unsetOnInactive`)는 노드 > `controls.children`의 `controls` > 조각의 `controls` > Form 속성, 같은 층은 하나라도 유지면 유지 |
| 배열 `push`/`remove`/`update` | 구조 연산 | 그 아이템만. `push`로 생긴 아이템은 생긴 노드이므로 채움을 받는다 |
| 채움 | 노드 생성 사건, 작성자 선언 | 노드가 **생길 때** 없음이면 `controls.default` > `default` > 없음. 이미 있던 노드는 새 조각이 켜져도 다시 채우지 않는다. 지운 값은 다시 채워지지 않는다. 채움 값은 그 노드가 처음 채워지는 전이 라운드의 유효 스키마에서 읽는다. 뒤 라운드에 켜진 조각의 `default`는 쓰지 않는다 |

쓰기의 종류는 호출자가 선언한다 — 비트마스크 `SetValueOption`이다(D-4, `07-conclusions.md` 6.1·6.2의 N1). core는 추론하지 않는다.

```ts
setValue(value, option?: SetValueOption) // Overwrite(기본) | Merge | DisableAutomaticWrites | EnableAutomaticWrites
reset(option?: SetValueOption)
<Form disableAutomaticWrites />          // 마운트(defaultValue)와 호출의 기본값
```

- 같은 비트가 `reset`과 마운트(`defaultValue`)에도 든다. 호출에 억제 비트가 둘 다 없으면 Form 속성 `disableAutomaticWrites`를 따르고, 호출 옵션은 Form 속성을 **양방향으로** 이긴다. 비트가 둘인 이유가 이것이다(상속·끄기·켜기 세 상태). 한 호출에 둘 다 주거나 한 배치에 서로 다른 억제 값이 섞이면 **억제가 이긴다**(`fn` 안의 `reset`의 로드는 묶음 밖이다, ADR 0008 §3).
- `Merge`는 V에 없는 키를 로드하지 않는다. V가 통째로 준 배열은 통째 교체다. 그 아이템 가운데 직전 커밋의 형상에 없던 노드는 생긴 노드로서 채움을 받으며, 어떤 아이템이 생긴 것인지는 원장 §6의 배열 아이템 설계 항목이다. `Merge`에 준 억제 비트는 그 호출이 일으킨 자동 쓰기에 적용되므로 이 채움도 막는다(원장 §3).
- 억제의 범위는 그 호출이 일으킨 예약 층의 자동 쓰기 전부다 — 채움, `controls.derived`, `controls.injectTo`, `controls.unsetValue`, 나감의 비움. 로드(전체 교체, `reset`, 마운트)뿐 아니라 `Merge`에 주면 `Merge`가 통째로 준 배열의 아이템 채움과 그 `Merge`가 촉발한 `controls.derived`도 막는다. 로드된 값 자체는 막지 않는다. `controls.active`의 방출 제외는 쓰기가 아니라 투영이므로 범위 밖이고, 정책이 참으로 정해진 노드의 나감 비움은 범위 안이다. 뒤이은 사용자 입력·리스너가 일으킨 정착은 다른 호출이므로 억제가 듣지 않는다(F25, 원장 §3).
- **`Refresh`는 공개 옵션이 아니다.** core가 **출처**로 판단한다 — 그 노드 자신의 입력에서 온 부분 쓰기는 내지 않고 그 밖의 쓰기가 원본을 바꾸면 낸다(F7, T-2). 타이핑은 리마운트되지 않는다.
- 호스트의 `raw`를 비우는 것은 사용자·호출자의 부분 쓰기뿐이고 **그 자식의 투영된 emit이 존재하게 될 때만** 비운다(F1). 자동 쓰기는 비객체 호스트의 원본을, core는 호출자가 넘긴 객체를 건드리지 않는다(F10·F24, T-12·T-19).
- 전체 교체는 **에지와 생김의 기준(직전 커밋)을 비운다**(F4). 그래서 로드에서는 최종 형상의 노드가 모두 생긴 노드로서 채움을 받는다. `controls.injectTo`는 직전 값이 없으므로 발화한다(`fire`, 소유자 동의). `controls.unsetValue`는 로드된 값으로 평가해 참이면 지우고 거짓이면 둔다(소유자 답 21). 런타임에는 둘 다 에지에서만 쓴다. `controls.injectTo`는 **원천의 방출 값이 직전 커밋과 다를 때**, `controls.unsetValue`는 식이 거짓에서 참이 될 때다. 그래서 사용자의 부분 쓰기를 되돌리지 않는다(F11). 한 정착 안에서 에지는 한 번만 소비된다(§1).

core가 스스로 원본을 "고치는" 일은 없다(P2). 원본에 쓰는 주체는 사용자 입력, 호출자의 `setValue`·`reset`, 그리고 작성자가 선언한 규칙이다. 규칙은 노드가 생길 때의 채움과 그 원천 `controls.default`·`default`, 예약 층의 `controls.derived`·`controls.injectTo`·`controls.unsetValue`, 그리고 정책이 참으로 정해진 노드의 나감 비움이다. "core는 값을 고치지 않는다"의 예외는 **방출 정책**(비활성 제외, `omitEmpty`·`omitTrailing`)뿐이고 원본을 건드리지 않는다(P4).

### 4. 소유자 결정 — D-7, D-9, D-10

상태: **수락 (2026-09-22, `reviews/round-4.md` §4)**. D-8(판별 프로퍼티에 암묵 `default` 없음)은 판별 프로퍼티와 함께 지워졌다(`07-conclusions.md` 4.0의 4.14 행, 4.25). 빈 값에서 분기가 켜지지 않는 것은 작성자가 분기 안 `if`의 `required`에 조건 프로퍼티를 넣거나, 분기에 `controls.active`를 적거나, union에 `controls.discriminator`를 적을 때 성립한다(ADR 0002).

| # | 결정 |
| - | ---- |
| D-7 | `setValue(getValue())`는 (a) — 전체 교체 뒤 **채움이 그대로 적용된다.** 로드는 생김의 기준을 비우므로 사용자가 지운 키의 노드도 생긴 노드로서 다시 채워지고, 그래서 멱등이 아니다. 필요하면 호출 단위 억제 비트 `DisableAutomaticWrites`로 끈다 |
| D-9 | `RequestRemount`는 공개 명령으로 남는다 — 사용자가 서브트리의 입력을 제어·비제어와 무관하게 최신화하는 도구(T-18) |
| D-10 | 루트 `onChange`는 **최외곽 동기 진입당 1회**, 그 진입의 마지막 파동 뒤 최종 emit으로. 디바운스 없음 |

- D-10의 구현은 **진입 깊이 카운터**다(`spikes/work-loop/REPORT-v4c.txt`). 배치 없는 연속 `setValue` 3회는 통지 3회, `batch`로 묶으면 1회다(C-8·C-9).
- **React 이펙트의 쓰기는 새 진입이다.** 레이아웃·패시브 어느 쪽이든 키 입력당 진입 2·통지 2·검증 2이고(C-10, React 19 + jsdom 7/7, `spikes/events/entry.spike.test.tsx`) 스택 기반 정의로는 합칠 수 없다. core가 풀 문제가 아니라 **문서화 대상**이다 — 파생 값은 `controls.derived`/`controls.injectTo` 또는 스토어 리스너로 쓴다(그러면 1·1·1). 첫 통지의 stale 검증 결과는 커밋 번호 스탬프(F28)가 버린다.

### 5. 원리에서 도출된 것 — D-1 ~ D-6

상태: **원리에서 도출(`reviews/round-5-derivations.md` §3).** `07-conclusions.md` 4.0의 4.14 행에 따라 D-5가 넓어졌고, D-6은 소유자 답 4로 현행 유지가 되었다.

| # | 도출 |
| - | ---- |
| D-1 | `null`은 키 없는 전체 교체다(P2). 자식 원본이 없음이 되고, 로드는 생김의 기준을 비우므로 형상의 노드가 채움을 받아 빈 상태를 만든다. #338의 null 계약은 §3 표의 귀결이다 |
| D-2 | 형상은 상태의 순수 함수(P3)이므로 출발점 고정 + 비단조 재평가. 고정점이 없는 스키마는 지원 범위 밖이고 `budgetExceeded`로 관측 가능하다 |
| D-3 | `false`와 `not`은 값의 유효성을 정하는 문법이므로 폼이 읽지 않는다(P1′). 숨기려는 작성자는 `controls.active`를 쓴다 |
| D-4 | 쓰기의 종류는 호출자가 선언한다(P2). 기본은 `Overwrite`이고 `Refresh`는 core가 출처로 판단한다(F7) |
| D-5 | 억제 비트는 그 호출(로드와 `Merge`)이 일으킨 예약 층의 자동 쓰기 전부(채움, `controls.derived`, `controls.injectTo`, `controls.unsetValue`, 나감의 비움)를 끈다. 로드된 값은 막지 않는다. Form 속성 = 기본, 호출 옵션 = 양방향 재정의, `reset`·마운트 포함 |
| D-6 | 참조 그룹(`options.virtual`)은 현행 유지다(`options` 그룹에 두되 `required` 재작성은 버리고 `options` 그룹째 검증기 앞에서 지워진다, 12라운드 답 8). 소유자: "virtual 은 스키마로 선언되는건 아니니까 그냥 둡시다. 유효성검증도 영향 없고." 참조 그룹 노드로의 전환은 하지 않는다 |

## 비용

| 시나리오 | 3.1판 | 대조 | 출처 |
| -------- | ----- | ---- | ---- |
| 키 입력, 평면 1,000 | 1.39 µs | 3차안 1.35 µs | `reviews/round-4.md` §2.1 |
| 분기 전환(4라운드 측정 시나리오) | 108 µs | 3차안 89 µs | `reviews/round-4.md` §2.1 |
| 루트 통째 쓰기 10,000 × 5 | 12.0 ms | 현재 217 ms | `reviews/round-4.md` §2.1, `reviews/round-2.md` §2 |
| 조건부 폼 생성 (가드 200개) | 23.0 ms (트리 585 µs + AJV 컴파일 22.4 ms) | 현재 13.4 ms | `reviews/round-2.md` §2 |
| 진입 깊이 카운터 | 측정 스프레드 안 (3–4%) | — | `spikes/work-loop/REPORT-v4c.txt` |

노드별 장부가 루트 통째 쓰기를 3차안보다 58% 늦춘다. 조건부 폼 생성은 재설계가 지는 유일한 지점이다.

## 결과

- 잠금용 불린들과 마이크로태스크 flush가 필요 없어지고, 두 단계 하네스는 단일 단계가 된다(T-5, F23).
- "한 번의 쓰기가 두 번 배달된다", "결과가 원인보다 먼저 배달된다"가 구조적으로 사라진다.
- 배열 구조 변경 API(`push`/`remove`/`clear`)가 동기가 된다. 파괴적 변경이다(T-7).
- 라이프사이클을 §1의 표 한 장으로 읽는다(G5). Q3(편집 중 상태와 가드가 보는 값)이 닫혔다.

## 미결

- **배열의 `Merge`가 무엇인가.** 닫힘. `Merge`가 준 배열은 통째 교체다(`07-conclusions.md` 4.0의 4.7 행).
- **`setValue(undefined)`의 정의.**
- **`budgetExceeded`에서 제출을 막는가.** 닫힘(14라운드 답 O-2). 다음 로드까지 남고 그 동안 `<Form>`의 제출은 거부된다. throw 정책은 ADR 0014(제안 3판).
- **`push()`로 만든 아이템이 로드인가.** 일부 닫힘. `push`로 생긴 아이템은 생긴 노드로서 채움을 받는다. 배열 아이템의 생김 세부(R13, `contains`·`prefixItems`)와 입력 컴포넌트의 객체 통째 쓰기는 남는다(`03-mental-model.md` §6).
- **억제 옵션의 이름.** 닫힘. `SetValueOption.DisableAutomaticWrites`/`EnableAutomaticWrites`, Form 속성 `disableAutomaticWrites`(`07-conclusions.md` 6.1). JSDoc이 "로드"를 정의한다.
- **값이 아닌 입구 (R15).** 그대로다. `setState`(dirty/touched)와 명령 시그널은 원본 쓰기가 아니다. `controls.resetInteraction`도 커밋 단계에서 판정되며 원본에 쓰지 않는다.
- **유효 스키마의 변경을 알리는 이벤트.** 닫힘. 유효 스키마가 바뀐 노드를 통지의 배달 집합에 넣는다(ADR 0008, `07-conclusions.md` 4.0의 4.9 행). 통지 표면의 세부는 `03-mental-model.md` §6.
- **검증을 입력 경로에서 떼어 내는 법 (R19).** 커밋 번호 스탬프는 경합만 막는다.
- Q13(`contains`·`prefixItems`), Q14(emit의 키 순서), Q15(직전 `active`를 출발 가설로).

## 되돌림 가능성

낮다. ADR 0006과 함께 노드 코어의 기반이다.
